#![doc = " Trait implementations for projective2."]
#![doc = ""]
#![doc = " This file is auto-generated by clifford-codegen."]
#![doc = " Do not edit manually."]
use super::types::{Flector, Line, Motor, Point, Scalar, Trivector};
#[allow(unused_imports)]
use crate::ops::{
    Antidot, Antiproject, Antireverse, Antisandwich, Antiwedge, BulkContract, BulkExpand, Dot,
    InverseAntisandwich, InverseSandwich, Involute, LeftContract, Project, Reverse,
    RightComplement, RightContract, Sandwich, ScalarProduct, Transform, Versor, VersorInverse,
    Wedge, WeightContract, WeightDual, WeightExpand,
};
use crate::scalar::Float;
#[allow(unused_imports)]
use crate::wrappers::Unitized;
use approx::{AbsDiffEq, RelativeEq, UlpsEq};
use std::ops::{Add, Mul, Neg, Sub};
impl<T: Float> Add for Flector<T> {
    type Output = Self;
    #[inline]
    fn add(self, rhs: Self) -> Self {
        Self::new_unchecked(
            self.s() + rhs.s(),
            self.d() + rhs.d(),
            self.nx() + rhs.nx(),
            self.ny() + rhs.ny(),
        )
    }
}
impl<T: Float> Sub for Flector<T> {
    type Output = Self;
    #[inline]
    fn sub(self, rhs: Self) -> Self {
        Self::new_unchecked(
            self.s() - rhs.s(),
            self.d() - rhs.d(),
            self.nx() - rhs.nx(),
            self.ny() - rhs.ny(),
        )
    }
}
impl<T: Float> Neg for Flector<T> {
    type Output = Self;
    #[inline]
    fn neg(self) -> Self {
        Self::new_unchecked(-self.s(), -self.d(), -self.nx(), -self.ny())
    }
}
impl<T: Float> Mul<T> for Flector<T> {
    type Output = Self;
    #[inline]
    fn mul(self, scalar: T) -> Self {
        self.scale(scalar)
    }
}
impl Mul<Flector<f32>> for f32 {
    type Output = Flector<f32>;
    #[inline]
    fn mul(self, v: Flector<f32>) -> Flector<f32> {
        v.scale(self)
    }
}
impl Mul<Flector<f64>> for f64 {
    type Output = Flector<f64>;
    #[inline]
    fn mul(self, v: Flector<f64>) -> Flector<f64> {
        v.scale(self)
    }
}
impl<T: Float> Mul<Flector<T>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn mul(self, rhs: Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            -(rhs.d() * self.d()) + rhs.s() * self.s(),
            rhs.d() * self.s() + rhs.s() * self.d(),
            -(rhs.d() * self.ny())
                + rhs.nx() * self.s()
                + rhs.ny() * self.d()
                + rhs.s() * self.nx(),
            -(rhs.nx() * self.d())
                + rhs.d() * self.nx()
                + rhs.ny() * self.s()
                + rhs.s() * self.ny(),
        )
    }
}
impl<T: Float> Mul<Line<T>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn mul(self, rhs: Line<T>) -> Flector<T> {
        Flector::new_unchecked(
            -(rhs.d() * self.d()),
            rhs.d() * self.s(),
            -(rhs.d() * self.ny()) + rhs.nx() * self.s() + rhs.ny() * self.d(),
            -(rhs.nx() * self.d()) + rhs.d() * self.nx() + rhs.ny() * self.s(),
        )
    }
}
impl<T: Float> Mul<Motor<T>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            rhs.tx() * self.d() + rhs.ty() * self.s(),
            -(rhs.ty() * self.d()) + rhs.tx() * self.s(),
            -(rhs.ps() * self.d())
                + -(rhs.tx() * self.nx())
                + rhs.r() * self.s()
                + rhs.ty() * self.ny(),
            rhs.ps() * self.s() + rhs.r() * self.d() + rhs.tx() * self.ny() + rhs.ty() * self.nx(),
        )
    }
}
impl<T: Float> Mul<Point<T>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Point<T>) -> Motor<T> {
        Motor::new_unchecked(
            rhs.x() * self.s() + rhs.y() * self.d(),
            -(rhs.x() * self.d()) + rhs.y() * self.s(),
            -(rhs.y() * self.nx()) + rhs.w() * self.s() + rhs.x() * self.ny(),
            rhs.w() * self.d() + rhs.x() * self.nx() + rhs.y() * self.ny(),
        )
    }
}
impl<T: Float> Mul<Scalar<T>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn mul(self, rhs: Scalar<T>) -> Flector<T> {
        Flector::new_unchecked(
            rhs.s() * self.s(),
            rhs.s() * self.d(),
            rhs.s() * self.nx(),
            rhs.s() * self.ny(),
        )
    }
}
impl<T: Float> Mul<Trivector<T>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Trivector<T>) -> Motor<T> {
        Motor::new_unchecked(
            T::zero(),
            T::zero(),
            -(rhs.ps() * self.d()),
            rhs.ps() * self.s(),
        )
    }
}
impl<T: Float> Add for Line<T> {
    type Output = Self;
    #[inline]
    fn add(self, rhs: Self) -> Self {
        Self::new_unchecked(
            self.d() + rhs.d(),
            self.nx() + rhs.nx(),
            self.ny() + rhs.ny(),
        )
    }
}
impl<T: Float> Sub for Line<T> {
    type Output = Self;
    #[inline]
    fn sub(self, rhs: Self) -> Self {
        Self::new_unchecked(
            self.d() - rhs.d(),
            self.nx() - rhs.nx(),
            self.ny() - rhs.ny(),
        )
    }
}
impl<T: Float> Neg for Line<T> {
    type Output = Self;
    #[inline]
    fn neg(self) -> Self {
        Self::new_unchecked(-self.d(), -self.nx(), -self.ny())
    }
}
impl<T: Float> Mul<T> for Line<T> {
    type Output = Self;
    #[inline]
    fn mul(self, scalar: T) -> Self {
        self.scale(scalar)
    }
}
impl Mul<Line<f32>> for f32 {
    type Output = Line<f32>;
    #[inline]
    fn mul(self, v: Line<f32>) -> Line<f32> {
        v.scale(self)
    }
}
impl Mul<Line<f64>> for f64 {
    type Output = Line<f64>;
    #[inline]
    fn mul(self, v: Line<f64>) -> Line<f64> {
        v.scale(self)
    }
}
impl<T: Float> Mul<Flector<T>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn mul(self, rhs: Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            -(rhs.d() * self.d()),
            rhs.s() * self.d(),
            -(rhs.d() * self.ny()) + rhs.ny() * self.d() + rhs.s() * self.nx(),
            -(rhs.nx() * self.d()) + rhs.d() * self.nx() + rhs.s() * self.ny(),
        )
    }
}
impl<T: Float> Mul<Line<T>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn mul(self, rhs: Line<T>) -> Flector<T> {
        Flector::new_unchecked(
            -(rhs.d() * self.d()),
            T::zero(),
            -(rhs.d() * self.ny()) + rhs.ny() * self.d(),
            -(rhs.nx() * self.d()) + rhs.d() * self.nx(),
        )
    }
}
impl<T: Float> Mul<Motor<T>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            rhs.tx() * self.d(),
            -(rhs.ty() * self.d()),
            -(rhs.ps() * self.d()) + -(rhs.tx() * self.nx()) + rhs.ty() * self.ny(),
            rhs.r() * self.d() + rhs.tx() * self.ny() + rhs.ty() * self.nx(),
        )
    }
}
impl<T: Float> Mul<Point<T>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Point<T>) -> Motor<T> {
        Motor::new_unchecked(
            rhs.y() * self.d(),
            -(rhs.x() * self.d()),
            -(rhs.y() * self.nx()) + rhs.x() * self.ny(),
            rhs.w() * self.d() + rhs.x() * self.nx() + rhs.y() * self.ny(),
        )
    }
}
impl<T: Float> Mul<Scalar<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn mul(self, rhs: Scalar<T>) -> Line<T> {
        Line::new_unchecked(rhs.s() * self.d(), rhs.s() * self.nx(), rhs.s() * self.ny())
    }
}
impl<T: Float> Mul<Trivector<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn mul(self, rhs: Trivector<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), -(rhs.ps() * self.d()))
    }
}
impl<T: Float> Add for Motor<T> {
    type Output = Self;
    #[inline]
    fn add(self, rhs: Self) -> Self {
        Self::new_unchecked(
            self.ty() + rhs.ty(),
            self.tx() + rhs.tx(),
            self.r() + rhs.r(),
            self.ps() + rhs.ps(),
        )
    }
}
impl<T: Float> Sub for Motor<T> {
    type Output = Self;
    #[inline]
    fn sub(self, rhs: Self) -> Self {
        Self::new_unchecked(
            self.ty() - rhs.ty(),
            self.tx() - rhs.tx(),
            self.r() - rhs.r(),
            self.ps() - rhs.ps(),
        )
    }
}
impl<T: Float> Neg for Motor<T> {
    type Output = Self;
    #[inline]
    fn neg(self) -> Self {
        Self::new_unchecked(-self.ty(), -self.tx(), -self.r(), -self.ps())
    }
}
impl<T: Float> Mul<T> for Motor<T> {
    type Output = Self;
    #[inline]
    fn mul(self, scalar: T) -> Self {
        self.scale(scalar)
    }
}
impl Mul<Motor<f32>> for f32 {
    type Output = Motor<f32>;
    #[inline]
    fn mul(self, v: Motor<f32>) -> Motor<f32> {
        v.scale(self)
    }
}
impl Mul<Motor<f64>> for f64 {
    type Output = Motor<f64>;
    #[inline]
    fn mul(self, v: Motor<f64>) -> Motor<f64> {
        v.scale(self)
    }
}
impl<T: Float> Mul<Flector<T>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Flector<T>) -> Motor<T> {
        Motor::new_unchecked(
            -(rhs.d() * self.tx()) + rhs.s() * self.ty(),
            rhs.d() * self.ty() + rhs.s() * self.tx(),
            -(rhs.d() * self.ps())
                + -(rhs.ny() * self.ty())
                + rhs.nx() * self.tx()
                + rhs.s() * self.r(),
            rhs.d() * self.r() + rhs.nx() * self.ty() + rhs.ny() * self.tx() + rhs.s() * self.ps(),
        )
    }
}
impl<T: Float> Mul<Line<T>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Line<T>) -> Motor<T> {
        Motor::new_unchecked(
            -(rhs.d() * self.tx()),
            rhs.d() * self.ty(),
            -(rhs.d() * self.ps()) + -(rhs.ny() * self.ty()) + rhs.nx() * self.tx(),
            rhs.d() * self.r() + rhs.nx() * self.ty() + rhs.ny() * self.tx(),
        )
    }
}
impl<T: Float> Mul<Motor<T>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn mul(self, rhs: Motor<T>) -> Flector<T> {
        Flector::new_unchecked(
            rhs.tx() * self.tx() + rhs.ty() * self.ty(),
            -(rhs.ty() * self.tx()) + rhs.tx() * self.ty(),
            -(rhs.tx() * self.r())
                + rhs.ps() * self.ty()
                + rhs.r() * self.tx()
                + rhs.ty() * self.ps(),
            -(rhs.r() * self.ty())
                + rhs.ps() * self.tx()
                + rhs.tx() * self.ps()
                + rhs.ty() * self.r(),
        )
    }
}
impl<T: Float> Mul<Point<T>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn mul(self, rhs: Point<T>) -> Flector<T> {
        Flector::new_unchecked(
            rhs.x() * self.ty() + rhs.y() * self.tx(),
            -(rhs.x() * self.tx()) + rhs.y() * self.ty(),
            -(rhs.y() * self.r()) + rhs.w() * self.tx() + rhs.x() * self.ps(),
            -(rhs.w() * self.ty()) + rhs.x() * self.r() + rhs.y() * self.ps(),
        )
    }
}
impl<T: Float> Mul<Scalar<T>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Scalar<T>) -> Motor<T> {
        Motor::new_unchecked(
            rhs.s() * self.ty(),
            rhs.s() * self.tx(),
            rhs.s() * self.r(),
            rhs.s() * self.ps(),
        )
    }
}
impl<T: Float> Mul<Trivector<T>> for Motor<T> {
    type Output = Line<T>;
    #[inline]
    fn mul(self, rhs: Trivector<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), rhs.ps() * self.ty(), rhs.ps() * self.tx())
    }
}
impl<T: Float> Add for Point<T> {
    type Output = Self;
    #[inline]
    fn add(self, rhs: Self) -> Self {
        Self::new_unchecked(self.x() + rhs.x(), self.y() + rhs.y(), self.w() + rhs.w())
    }
}
impl<T: Float> Sub for Point<T> {
    type Output = Self;
    #[inline]
    fn sub(self, rhs: Self) -> Self {
        Self::new_unchecked(self.x() - rhs.x(), self.y() - rhs.y(), self.w() - rhs.w())
    }
}
impl<T: Float> Neg for Point<T> {
    type Output = Self;
    #[inline]
    fn neg(self) -> Self {
        Self::new_unchecked(-self.x(), -self.y(), -self.w())
    }
}
impl<T: Float> Mul<T> for Point<T> {
    type Output = Self;
    #[inline]
    fn mul(self, scalar: T) -> Self {
        self.scale(scalar)
    }
}
impl Mul<Point<f32>> for f32 {
    type Output = Point<f32>;
    #[inline]
    fn mul(self, v: Point<f32>) -> Point<f32> {
        v.scale(self)
    }
}
impl Mul<Point<f64>> for f64 {
    type Output = Point<f64>;
    #[inline]
    fn mul(self, v: Point<f64>) -> Point<f64> {
        v.scale(self)
    }
}
impl<T: Float> Mul<Flector<T>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Flector<T>) -> Motor<T> {
        Motor::new_unchecked(
            -(rhs.d() * self.y()) + rhs.s() * self.x(),
            rhs.d() * self.x() + rhs.s() * self.y(),
            -(rhs.ny() * self.x()) + rhs.nx() * self.y() + rhs.s() * self.w(),
            rhs.d() * self.w() + rhs.nx() * self.x() + rhs.ny() * self.y(),
        )
    }
}
impl<T: Float> Mul<Line<T>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Line<T>) -> Motor<T> {
        Motor::new_unchecked(
            -(rhs.d() * self.y()),
            rhs.d() * self.x(),
            -(rhs.ny() * self.x()) + rhs.nx() * self.y(),
            rhs.d() * self.w() + rhs.nx() * self.x() + rhs.ny() * self.y(),
        )
    }
}
impl<T: Float> Mul<Motor<T>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn mul(self, rhs: Motor<T>) -> Flector<T> {
        Flector::new_unchecked(
            rhs.tx() * self.y() + rhs.ty() * self.x(),
            -(rhs.ty() * self.y()) + rhs.tx() * self.x(),
            -(rhs.tx() * self.w()) + rhs.ps() * self.x() + rhs.r() * self.y(),
            -(rhs.r() * self.x()) + rhs.ps() * self.y() + rhs.ty() * self.w(),
        )
    }
}
impl<T: Float> Mul<Point<T>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn mul(self, rhs: Point<T>) -> Flector<T> {
        Flector::new_unchecked(
            rhs.x() * self.x() + rhs.y() * self.y(),
            -(rhs.x() * self.y()) + rhs.y() * self.x(),
            -(rhs.y() * self.w()) + rhs.w() * self.y(),
            -(rhs.w() * self.x()) + rhs.x() * self.w(),
        )
    }
}
impl<T: Float> Mul<Scalar<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn mul(self, rhs: Scalar<T>) -> Point<T> {
        Point::new_unchecked(rhs.s() * self.x(), rhs.s() * self.y(), rhs.s() * self.w())
    }
}
impl<T: Float> Mul<Trivector<T>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn mul(self, rhs: Trivector<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), rhs.ps() * self.x(), rhs.ps() * self.y())
    }
}
impl<T: Float> Add for Scalar<T> {
    type Output = Self;
    #[inline]
    fn add(self, rhs: Self) -> Self {
        Self::new_unchecked(self.s() + rhs.s())
    }
}
impl<T: Float> Sub for Scalar<T> {
    type Output = Self;
    #[inline]
    fn sub(self, rhs: Self) -> Self {
        Self::new_unchecked(self.s() - rhs.s())
    }
}
impl<T: Float> Neg for Scalar<T> {
    type Output = Self;
    #[inline]
    fn neg(self) -> Self {
        Self::new_unchecked(-self.s())
    }
}
impl<T: Float> Mul<T> for Scalar<T> {
    type Output = Self;
    #[inline]
    fn mul(self, scalar: T) -> Self {
        self.scale(scalar)
    }
}
impl Mul<Scalar<f32>> for f32 {
    type Output = Scalar<f32>;
    #[inline]
    fn mul(self, v: Scalar<f32>) -> Scalar<f32> {
        v.scale(self)
    }
}
impl Mul<Scalar<f64>> for f64 {
    type Output = Scalar<f64>;
    #[inline]
    fn mul(self, v: Scalar<f64>) -> Scalar<f64> {
        v.scale(self)
    }
}
impl<T: Float> Mul<Flector<T>> for Scalar<T> {
    type Output = Flector<T>;
    #[inline]
    fn mul(self, rhs: Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            rhs.s() * self.s(),
            rhs.d() * self.s(),
            rhs.nx() * self.s(),
            rhs.ny() * self.s(),
        )
    }
}
impl<T: Float> Mul<Line<T>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn mul(self, rhs: Line<T>) -> Line<T> {
        Line::new_unchecked(rhs.d() * self.s(), rhs.nx() * self.s(), rhs.ny() * self.s())
    }
}
impl<T: Float> Mul<Motor<T>> for Scalar<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            rhs.ty() * self.s(),
            rhs.tx() * self.s(),
            rhs.r() * self.s(),
            rhs.ps() * self.s(),
        )
    }
}
impl<T: Float> Mul<Point<T>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn mul(self, rhs: Point<T>) -> Point<T> {
        Point::new_unchecked(rhs.x() * self.s(), rhs.y() * self.s(), rhs.w() * self.s())
    }
}
impl<T: Float> Mul<Scalar<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn mul(self, rhs: Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.s() * self.s())
    }
}
impl<T: Float> Mul<Trivector<T>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn mul(self, rhs: Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.ps() * self.s())
    }
}
impl<T: Float> Add for Trivector<T> {
    type Output = Self;
    #[inline]
    fn add(self, rhs: Self) -> Self {
        Self::new_unchecked(self.ps() + rhs.ps())
    }
}
impl<T: Float> Sub for Trivector<T> {
    type Output = Self;
    #[inline]
    fn sub(self, rhs: Self) -> Self {
        Self::new_unchecked(self.ps() - rhs.ps())
    }
}
impl<T: Float> Neg for Trivector<T> {
    type Output = Self;
    #[inline]
    fn neg(self) -> Self {
        Self::new_unchecked(-self.ps())
    }
}
impl<T: Float> Mul<T> for Trivector<T> {
    type Output = Self;
    #[inline]
    fn mul(self, scalar: T) -> Self {
        self.scale(scalar)
    }
}
impl Mul<Trivector<f32>> for f32 {
    type Output = Trivector<f32>;
    #[inline]
    fn mul(self, v: Trivector<f32>) -> Trivector<f32> {
        v.scale(self)
    }
}
impl Mul<Trivector<f64>> for f64 {
    type Output = Trivector<f64>;
    #[inline]
    fn mul(self, v: Trivector<f64>) -> Trivector<f64> {
        v.scale(self)
    }
}
impl<T: Float> Mul<Flector<T>> for Trivector<T> {
    type Output = Motor<T>;
    #[inline]
    fn mul(self, rhs: Flector<T>) -> Motor<T> {
        Motor::new_unchecked(
            T::zero(),
            T::zero(),
            -(rhs.d() * self.ps()),
            rhs.s() * self.ps(),
        )
    }
}
impl<T: Float> Mul<Line<T>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn mul(self, rhs: Line<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), -(rhs.d() * self.ps()))
    }
}
impl<T: Float> Mul<Motor<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn mul(self, rhs: Motor<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), rhs.ty() * self.ps(), rhs.tx() * self.ps())
    }
}
impl<T: Float> Mul<Point<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn mul(self, rhs: Point<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), rhs.x() * self.ps(), rhs.y() * self.ps())
    }
}
impl<T: Float> Mul<Scalar<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn mul(self, rhs: Scalar<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.s() * self.ps())
    }
}
impl<T: Float> Wedge<Point<T>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Point<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.w() * self.d() + rhs.x() * self.nx() + rhs.y() * self.ny())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Point<T>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Point<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            rhs.w() * self.as_inner().d()
                + rhs.x() * self.as_inner().nx()
                + rhs.y() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Point<T>>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Point<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            rhs.as_inner().w() * self.d()
                + rhs.as_inner().x() * self.nx()
                + rhs.as_inner().y() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Point<T>>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Point<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            rhs.as_inner().w() * self.as_inner().d()
                + rhs.as_inner().x() * self.as_inner().nx()
                + rhs.as_inner().y() * self.as_inner().ny(),
        )
    }
}
impl<T: Float> Wedge<Scalar<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Scalar<T>) -> Line<T> {
        Line::new_unchecked(rhs.s() * self.d(), rhs.s() * self.nx(), rhs.s() * self.ny())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Scalar<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Scalar<T>) -> Line<T> {
        Line::new_unchecked(
            rhs.s() * self.as_inner().d(),
            rhs.s() * self.as_inner().nx(),
            rhs.s() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Scalar<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Scalar<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Scalar<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Scalar<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> Wedge<Line<T>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Line<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.d() * self.w() + rhs.nx() * self.x() + rhs.ny() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Line<T>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Line<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            rhs.d() * self.as_inner().w()
                + rhs.nx() * self.as_inner().x()
                + rhs.ny() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Line<T>>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Line<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            rhs.as_inner().d() * self.w()
                + rhs.as_inner().nx() * self.x()
                + rhs.as_inner().ny() * self.y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Line<T>>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Line<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            rhs.as_inner().d() * self.as_inner().w()
                + rhs.as_inner().nx() * self.as_inner().x()
                + rhs.as_inner().ny() * self.as_inner().y(),
        )
    }
}
impl<T: Float> Wedge<Point<T>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.x() * self.y()) + rhs.y() * self.x(),
            -(rhs.y() * self.w()) + rhs.w() * self.y(),
            -(rhs.w() * self.x()) + rhs.x() * self.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Point<T>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.x() * self.as_inner().y()) + rhs.y() * self.as_inner().x(),
            -(rhs.y() * self.as_inner().w()) + rhs.w() * self.as_inner().y(),
            -(rhs.w() * self.as_inner().x()) + rhs.x() * self.as_inner().w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Point<T>>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.as_inner().x() * self.y()) + rhs.as_inner().y() * self.x(),
            -(rhs.as_inner().y() * self.w()) + rhs.as_inner().w() * self.y(),
            -(rhs.as_inner().w() * self.x()) + rhs.as_inner().x() * self.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.as_inner().x() * self.as_inner().y()) + rhs.as_inner().y() * self.as_inner().x(),
            -(rhs.as_inner().y() * self.as_inner().w()) + rhs.as_inner().w() * self.as_inner().y(),
            -(rhs.as_inner().w() * self.as_inner().x()) + rhs.as_inner().x() * self.as_inner().w(),
        )
    }
}
impl<T: Float> Wedge<Scalar<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn wedge(&self, rhs: &Scalar<T>) -> Point<T> {
        Point::new_unchecked(rhs.s() * self.x(), rhs.s() * self.y(), rhs.s() * self.w())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Scalar<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn wedge(&self, rhs: &Scalar<T>) -> Point<T> {
        Point::new_unchecked(
            rhs.s() * self.as_inner().x(),
            rhs.s() * self.as_inner().y(),
            rhs.s() * self.as_inner().w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Scalar<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Scalar<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Scalar<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Scalar<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> Wedge<Line<T>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Line<T>) -> Line<T> {
        Line::new_unchecked(rhs.d() * self.s(), rhs.nx() * self.s(), rhs.ny() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Line<T>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Line<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Line<T>>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            rhs.as_inner().d() * self.s(),
            rhs.as_inner().nx() * self.s(),
            rhs.as_inner().ny() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Line<T>>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> Wedge<Point<T>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn wedge(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(rhs.x() * self.s(), rhs.y() * self.s(), rhs.w() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Point<T>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn wedge(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Point<T>>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            rhs.as_inner().x() * self.s(),
            rhs.as_inner().y() * self.s(),
            rhs.as_inner().w() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Point<T>>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> Wedge<Scalar<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn wedge(&self, rhs: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.s() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Scalar<T>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn wedge(&self, rhs: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Scalar<T>>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Scalar<T>>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
impl<T: Float> Wedge<Trivector<T>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.ps() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Trivector<T>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Trivector<T>>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.as_inner().ps() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Trivector<T>>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
impl<T: Float> Wedge<Scalar<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Scalar<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.s() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Scalar<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Scalar<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.s() * self.as_inner().ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Scalar<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Scalar<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Wedge<Unitized<Scalar<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn wedge(&self, rhs: &Unitized<Scalar<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
impl<T: Float> Antiwedge<Line<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.ny() * self.d()) + rhs.d() * self.ny(),
            -(rhs.d() * self.nx()) + rhs.nx() * self.d(),
            -(rhs.nx() * self.ny()) + rhs.ny() * self.nx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Line<T>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.ny() * self.as_inner().d()) + rhs.d() * self.as_inner().ny(),
            -(rhs.d() * self.as_inner().nx()) + rhs.nx() * self.as_inner().d(),
            -(rhs.nx() * self.as_inner().ny()) + rhs.ny() * self.as_inner().nx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Line<T>>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().ny() * self.d()) + rhs.as_inner().d() * self.ny(),
            -(rhs.as_inner().d() * self.nx()) + rhs.as_inner().nx() * self.d(),
            -(rhs.as_inner().nx() * self.ny()) + rhs.as_inner().ny() * self.nx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().ny() * self.as_inner().d())
                + rhs.as_inner().d() * self.as_inner().ny(),
            -(rhs.as_inner().d() * self.as_inner().nx())
                + rhs.as_inner().nx() * self.as_inner().d(),
            -(rhs.as_inner().nx() * self.as_inner().ny())
                + rhs.as_inner().ny() * self.as_inner().nx(),
        )
    }
}
impl<T: Float> Antiwedge<Point<T>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Point<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.w() * self.d() + rhs.x() * self.nx() + rhs.y() * self.ny())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Point<T>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Point<T>) -> Scalar<T> {
        Scalar::new_unchecked(
            rhs.w() * self.as_inner().d()
                + rhs.x() * self.as_inner().nx()
                + rhs.y() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Point<T>>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Point<T>>) -> Scalar<T> {
        Scalar::new_unchecked(
            rhs.as_inner().w() * self.d()
                + rhs.as_inner().x() * self.nx()
                + rhs.as_inner().y() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Point<T>>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Point<T>>) -> Scalar<T> {
        Scalar::new_unchecked(
            rhs.as_inner().w() * self.as_inner().d()
                + rhs.as_inner().x() * self.as_inner().nx()
                + rhs.as_inner().y() * self.as_inner().ny(),
        )
    }
}
impl<T: Float> Antiwedge<Trivector<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Trivector<T>) -> Line<T> {
        Line::new_unchecked(
            rhs.ps() * self.d(),
            rhs.ps() * self.nx(),
            rhs.ps() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Trivector<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Trivector<T>) -> Line<T> {
        Line::new_unchecked(
            rhs.ps() * self.as_inner().d(),
            rhs.ps() * self.as_inner().nx(),
            rhs.ps() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Trivector<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Trivector<T>>) -> Line<T> {
        Line::new_unchecked(
            rhs.as_inner().ps() * self.d(),
            rhs.as_inner().ps() * self.nx(),
            rhs.as_inner().ps() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Trivector<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Trivector<T>>) -> Line<T> {
        Line::new_unchecked(
            rhs.as_inner().ps() * self.as_inner().d(),
            rhs.as_inner().ps() * self.as_inner().nx(),
            rhs.as_inner().ps() * self.as_inner().ny(),
        )
    }
}
impl<T: Float> Antiwedge<Line<T>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Line<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.d() * self.w() + rhs.nx() * self.x() + rhs.ny() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Line<T>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Line<T>) -> Scalar<T> {
        Scalar::new_unchecked(
            rhs.d() * self.as_inner().w()
                + rhs.nx() * self.as_inner().x()
                + rhs.ny() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Line<T>>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Line<T>>) -> Scalar<T> {
        Scalar::new_unchecked(
            rhs.as_inner().d() * self.w()
                + rhs.as_inner().nx() * self.x()
                + rhs.as_inner().ny() * self.y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Line<T>>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Line<T>>) -> Scalar<T> {
        Scalar::new_unchecked(
            rhs.as_inner().d() * self.as_inner().w()
                + rhs.as_inner().nx() * self.as_inner().x()
                + rhs.as_inner().ny() * self.as_inner().y(),
        )
    }
}
impl<T: Float> Antiwedge<Trivector<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Trivector<T>) -> Point<T> {
        Point::new_unchecked(
            rhs.ps() * self.x(),
            rhs.ps() * self.y(),
            rhs.ps() * self.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Trivector<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Trivector<T>) -> Point<T> {
        Point::new_unchecked(
            rhs.ps() * self.as_inner().x(),
            rhs.ps() * self.as_inner().y(),
            rhs.ps() * self.as_inner().w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Trivector<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Trivector<T>>) -> Point<T> {
        Point::new_unchecked(
            rhs.as_inner().ps() * self.x(),
            rhs.as_inner().ps() * self.y(),
            rhs.as_inner().ps() * self.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Trivector<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Trivector<T>>) -> Point<T> {
        Point::new_unchecked(
            rhs.as_inner().ps() * self.as_inner().x(),
            rhs.as_inner().ps() * self.as_inner().y(),
            rhs.as_inner().ps() * self.as_inner().w(),
        )
    }
}
impl<T: Float> Antiwedge<Trivector<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Trivector<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.ps() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Trivector<T>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Trivector<T>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Trivector<T>>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Trivector<T>>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.as_inner().ps() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Trivector<T>>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Trivector<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
impl<T: Float> Antiwedge<Line<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            rhs.d() * self.ps(),
            rhs.nx() * self.ps(),
            rhs.ny() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Line<T>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            rhs.d() * self.as_inner().ps(),
            rhs.nx() * self.as_inner().ps(),
            rhs.ny() * self.as_inner().ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Line<T>>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            rhs.as_inner().d() * self.ps(),
            rhs.as_inner().nx() * self.ps(),
            rhs.as_inner().ny() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Line<T>>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            rhs.as_inner().d() * self.as_inner().ps(),
            rhs.as_inner().nx() * self.as_inner().ps(),
            rhs.as_inner().ny() * self.as_inner().ps(),
        )
    }
}
impl<T: Float> Antiwedge<Point<T>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            rhs.x() * self.ps(),
            rhs.y() * self.ps(),
            rhs.w() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Point<T>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            rhs.x() * self.as_inner().ps(),
            rhs.y() * self.as_inner().ps(),
            rhs.w() * self.as_inner().ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Point<T>>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            rhs.as_inner().x() * self.ps(),
            rhs.as_inner().y() * self.ps(),
            rhs.as_inner().w() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Point<T>>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            rhs.as_inner().x() * self.as_inner().ps(),
            rhs.as_inner().y() * self.as_inner().ps(),
            rhs.as_inner().w() * self.as_inner().ps(),
        )
    }
}
impl<T: Float> Antiwedge<Scalar<T>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.s() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Scalar<T>> for Unitized<Trivector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.s() * self.as_inner().ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Scalar<T>>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Scalar<T>>> for Unitized<Trivector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
impl<T: Float> Antiwedge<Trivector<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.ps() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Trivector<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.ps() * self.as_inner().ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Trivector<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.as_inner().ps() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antiwedge<Unitized<Trivector<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antiwedge(&self, rhs: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.as_inner().ps() * self.as_inner().ps())
    }
}
impl<T: Float> LeftContract<Line<T>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Line<T>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.d() * self.d()))
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Line<T>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Line<T>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.d() * self.as_inner().d()))
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Line<T>>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Line<T>>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.as_inner().d() * self.d()))
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Line<T>>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.as_inner().d() * self.as_inner().d()))
    }
}
impl<T: Float> LeftContract<Trivector<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Trivector<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), -(rhs.ps() * self.d()))
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Trivector<T>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Trivector<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), -(rhs.ps() * self.as_inner().d()))
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Trivector<T>>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Trivector<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), -(rhs.as_inner().ps() * self.d()))
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Trivector<T>>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Trivector<T>>) -> Point<T> {
        Point::new_unchecked(
            T::zero(),
            T::zero(),
            -(rhs.as_inner().ps() * self.as_inner().d()),
        )
    }
}
impl<T: Float> LeftContract<Line<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.d() * self.y()),
            rhs.d() * self.x(),
            -(rhs.ny() * self.x()) + rhs.nx() * self.y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Line<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.d() * self.as_inner().y()),
            rhs.d() * self.as_inner().x(),
            -(rhs.ny() * self.as_inner().x()) + rhs.nx() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Line<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().d() * self.y()),
            rhs.as_inner().d() * self.x(),
            -(rhs.as_inner().ny() * self.x()) + rhs.as_inner().nx() * self.y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Line<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().d() * self.as_inner().y()),
            rhs.as_inner().d() * self.as_inner().x(),
            -(rhs.as_inner().ny() * self.as_inner().x())
                + rhs.as_inner().nx() * self.as_inner().y(),
        )
    }
}
impl<T: Float> LeftContract<Point<T>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Point<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.x() * self.x() + rhs.y() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Point<T>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Point<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.x() * self.as_inner().x() + rhs.y() * self.as_inner().y())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Point<T>>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Point<T>>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.as_inner().x() * self.x() + rhs.as_inner().y() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Point<T>>) -> Scalar<T> {
        Scalar::new_unchecked(
            rhs.as_inner().x() * self.as_inner().x() + rhs.as_inner().y() * self.as_inner().y(),
        )
    }
}
impl<T: Float> LeftContract<Trivector<T>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn left_contract(&self, rhs: &Trivector<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), rhs.ps() * self.x(), rhs.ps() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Trivector<T>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn left_contract(&self, rhs: &Trivector<T>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.ps() * self.as_inner().x(),
            rhs.ps() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Trivector<T>>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Trivector<T>>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.as_inner().ps() * self.x(),
            rhs.as_inner().ps() * self.y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Trivector<T>>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Trivector<T>>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.as_inner().ps() * self.as_inner().x(),
            rhs.as_inner().ps() * self.as_inner().y(),
        )
    }
}
impl<T: Float> LeftContract<Line<T>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn left_contract(&self, rhs: &Line<T>) -> Line<T> {
        Line::new_unchecked(rhs.d() * self.s(), rhs.nx() * self.s(), rhs.ny() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Line<T>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn left_contract(&self, rhs: &Line<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Line<T>>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            rhs.as_inner().d() * self.s(),
            rhs.as_inner().nx() * self.s(),
            rhs.as_inner().ny() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Line<T>>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> LeftContract<Point<T>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(rhs.x() * self.s(), rhs.y() * self.s(), rhs.w() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Point<T>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Point<T>>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            rhs.as_inner().x() * self.s(),
            rhs.as_inner().y() * self.s(),
            rhs.as_inner().w() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Point<T>>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> LeftContract<Scalar<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.s() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Scalar<T>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Scalar<T>>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Scalar<T>>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
impl<T: Float> LeftContract<Trivector<T>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn left_contract(&self, rhs: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.ps() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Trivector<T>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn left_contract(&self, rhs: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Trivector<T>>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.as_inner().ps() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> LeftContract<Unitized<Trivector<T>>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn left_contract(&self, rhs: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
impl<T: Float> RightContract<Line<T>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Line<T>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.d() * self.d()))
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Line<T>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Line<T>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.d() * self.as_inner().d()))
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Line<T>>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Line<T>>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.as_inner().d() * self.d()))
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Line<T>>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.as_inner().d() * self.as_inner().d()))
    }
}
impl<T: Float> RightContract<Point<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            rhs.y() * self.d(),
            -(rhs.x() * self.d()),
            -(rhs.y() * self.nx()) + rhs.x() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Point<T>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            rhs.y() * self.as_inner().d(),
            -(rhs.x() * self.as_inner().d()),
            -(rhs.y() * self.as_inner().nx()) + rhs.x() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Point<T>>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            rhs.as_inner().y() * self.d(),
            -(rhs.as_inner().x() * self.d()),
            -(rhs.as_inner().y() * self.nx()) + rhs.as_inner().x() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Point<T>>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            rhs.as_inner().y() * self.as_inner().d(),
            -(rhs.as_inner().x() * self.as_inner().d()),
            -(rhs.as_inner().y() * self.as_inner().nx())
                + rhs.as_inner().x() * self.as_inner().ny(),
        )
    }
}
impl<T: Float> RightContract<Scalar<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn right_contract(&self, rhs: &Scalar<T>) -> Line<T> {
        Line::new_unchecked(rhs.s() * self.d(), rhs.s() * self.nx(), rhs.s() * self.ny())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Scalar<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn right_contract(&self, rhs: &Scalar<T>) -> Line<T> {
        Line::new_unchecked(
            rhs.s() * self.as_inner().d(),
            rhs.s() * self.as_inner().nx(),
            rhs.s() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Scalar<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Scalar<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Scalar<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Scalar<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> RightContract<Point<T>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Point<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.x() * self.x() + rhs.y() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Point<T>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Point<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.x() * self.as_inner().x() + rhs.y() * self.as_inner().y())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Point<T>>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Point<T>>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.as_inner().x() * self.x() + rhs.as_inner().y() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Point<T>>) -> Scalar<T> {
        Scalar::new_unchecked(
            rhs.as_inner().x() * self.as_inner().x() + rhs.as_inner().y() * self.as_inner().y(),
        )
    }
}
impl<T: Float> RightContract<Scalar<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Scalar<T>) -> Point<T> {
        Point::new_unchecked(rhs.s() * self.x(), rhs.s() * self.y(), rhs.s() * self.w())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Scalar<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Scalar<T>) -> Point<T> {
        Point::new_unchecked(
            rhs.s() * self.as_inner().x(),
            rhs.s() * self.as_inner().y(),
            rhs.s() * self.as_inner().w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Scalar<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Scalar<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Scalar<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Scalar<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> RightContract<Scalar<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.s() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Scalar<T>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Scalar<T>>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Scalar<T>>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
impl<T: Float> RightContract<Line<T>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), -(rhs.d() * self.ps()))
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Line<T>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), -(rhs.d() * self.as_inner().ps()))
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Line<T>>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), -(rhs.as_inner().d() * self.ps()))
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Line<T>>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(
            T::zero(),
            T::zero(),
            -(rhs.as_inner().d() * self.as_inner().ps()),
        )
    }
}
impl<T: Float> RightContract<Point<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn right_contract(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), rhs.x() * self.ps(), rhs.y() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Point<T>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn right_contract(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.x() * self.as_inner().ps(),
            rhs.y() * self.as_inner().ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Point<T>>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.as_inner().x() * self.ps(),
            rhs.as_inner().y() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Point<T>>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.as_inner().x() * self.as_inner().ps(),
            rhs.as_inner().y() * self.as_inner().ps(),
        )
    }
}
impl<T: Float> RightContract<Scalar<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn right_contract(&self, rhs: &Scalar<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.s() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Scalar<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn right_contract(&self, rhs: &Scalar<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.s() * self.as_inner().ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Scalar<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Scalar<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> RightContract<Unitized<Scalar<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn right_contract(&self, rhs: &Unitized<Scalar<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            self.s() * operand.s() * self.s() - self.d() * operand.d() * self.s()
                + self.d() * operand.s() * self.d()
                + self.s() * operand.d() * self.d(),
            self.d() * operand.s() * self.s() + self.d() * operand.d() * self.d()
                - self.s() * operand.s() * self.d()
                + self.s() * operand.d() * self.s(),
            self.nx() * operand.d() * self.d() + self.d() * operand.d() * self.nx()
                - self.s() * operand.s() * self.nx()
                + self.s() * operand.ny() * self.d()
                + self.d() * operand.ny() * self.s()
                - self.d() * operand.nx() * self.d()
                + self.nx() * operand.s() * self.s()
                - self.ny() * operand.d() * self.s()
                + self.ny() * operand.s() * self.d()
                + self.s() * operand.nx() * self.s()
                - self.s() * operand.d() * self.ny()
                - self.d() * operand.s() * self.ny(),
            -(self.s() * operand.nx() * self.d()
                + self.s() * operand.s() * self.ny()
                + self.d() * operand.ny() * self.d()
                - self.s() * operand.ny() * self.s()
                + self.nx() * operand.s() * self.d()
                - self.nx() * operand.d() * self.s()
                - self.ny() * operand.d() * self.d()
                - self.d() * operand.d() * self.ny()
                - self.ny() * operand.s() * self.s()
                - self.d() * operand.s() * self.nx()
                + self.d() * operand.nx() * self.s()
                - self.s() * operand.d() * self.nx()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Unitized<Flector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            operand.s() * self.as_inner().d() * self.as_inner().d()
                + operand.s() * self.as_inner().s() * self.as_inner().s(),
            operand.d() * self.as_inner().d() * self.as_inner().d()
                + operand.d() * self.as_inner().s() * self.as_inner().s(),
            -(operand.nx() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.d() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.d() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.ny() * self.as_inner().d() * self.as_inner().s()
                + operand.nx() * self.as_inner().s() * self.as_inner().s(),
            -(operand.ny() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.nx() * self.as_inner().d() * self.as_inner().s()
                + T::TWO * operand.d() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.d() * self.as_inner().nx() * self.as_inner().s()
                + operand.ny() * self.as_inner().s() * self.as_inner().s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.d() * self.d()
                + operand.as_inner().s() * self.s() * self.s(),
            operand.as_inner().d() * self.d() * self.d()
                + operand.as_inner().d() * self.s() * self.s(),
            -(operand.as_inner().nx() * self.d() * self.d())
                + -T::TWO * operand.as_inner().d() * self.ny() * self.s()
                + T::TWO * operand.as_inner().d() * self.d() * self.nx()
                + T::TWO * operand.as_inner().ny() * self.d() * self.s()
                + operand.as_inner().nx() * self.s() * self.s(),
            -(operand.as_inner().ny() * self.d() * self.d())
                + -T::TWO * operand.as_inner().nx() * self.d() * self.s()
                + T::TWO * operand.as_inner().d() * self.d() * self.ny()
                + T::TWO * operand.as_inner().d() * self.nx() * self.s()
                + operand.as_inner().ny() * self.s() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Unitized<Flector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.as_inner().d() * self.as_inner().d()
                + operand.as_inner().s() * self.as_inner().s() * self.as_inner().s(),
            operand.as_inner().d() * self.as_inner().d() * self.as_inner().d()
                + operand.as_inner().d() * self.as_inner().s() * self.as_inner().s(),
            -(operand.as_inner().nx() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.as_inner().d() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.as_inner().d() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().ny() * self.as_inner().d() * self.as_inner().s()
                + operand.as_inner().nx() * self.as_inner().s() * self.as_inner().s(),
            -(operand.as_inner().ny() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.as_inner().nx() * self.as_inner().d() * self.as_inner().s()
                + T::TWO * operand.as_inner().d() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.as_inner().d() * self.as_inner().nx() * self.as_inner().s()
                + operand.as_inner().ny() * self.as_inner().s() * self.as_inner().s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Flector<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            self.s() * operand.d() * self.s() + self.d() * operand.d() * self.d(),
            self.d() * operand.d() * self.nx() - self.s() * operand.d() * self.ny()
                + self.s() * operand.ny() * self.d()
                + self.nx() * operand.d() * self.d()
                + self.s() * operand.nx() * self.s()
                - self.ny() * operand.d() * self.s()
                - self.d() * operand.nx() * self.d()
                + self.d() * operand.ny() * self.s(),
            -(-(self.d() * operand.d() * self.ny())
                - self.ny() * operand.d() * self.d()
                - self.s() * operand.ny() * self.s()
                - self.nx() * operand.d() * self.s()
                - self.s() * operand.d() * self.nx()
                + self.s() * operand.nx() * self.d()
                + self.d() * operand.ny() * self.d()
                + self.d() * operand.nx() * self.s()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Unitized<Flector<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            operand.d() * self.as_inner().d() * self.as_inner().d()
                + operand.d() * self.as_inner().s() * self.as_inner().s(),
            -(operand.nx() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.d() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.d() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.ny() * self.as_inner().d() * self.as_inner().s()
                + operand.nx() * self.as_inner().s() * self.as_inner().s(),
            -(operand.ny() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.nx() * self.as_inner().d() * self.as_inner().s()
                + T::TWO * operand.d() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.d() * self.as_inner().nx() * self.as_inner().s()
                + operand.ny() * self.as_inner().s() * self.as_inner().s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Flector<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            operand.as_inner().d() * self.d() * self.d()
                + operand.as_inner().d() * self.s() * self.s(),
            -(operand.as_inner().nx() * self.d() * self.d())
                + -T::TWO * operand.as_inner().d() * self.ny() * self.s()
                + T::TWO * operand.as_inner().d() * self.d() * self.nx()
                + T::TWO * operand.as_inner().ny() * self.d() * self.s()
                + operand.as_inner().nx() * self.s() * self.s(),
            -(operand.as_inner().ny() * self.d() * self.d())
                + -T::TWO * operand.as_inner().nx() * self.d() * self.s()
                + T::TWO * operand.as_inner().d() * self.d() * self.ny()
                + T::TWO * operand.as_inner().d() * self.nx() * self.s()
                + operand.as_inner().ny() * self.s() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Unitized<Flector<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            operand.as_inner().d() * self.as_inner().d() * self.as_inner().d()
                + operand.as_inner().d() * self.as_inner().s() * self.as_inner().s(),
            -(operand.as_inner().nx() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.as_inner().d() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.as_inner().d() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().ny() * self.as_inner().d() * self.as_inner().s()
                + operand.as_inner().nx() * self.as_inner().s() * self.as_inner().s(),
            -(operand.as_inner().ny() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.as_inner().nx() * self.as_inner().d() * self.as_inner().s()
                + T::TWO * operand.as_inner().d() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.as_inner().d() * self.as_inner().nx() * self.as_inner().s()
                + operand.as_inner().ny() * self.as_inner().s() * self.as_inner().s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            self.s() * operand.ty() * self.s() + self.s() * operand.tx() * self.d()
                - self.d() * operand.ty() * self.d()
                + self.d() * operand.tx() * self.s(),
            -(self.d() * operand.ty() * self.s()) + self.s() * operand.tx() * self.s()
                - self.d() * operand.tx() * self.d()
                - self.s() * operand.ty() * self.d(),
            self.s() * operand.ty() * self.ny() - self.d() * operand.ps() * self.s()
                + self.ny() * operand.ty() * self.s()
                + self.d() * operand.ty() * self.nx()
                - self.nx() * operand.tx() * self.s()
                + self.d() * operand.tx() * self.ny()
                - self.s() * operand.tx() * self.nx()
                + self.d() * operand.r() * self.d()
                + self.nx() * operand.ty() * self.d()
                + self.s() * operand.r() * self.s()
                + self.ny() * operand.tx() * self.d()
                + self.s() * operand.ps() * self.d(),
            -(self.s() * operand.ty() * self.nx()) - self.s() * operand.tx() * self.ny()
                + self.d() * operand.ty() * self.ny()
                - self.d() * operand.tx() * self.nx()
                - self.s() * operand.r() * self.d()
                + self.d() * operand.ps() * self.d()
                + self.d() * operand.r() * self.s()
                + self.nx() * operand.tx() * self.d()
                - self.ny() * operand.ty() * self.d()
                + self.ny() * operand.tx() * self.s()
                + self.s() * operand.ps() * self.s()
                + self.nx() * operand.ty() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Unitized<Flector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.ty() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.tx() * self.as_inner().d() * self.as_inner().s()
                + operand.ty() * self.as_inner().s() * self.as_inner().s(),
            -(operand.tx() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.ty() * self.as_inner().d() * self.as_inner().s()
                + operand.tx() * self.as_inner().s() * self.as_inner().s(),
            -T::TWO * operand.tx() * self.as_inner().nx() * self.as_inner().s()
                + T::TWO * operand.tx() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.ty() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.ty() * self.as_inner().ny() * self.as_inner().s()
                + operand.r() * self.as_inner().d() * self.as_inner().d()
                + operand.r() * self.as_inner().s() * self.as_inner().s(),
            operand.ps() * self.as_inner().d() * self.as_inner().d()
                + operand.ps() * self.as_inner().s() * self.as_inner().s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.d() * self.d())
                + T::TWO * operand.as_inner().tx() * self.d() * self.s()
                + operand.as_inner().ty() * self.s() * self.s(),
            -(operand.as_inner().tx() * self.d() * self.d())
                + -T::TWO * operand.as_inner().ty() * self.d() * self.s()
                + operand.as_inner().tx() * self.s() * self.s(),
            -T::TWO * operand.as_inner().tx() * self.nx() * self.s()
                + T::TWO * operand.as_inner().tx() * self.d() * self.ny()
                + T::TWO * operand.as_inner().ty() * self.d() * self.nx()
                + T::TWO * operand.as_inner().ty() * self.ny() * self.s()
                + operand.as_inner().r() * self.d() * self.d()
                + operand.as_inner().r() * self.s() * self.s(),
            operand.as_inner().ps() * self.d() * self.d()
                + operand.as_inner().ps() * self.s() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Unitized<Flector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.as_inner().tx() * self.as_inner().d() * self.as_inner().s()
                + operand.as_inner().ty() * self.as_inner().s() * self.as_inner().s(),
            -(operand.as_inner().tx() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.as_inner().ty() * self.as_inner().d() * self.as_inner().s()
                + operand.as_inner().tx() * self.as_inner().s() * self.as_inner().s(),
            -T::TWO * operand.as_inner().tx() * self.as_inner().nx() * self.as_inner().s()
                + T::TWO * operand.as_inner().tx() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ty() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().ty() * self.as_inner().ny() * self.as_inner().s()
                + operand.as_inner().r() * self.as_inner().d() * self.as_inner().d()
                + operand.as_inner().r() * self.as_inner().s() * self.as_inner().s(),
            operand.as_inner().ps() * self.as_inner().d() * self.as_inner().d()
                + operand.as_inner().ps() * self.as_inner().s() * self.as_inner().s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Flector<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(self.d() * operand.x() * self.d())
                + self.s() * operand.x() * self.s()
                + self.s() * operand.y() * self.d()
                + self.d() * operand.y() * self.s(),
            -(self.s() * operand.x() * self.d()) - self.d() * operand.x() * self.s()
                + self.s() * operand.y() * self.s()
                - self.d() * operand.y() * self.d(),
            self.s() * operand.w() * self.s()
                + self.d() * operand.y() * self.ny()
                + self.nx() * operand.x() * self.d()
                + self.ny() * operand.y() * self.d()
                + self.s() * operand.x() * self.ny()
                + self.d() * operand.x() * self.nx()
                + self.d() * operand.w() * self.d()
                - self.nx() * operand.y() * self.s()
                + self.ny() * operand.x() * self.s()
                - self.s() * operand.y() * self.nx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Unitized<Flector<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(operand.x() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.y() * self.as_inner().d() * self.as_inner().s()
                + operand.x() * self.as_inner().s() * self.as_inner().s(),
            -(operand.y() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.x() * self.as_inner().d() * self.as_inner().s()
                + operand.y() * self.as_inner().s() * self.as_inner().s(),
            -T::TWO * operand.y() * self.as_inner().nx() * self.as_inner().s()
                + T::TWO * operand.x() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.x() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.y() * self.as_inner().d() * self.as_inner().ny()
                + operand.w() * self.as_inner().d() * self.as_inner().d()
                + operand.w() * self.as_inner().s() * self.as_inner().s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Flector<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.d() * self.d())
                + T::TWO * operand.as_inner().y() * self.d() * self.s()
                + operand.as_inner().x() * self.s() * self.s(),
            -(operand.as_inner().y() * self.d() * self.d())
                + -T::TWO * operand.as_inner().x() * self.d() * self.s()
                + operand.as_inner().y() * self.s() * self.s(),
            -T::TWO * operand.as_inner().y() * self.nx() * self.s()
                + T::TWO * operand.as_inner().x() * self.d() * self.nx()
                + T::TWO * operand.as_inner().x() * self.ny() * self.s()
                + T::TWO * operand.as_inner().y() * self.d() * self.ny()
                + operand.as_inner().w() * self.d() * self.d()
                + operand.as_inner().w() * self.s() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Unitized<Flector<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.as_inner().y() * self.as_inner().d() * self.as_inner().s()
                + operand.as_inner().x() * self.as_inner().s() * self.as_inner().s(),
            -(operand.as_inner().y() * self.as_inner().d() * self.as_inner().d())
                + -T::TWO * operand.as_inner().x() * self.as_inner().d() * self.as_inner().s()
                + operand.as_inner().y() * self.as_inner().s() * self.as_inner().s(),
            -T::TWO * operand.as_inner().y() * self.as_inner().nx() * self.as_inner().s()
                + T::TWO * operand.as_inner().x() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().x() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.as_inner().y() * self.as_inner().d() * self.as_inner().ny()
                + operand.as_inner().w() * self.as_inner().d() * self.as_inner().d()
                + operand.as_inner().w() * self.as_inner().s() * self.as_inner().s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Flector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(self.d() * operand.s() * self.d() + self.s() * operand.s() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Unitized<Flector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(
            operand.s() * self.as_inner().d() * self.as_inner().d()
                + operand.s() * self.as_inner().s() * self.as_inner().s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Flector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Unitized<Flector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Flector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            self.s() * operand.ps() * self.s() + self.d() * operand.ps() * self.d(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Unitized<Flector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.ps() * self.as_inner().d() * self.as_inner().d()
                + operand.ps() * self.as_inner().s() * self.as_inner().s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Flector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.as_inner().ps() * self.d() * self.d()
                + operand.as_inner().ps() * self.s() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Unitized<Flector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.as_inner().ps() * self.as_inner().d() * self.as_inner().d()
                + operand.as_inner().ps() * self.as_inner().s() * self.as_inner().s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            self.d() * operand.s() * self.d(),
            self.d() * operand.d() * self.d(),
            self.ny() * operand.s() * self.d() + self.d() * operand.d() * self.nx()
                - self.d() * operand.nx() * self.d()
                + self.nx() * operand.d() * self.d()
                - self.d() * operand.s() * self.ny(),
            -(self.d() * operand.ny() * self.d() + self.nx() * operand.s() * self.d()
                - self.d() * operand.s() * self.nx()
                - self.ny() * operand.d() * self.d()
                - self.d() * operand.d() * self.ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Unitized<Line<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            operand.s() * self.as_inner().d() * self.as_inner().d(),
            operand.d() * self.as_inner().d() * self.as_inner().d(),
            -(operand.nx() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.d() * self.as_inner().d() * self.as_inner().nx(),
            -(operand.ny() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.d() * self.as_inner().d() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.d() * self.d(),
            operand.as_inner().d() * self.d() * self.d(),
            -(operand.as_inner().nx() * self.d() * self.d())
                + T::TWO * operand.as_inner().d() * self.d() * self.nx(),
            -(operand.as_inner().ny() * self.d() * self.d())
                + T::TWO * operand.as_inner().d() * self.d() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Unitized<Line<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.as_inner().d() * self.as_inner().d(),
            operand.as_inner().d() * self.as_inner().d() * self.as_inner().d(),
            -(operand.as_inner().nx() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.as_inner().d() * self.as_inner().d() * self.as_inner().nx(),
            -(operand.as_inner().ny() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.as_inner().d() * self.as_inner().d() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            self.d() * operand.d() * self.d(),
            self.d() * operand.d() * self.nx() + self.nx() * operand.d() * self.d()
                - self.d() * operand.nx() * self.d(),
            -(self.d() * operand.ny() * self.d()
                - self.ny() * operand.d() * self.d()
                - self.d() * operand.d() * self.ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            operand.d() * self.as_inner().d() * self.as_inner().d(),
            -(operand.nx() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.d() * self.as_inner().d() * self.as_inner().nx(),
            -(operand.ny() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.d() * self.as_inner().d() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            operand.as_inner().d() * self.d() * self.d(),
            -(operand.as_inner().nx() * self.d() * self.d())
                + T::TWO * operand.as_inner().d() * self.d() * self.nx(),
            -(operand.as_inner().ny() * self.d() * self.d())
                + T::TWO * operand.as_inner().d() * self.d() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            operand.as_inner().d() * self.as_inner().d() * self.as_inner().d(),
            -(operand.as_inner().nx() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.as_inner().d() * self.as_inner().d() * self.as_inner().nx(),
            -(operand.as_inner().ny() * self.as_inner().d() * self.as_inner().d())
                + T::TWO * operand.as_inner().d() * self.as_inner().d() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            -(self.d() * operand.ty() * self.d()),
            -(self.d() * operand.tx() * self.d()),
            self.d() * operand.r() * self.d()
                + self.d() * operand.tx() * self.ny()
                + self.d() * operand.ty() * self.nx()
                + self.nx() * operand.ty() * self.d()
                + self.ny() * operand.tx() * self.d(),
            self.d() * operand.ty() * self.ny()
                + self.nx() * operand.tx() * self.d()
                + self.d() * operand.ps() * self.d()
                - self.ny() * operand.ty() * self.d()
                - self.d() * operand.tx() * self.nx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Unitized<Line<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.ty() * self.as_inner().d() * self.as_inner().d()),
            -(operand.tx() * self.as_inner().d() * self.as_inner().d()),
            T::TWO * operand.tx() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.ty() * self.as_inner().d() * self.as_inner().nx()
                + operand.r() * self.as_inner().d() * self.as_inner().d(),
            operand.ps() * self.as_inner().d() * self.as_inner().d(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.d() * self.d()),
            -(operand.as_inner().tx() * self.d() * self.d()),
            T::TWO * operand.as_inner().tx() * self.d() * self.ny()
                + T::TWO * operand.as_inner().ty() * self.d() * self.nx()
                + operand.as_inner().r() * self.d() * self.d(),
            operand.as_inner().ps() * self.d() * self.d(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Unitized<Line<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.as_inner().d() * self.as_inner().d()),
            -(operand.as_inner().tx() * self.as_inner().d() * self.as_inner().d()),
            T::TWO * operand.as_inner().tx() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ty() * self.as_inner().d() * self.as_inner().nx()
                + operand.as_inner().r() * self.as_inner().d() * self.as_inner().d(),
            operand.as_inner().ps() * self.as_inner().d() * self.as_inner().d(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(self.d() * operand.x() * self.d()),
            -(self.d() * operand.y() * self.d()),
            self.d() * operand.w() * self.d()
                + self.d() * operand.x() * self.nx()
                + self.ny() * operand.y() * self.d()
                + self.d() * operand.y() * self.ny()
                + self.nx() * operand.x() * self.d(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(operand.x() * self.as_inner().d() * self.as_inner().d()),
            -(operand.y() * self.as_inner().d() * self.as_inner().d()),
            T::TWO * operand.x() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.y() * self.as_inner().d() * self.as_inner().ny()
                + operand.w() * self.as_inner().d() * self.as_inner().d(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.d() * self.d()),
            -(operand.as_inner().y() * self.d() * self.d()),
            T::TWO * operand.as_inner().x() * self.d() * self.nx()
                + T::TWO * operand.as_inner().y() * self.d() * self.ny()
                + operand.as_inner().w() * self.d() * self.d(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.as_inner().d() * self.as_inner().d()),
            -(operand.as_inner().y() * self.as_inner().d() * self.as_inner().d()),
            T::TWO * operand.as_inner().x() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().y() * self.as_inner().d() * self.as_inner().ny()
                + operand.as_inner().w() * self.as_inner().d() * self.as_inner().d(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(self.d() * operand.s() * self.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(operand.s() * self.as_inner().d() * self.as_inner().d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(self.d() * operand.ps() * self.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(operand.ps() * self.as_inner().d() * self.as_inner().d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(operand.as_inner().ps() * self.d() * self.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.as_inner().ps() * self.as_inner().d() * self.as_inner().d(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            self.tx() * operand.s() * self.tx() + self.ty() * operand.d() * self.tx()
                - self.tx() * operand.d() * self.ty()
                + self.ty() * operand.s() * self.ty(),
            -(self.ty() * operand.d() * self.ty()) + self.ty() * operand.s() * self.tx()
                - self.tx() * operand.d() * self.tx()
                - self.tx() * operand.s() * self.ty(),
            self.ps() * operand.d() * self.tx()
                + self.r() * operand.d() * self.ty()
                + self.tx() * operand.ny() * self.ty()
                - self.ty() * operand.s() * self.ps()
                - self.r() * operand.s() * self.tx()
                + self.ps() * operand.s() * self.ty()
                + self.ty() * operand.ny() * self.tx()
                + self.tx() * operand.d() * self.ps()
                + self.ty() * operand.d() * self.r()
                + self.ty() * operand.nx() * self.ty()
                + self.tx() * operand.s() * self.r()
                - self.tx() * operand.nx() * self.tx(),
            -(self.tx() * operand.s() * self.ps() - self.r() * operand.d() * self.tx()
                + self.ps() * operand.d() * self.ty()
                - self.tx() * operand.d() * self.r()
                - self.tx() * operand.nx() * self.ty()
                + self.ty() * operand.s() * self.r()
                - self.ty() * operand.nx() * self.tx()
                - self.tx() * operand.ny() * self.tx()
                + self.ty() * operand.d() * self.ps()
                - self.ps() * operand.s() * self.tx()
                - self.r() * operand.s() * self.ty()
                + self.ty() * operand.ny() * self.ty()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Unitized<Motor<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            operand.s() * self.as_inner().tx() * self.as_inner().tx()
                + operand.s() * self.as_inner().ty() * self.as_inner().ty(),
            -(operand.d() * self.as_inner().tx() * self.as_inner().tx())
                + -(operand.d() * self.as_inner().ty() * self.as_inner().ty()),
            -(operand.nx() * self.as_inner().tx() * self.as_inner().tx())
                + T::TWO * operand.d() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.d() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.ny() * self.as_inner().tx() * self.as_inner().ty()
                + operand.nx() * self.as_inner().ty() * self.as_inner().ty(),
            -(operand.ny() * self.as_inner().ty() * self.as_inner().ty())
                + -T::TWO * operand.d() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.d() * self.as_inner().r() * self.as_inner().tx()
                + T::TWO * operand.nx() * self.as_inner().tx() * self.as_inner().ty()
                + operand.ny() * self.as_inner().tx() * self.as_inner().tx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.tx() * self.tx()
                + operand.as_inner().s() * self.ty() * self.ty(),
            -(operand.as_inner().d() * self.tx() * self.tx())
                + -(operand.as_inner().d() * self.ty() * self.ty()),
            -(operand.as_inner().nx() * self.tx() * self.tx())
                + T::TWO * operand.as_inner().d() * self.ps() * self.tx()
                + T::TWO * operand.as_inner().d() * self.r() * self.ty()
                + T::TWO * operand.as_inner().ny() * self.tx() * self.ty()
                + operand.as_inner().nx() * self.ty() * self.ty(),
            -(operand.as_inner().ny() * self.ty() * self.ty())
                + -T::TWO * operand.as_inner().d() * self.ps() * self.ty()
                + T::TWO * operand.as_inner().d() * self.r() * self.tx()
                + T::TWO * operand.as_inner().nx() * self.tx() * self.ty()
                + operand.as_inner().ny() * self.tx() * self.tx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Unitized<Motor<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.as_inner().tx() * self.as_inner().tx()
                + operand.as_inner().s() * self.as_inner().ty() * self.as_inner().ty(),
            -(operand.as_inner().d() * self.as_inner().tx() * self.as_inner().tx())
                + -(operand.as_inner().d() * self.as_inner().ty() * self.as_inner().ty()),
            -(operand.as_inner().nx() * self.as_inner().tx() * self.as_inner().tx())
                + T::TWO * operand.as_inner().d() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.as_inner().d() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.as_inner().ny() * self.as_inner().tx() * self.as_inner().ty()
                + operand.as_inner().nx() * self.as_inner().ty() * self.as_inner().ty(),
            -(operand.as_inner().ny() * self.as_inner().ty() * self.as_inner().ty())
                + -T::TWO * operand.as_inner().d() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.as_inner().d() * self.as_inner().r() * self.as_inner().tx()
                + T::TWO * operand.as_inner().nx() * self.as_inner().tx() * self.as_inner().ty()
                + operand.as_inner().ny() * self.as_inner().tx() * self.as_inner().tx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Motor<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -(self.ty() * operand.d() * self.ty()) - self.tx() * operand.d() * self.tx(),
            self.tx() * operand.ny() * self.ty()
                + self.ps() * operand.d() * self.tx()
                + self.r() * operand.d() * self.ty()
                + self.tx() * operand.d() * self.ps()
                + self.ty() * operand.ny() * self.tx()
                - self.tx() * operand.nx() * self.tx()
                + self.ty() * operand.d() * self.r()
                + self.ty() * operand.nx() * self.ty(),
            -(-(self.r() * operand.d() * self.tx()) + self.ty() * operand.d() * self.ps()
                - self.tx() * operand.d() * self.r()
                - self.ty() * operand.nx() * self.tx()
                - self.tx() * operand.ny() * self.tx()
                - self.tx() * operand.nx() * self.ty()
                + self.ty() * operand.ny() * self.ty()
                + self.ps() * operand.d() * self.ty()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Unitized<Motor<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -(operand.d() * self.as_inner().tx() * self.as_inner().tx())
                + -(operand.d() * self.as_inner().ty() * self.as_inner().ty()),
            -(operand.nx() * self.as_inner().tx() * self.as_inner().tx())
                + T::TWO * operand.d() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.d() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.ny() * self.as_inner().tx() * self.as_inner().ty()
                + operand.nx() * self.as_inner().ty() * self.as_inner().ty(),
            -(operand.ny() * self.as_inner().ty() * self.as_inner().ty())
                + -T::TWO * operand.d() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.d() * self.as_inner().r() * self.as_inner().tx()
                + T::TWO * operand.nx() * self.as_inner().tx() * self.as_inner().ty()
                + operand.ny() * self.as_inner().tx() * self.as_inner().tx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Motor<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -(operand.as_inner().d() * self.tx() * self.tx())
                + -(operand.as_inner().d() * self.ty() * self.ty()),
            -(operand.as_inner().nx() * self.tx() * self.tx())
                + T::TWO * operand.as_inner().d() * self.ps() * self.tx()
                + T::TWO * operand.as_inner().d() * self.r() * self.ty()
                + T::TWO * operand.as_inner().ny() * self.tx() * self.ty()
                + operand.as_inner().nx() * self.ty() * self.ty(),
            -(operand.as_inner().ny() * self.ty() * self.ty())
                + -T::TWO * operand.as_inner().d() * self.ps() * self.ty()
                + T::TWO * operand.as_inner().d() * self.r() * self.tx()
                + T::TWO * operand.as_inner().nx() * self.tx() * self.ty()
                + operand.as_inner().ny() * self.tx() * self.tx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Unitized<Motor<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -(operand.as_inner().d() * self.as_inner().tx() * self.as_inner().tx())
                + -(operand.as_inner().d() * self.as_inner().ty() * self.as_inner().ty()),
            -(operand.as_inner().nx() * self.as_inner().tx() * self.as_inner().tx())
                + T::TWO * operand.as_inner().d() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.as_inner().d() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.as_inner().ny() * self.as_inner().tx() * self.as_inner().ty()
                + operand.as_inner().nx() * self.as_inner().ty() * self.as_inner().ty(),
            -(operand.as_inner().ny() * self.as_inner().ty() * self.as_inner().ty())
                + -T::TWO * operand.as_inner().d() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.as_inner().d() * self.as_inner().r() * self.as_inner().tx()
                + T::TWO * operand.as_inner().nx() * self.as_inner().tx() * self.as_inner().ty()
                + operand.as_inner().ny() * self.as_inner().tx() * self.as_inner().tx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            self.ty() * operand.tx() * self.tx()
                + self.tx() * operand.tx() * self.ty()
                + self.ty() * operand.ty() * self.ty()
                - self.tx() * operand.ty() * self.tx(),
            self.ty() * operand.ty() * self.tx() + self.tx() * operand.ty() * self.ty()
                - self.ty() * operand.tx() * self.ty()
                + self.tx() * operand.tx() * self.tx(),
            -(self.ty() * operand.ps() * self.tx()) + self.ty() * operand.ty() * self.r()
                - self.tx() * operand.ty() * self.ps()
                + self.tx() * operand.tx() * self.r()
                + self.r() * operand.ty() * self.ty()
                - self.ps() * operand.ty() * self.tx()
                + self.tx() * operand.ps() * self.ty()
                + self.ty() * operand.tx() * self.ps()
                - self.ty() * operand.r() * self.ty()
                - self.tx() * operand.r() * self.tx()
                + self.r() * operand.tx() * self.tx()
                + self.ps() * operand.tx() * self.ty(),
            self.ty() * operand.tx() * self.r()
                - self.tx() * operand.ty() * self.r()
                - self.ty() * operand.ty() * self.ps()
                - self.tx() * operand.tx() * self.ps()
                + self.r() * operand.ty() * self.tx()
                - self.r() * operand.tx() * self.ty()
                + self.ps() * operand.ty() * self.ty()
                - self.ty() * operand.r() * self.tx()
                + self.ps() * operand.tx() * self.tx()
                + self.tx() * operand.ps() * self.tx()
                + self.ty() * operand.ps() * self.ty()
                + self.tx() * operand.r() * self.ty(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Unitized<Motor<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.ty() * self.as_inner().tx() * self.as_inner().tx())
                + T::TWO * operand.tx() * self.as_inner().tx() * self.as_inner().ty()
                + operand.ty() * self.as_inner().ty() * self.as_inner().ty(),
            -(operand.tx() * self.as_inner().ty() * self.as_inner().ty())
                + T::TWO * operand.ty() * self.as_inner().tx() * self.as_inner().ty()
                + operand.tx() * self.as_inner().tx() * self.as_inner().tx(),
            -(operand.r() * self.as_inner().tx() * self.as_inner().tx())
                + -(operand.r() * self.as_inner().ty() * self.as_inner().ty())
                + -T::TWO * operand.ty() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.tx() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.tx() * self.as_inner().r() * self.as_inner().tx()
                + T::TWO * operand.ty() * self.as_inner().r() * self.as_inner().ty(),
            operand.ps() * self.as_inner().tx() * self.as_inner().tx()
                + operand.ps() * self.as_inner().ty() * self.as_inner().ty(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.tx() * self.tx())
                + T::TWO * operand.as_inner().tx() * self.tx() * self.ty()
                + operand.as_inner().ty() * self.ty() * self.ty(),
            -(operand.as_inner().tx() * self.ty() * self.ty())
                + T::TWO * operand.as_inner().ty() * self.tx() * self.ty()
                + operand.as_inner().tx() * self.tx() * self.tx(),
            -(operand.as_inner().r() * self.tx() * self.tx())
                + -(operand.as_inner().r() * self.ty() * self.ty())
                + -T::TWO * operand.as_inner().ty() * self.ps() * self.tx()
                + T::TWO * operand.as_inner().tx() * self.ps() * self.ty()
                + T::TWO * operand.as_inner().tx() * self.r() * self.tx()
                + T::TWO * operand.as_inner().ty() * self.r() * self.ty(),
            operand.as_inner().ps() * self.tx() * self.tx()
                + operand.as_inner().ps() * self.ty() * self.ty(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Unitized<Motor<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.as_inner().tx() * self.as_inner().tx())
                + T::TWO * operand.as_inner().tx() * self.as_inner().tx() * self.as_inner().ty()
                + operand.as_inner().ty() * self.as_inner().ty() * self.as_inner().ty(),
            -(operand.as_inner().tx() * self.as_inner().ty() * self.as_inner().ty())
                + T::TWO * operand.as_inner().ty() * self.as_inner().tx() * self.as_inner().ty()
                + operand.as_inner().tx() * self.as_inner().tx() * self.as_inner().tx(),
            -(operand.as_inner().r() * self.as_inner().tx() * self.as_inner().tx())
                + -(operand.as_inner().r() * self.as_inner().ty() * self.as_inner().ty())
                + -T::TWO * operand.as_inner().ty() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.as_inner().tx() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.as_inner().tx() * self.as_inner().r() * self.as_inner().tx()
                + T::TWO * operand.as_inner().ty() * self.as_inner().r() * self.as_inner().ty(),
            operand.as_inner().ps() * self.as_inner().tx() * self.as_inner().tx()
                + operand.as_inner().ps() * self.as_inner().ty() * self.as_inner().ty(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Motor<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            self.ty() * operand.y() * self.tx() - self.tx() * operand.x() * self.tx()
                + self.tx() * operand.y() * self.ty()
                + self.ty() * operand.x() * self.ty(),
            -(self.ty() * operand.y() * self.ty())
                + self.tx() * operand.y() * self.tx()
                + self.ty() * operand.x() * self.tx()
                + self.tx() * operand.x() * self.ty(),
            self.ty() * operand.y() * self.ps()
                - self.ty() * operand.w() * self.ty()
                - self.ps() * operand.x() * self.tx()
                + self.r() * operand.x() * self.ty()
                + self.r() * operand.y() * self.tx()
                - self.tx() * operand.x() * self.ps()
                + self.tx() * operand.y() * self.r()
                + self.ty() * operand.x() * self.r()
                - self.tx() * operand.w() * self.tx()
                + self.ps() * operand.y() * self.ty(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Unitized<Motor<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(operand.x() * self.as_inner().tx() * self.as_inner().tx())
                + T::TWO * operand.y() * self.as_inner().tx() * self.as_inner().ty()
                + operand.x() * self.as_inner().ty() * self.as_inner().ty(),
            -(operand.y() * self.as_inner().ty() * self.as_inner().ty())
                + T::TWO * operand.x() * self.as_inner().tx() * self.as_inner().ty()
                + operand.y() * self.as_inner().tx() * self.as_inner().tx(),
            -(operand.w() * self.as_inner().tx() * self.as_inner().tx())
                + -(operand.w() * self.as_inner().ty() * self.as_inner().ty())
                + -T::TWO * operand.x() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.x() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.y() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.y() * self.as_inner().r() * self.as_inner().tx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Motor<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.tx() * self.tx())
                + T::TWO * operand.as_inner().y() * self.tx() * self.ty()
                + operand.as_inner().x() * self.ty() * self.ty(),
            -(operand.as_inner().y() * self.ty() * self.ty())
                + T::TWO * operand.as_inner().x() * self.tx() * self.ty()
                + operand.as_inner().y() * self.tx() * self.tx(),
            -(operand.as_inner().w() * self.tx() * self.tx())
                + -(operand.as_inner().w() * self.ty() * self.ty())
                + -T::TWO * operand.as_inner().x() * self.ps() * self.tx()
                + T::TWO * operand.as_inner().x() * self.r() * self.ty()
                + T::TWO * operand.as_inner().y() * self.ps() * self.ty()
                + T::TWO * operand.as_inner().y() * self.r() * self.tx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Unitized<Motor<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.as_inner().tx() * self.as_inner().tx())
                + T::TWO * operand.as_inner().y() * self.as_inner().tx() * self.as_inner().ty()
                + operand.as_inner().x() * self.as_inner().ty() * self.as_inner().ty(),
            -(operand.as_inner().y() * self.as_inner().ty() * self.as_inner().ty())
                + T::TWO * operand.as_inner().x() * self.as_inner().tx() * self.as_inner().ty()
                + operand.as_inner().y() * self.as_inner().tx() * self.as_inner().tx(),
            -(operand.as_inner().w() * self.as_inner().tx() * self.as_inner().tx())
                + -(operand.as_inner().w() * self.as_inner().ty() * self.as_inner().ty())
                + -T::TWO * operand.as_inner().x() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.as_inner().x() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.as_inner().y() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.as_inner().y() * self.as_inner().r() * self.as_inner().tx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Motor<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(
            self.tx() * operand.s() * self.tx() + self.ty() * operand.s() * self.ty(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Unitized<Motor<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(
            operand.s() * self.as_inner().tx() * self.as_inner().tx()
                + operand.s() * self.as_inner().ty() * self.as_inner().ty(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Motor<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Unitized<Motor<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Motor<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            self.tx() * operand.ps() * self.tx() + self.ty() * operand.ps() * self.ty(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Unitized<Motor<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.ps() * self.as_inner().tx() * self.as_inner().tx()
                + operand.ps() * self.as_inner().ty() * self.as_inner().ty(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Motor<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.as_inner().ps() * self.tx() * self.tx()
                + operand.as_inner().ps() * self.ty() * self.ty(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Unitized<Motor<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.as_inner().ps() * self.as_inner().tx() * self.as_inner().tx()
                + operand.as_inner().ps() * self.as_inner().ty() * self.as_inner().ty(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            -(self.y() * operand.d() * self.x())
                + self.x() * operand.s() * self.x()
                + self.x() * operand.d() * self.y()
                + self.y() * operand.s() * self.y(),
            -(self.y() * operand.d() * self.y()) - self.x() * operand.d() * self.x()
                + self.x() * operand.s() * self.y()
                - self.y() * operand.s() * self.x(),
            -(self.w() * operand.s() * self.y())
                + self.x() * operand.d() * self.w()
                + self.y() * operand.ny() * self.x()
                + self.x() * operand.ny() * self.y()
                + self.x() * operand.nx() * self.x()
                + self.y() * operand.s() * self.w()
                - self.y() * operand.nx() * self.y()
                + self.w() * operand.d() * self.x(),
            -(self.x() * operand.s() * self.w()
                - self.x() * operand.nx() * self.y()
                - self.w() * operand.d() * self.y()
                - self.y() * operand.d() * self.w()
                - self.y() * operand.ny() * self.y()
                + self.x() * operand.ny() * self.x()
                - self.w() * operand.s() * self.x()
                - self.y() * operand.nx() * self.x()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Unitized<Point<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            operand.s() * self.as_inner().x() * self.as_inner().x()
                + operand.s() * self.as_inner().y() * self.as_inner().y(),
            -(operand.d() * self.as_inner().x() * self.as_inner().x())
                + -(operand.d() * self.as_inner().y() * self.as_inner().y()),
            -(operand.nx() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.d() * self.as_inner().w() * self.as_inner().x()
                + T::TWO * operand.ny() * self.as_inner().x() * self.as_inner().y()
                + operand.nx() * self.as_inner().x() * self.as_inner().x(),
            -(operand.ny() * self.as_inner().x() * self.as_inner().x())
                + T::TWO * operand.d() * self.as_inner().w() * self.as_inner().y()
                + T::TWO * operand.nx() * self.as_inner().x() * self.as_inner().y()
                + operand.ny() * self.as_inner().y() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.x() * self.x()
                + operand.as_inner().s() * self.y() * self.y(),
            -(operand.as_inner().d() * self.x() * self.x())
                + -(operand.as_inner().d() * self.y() * self.y()),
            -(operand.as_inner().nx() * self.y() * self.y())
                + T::TWO * operand.as_inner().d() * self.w() * self.x()
                + T::TWO * operand.as_inner().ny() * self.x() * self.y()
                + operand.as_inner().nx() * self.x() * self.x(),
            -(operand.as_inner().ny() * self.x() * self.x())
                + T::TWO * operand.as_inner().d() * self.w() * self.y()
                + T::TWO * operand.as_inner().nx() * self.x() * self.y()
                + operand.as_inner().ny() * self.y() * self.y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Unitized<Point<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.as_inner().x() * self.as_inner().x()
                + operand.as_inner().s() * self.as_inner().y() * self.as_inner().y(),
            -(operand.as_inner().d() * self.as_inner().x() * self.as_inner().x())
                + -(operand.as_inner().d() * self.as_inner().y() * self.as_inner().y()),
            -(operand.as_inner().nx() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.as_inner().d() * self.as_inner().w() * self.as_inner().x()
                + T::TWO * operand.as_inner().ny() * self.as_inner().x() * self.as_inner().y()
                + operand.as_inner().nx() * self.as_inner().x() * self.as_inner().x(),
            -(operand.as_inner().ny() * self.as_inner().x() * self.as_inner().x())
                + T::TWO * operand.as_inner().d() * self.as_inner().w() * self.as_inner().y()
                + T::TWO * operand.as_inner().nx() * self.as_inner().x() * self.as_inner().y()
                + operand.as_inner().ny() * self.as_inner().y() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -(self.x() * operand.d() * self.x()) - self.y() * operand.d() * self.y(),
            -(self.y() * operand.nx() * self.y())
                + self.w() * operand.d() * self.x()
                + self.x() * operand.d() * self.w()
                + self.x() * operand.nx() * self.x()
                + self.x() * operand.ny() * self.y()
                + self.y() * operand.ny() * self.x(),
            -(-(self.w() * operand.d() * self.y()) - self.y() * operand.d() * self.w()
                + self.x() * operand.ny() * self.x()
                - self.x() * operand.nx() * self.y()
                - self.y() * operand.nx() * self.x()
                - self.y() * operand.ny() * self.y()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -(operand.d() * self.as_inner().x() * self.as_inner().x())
                + -(operand.d() * self.as_inner().y() * self.as_inner().y()),
            -(operand.nx() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.d() * self.as_inner().w() * self.as_inner().x()
                + T::TWO * operand.ny() * self.as_inner().x() * self.as_inner().y()
                + operand.nx() * self.as_inner().x() * self.as_inner().x(),
            -(operand.ny() * self.as_inner().x() * self.as_inner().x())
                + T::TWO * operand.d() * self.as_inner().w() * self.as_inner().y()
                + T::TWO * operand.nx() * self.as_inner().x() * self.as_inner().y()
                + operand.ny() * self.as_inner().y() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -(operand.as_inner().d() * self.x() * self.x())
                + -(operand.as_inner().d() * self.y() * self.y()),
            -(operand.as_inner().nx() * self.y() * self.y())
                + T::TWO * operand.as_inner().d() * self.w() * self.x()
                + T::TWO * operand.as_inner().ny() * self.x() * self.y()
                + operand.as_inner().nx() * self.x() * self.x(),
            -(operand.as_inner().ny() * self.x() * self.x())
                + T::TWO * operand.as_inner().d() * self.w() * self.y()
                + T::TWO * operand.as_inner().nx() * self.x() * self.y()
                + operand.as_inner().ny() * self.y() * self.y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -(operand.as_inner().d() * self.as_inner().x() * self.as_inner().x())
                + -(operand.as_inner().d() * self.as_inner().y() * self.as_inner().y()),
            -(operand.as_inner().nx() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.as_inner().d() * self.as_inner().w() * self.as_inner().x()
                + T::TWO * operand.as_inner().ny() * self.as_inner().x() * self.as_inner().y()
                + operand.as_inner().nx() * self.as_inner().x() * self.as_inner().x(),
            -(operand.as_inner().ny() * self.as_inner().x() * self.as_inner().x())
                + T::TWO * operand.as_inner().d() * self.as_inner().w() * self.as_inner().y()
                + T::TWO * operand.as_inner().nx() * self.as_inner().x() * self.as_inner().y()
                + operand.as_inner().ny() * self.as_inner().y() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            self.x() * operand.tx() * self.y() - self.y() * operand.ty() * self.y()
                + self.y() * operand.tx() * self.x()
                + self.x() * operand.ty() * self.x(),
            self.x() * operand.ty() * self.y() - self.x() * operand.tx() * self.x()
                + self.y() * operand.tx() * self.y()
                + self.y() * operand.ty() * self.x(),
            -(self.x() * operand.r() * self.x())
                + self.y() * operand.ps() * self.x()
                + self.w() * operand.ty() * self.x()
                + self.w() * operand.tx() * self.y()
                - self.x() * operand.ps() * self.y()
                + self.x() * operand.ty() * self.w()
                + self.y() * operand.tx() * self.w()
                - self.y() * operand.r() * self.y(),
            -(self.x() * operand.r() * self.y())
                + self.y() * operand.ps() * self.y()
                + self.x() * operand.ps() * self.x()
                - self.w() * operand.tx() * self.x()
                + self.x() * operand.tx() * self.w()
                + self.y() * operand.r() * self.x()
                + self.w() * operand.ty() * self.y()
                - self.y() * operand.ty() * self.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Unitized<Point<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.ty() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.tx() * self.as_inner().x() * self.as_inner().y()
                + operand.ty() * self.as_inner().x() * self.as_inner().x(),
            -(operand.tx() * self.as_inner().x() * self.as_inner().x())
                + T::TWO * operand.ty() * self.as_inner().x() * self.as_inner().y()
                + operand.tx() * self.as_inner().y() * self.as_inner().y(),
            -(operand.r() * self.as_inner().x() * self.as_inner().x())
                + -(operand.r() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.tx() * self.as_inner().w() * self.as_inner().y()
                + T::TWO * operand.ty() * self.as_inner().w() * self.as_inner().x(),
            operand.ps() * self.as_inner().x() * self.as_inner().x()
                + operand.ps() * self.as_inner().y() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.y() * self.y())
                + T::TWO * operand.as_inner().tx() * self.x() * self.y()
                + operand.as_inner().ty() * self.x() * self.x(),
            -(operand.as_inner().tx() * self.x() * self.x())
                + T::TWO * operand.as_inner().ty() * self.x() * self.y()
                + operand.as_inner().tx() * self.y() * self.y(),
            -(operand.as_inner().r() * self.x() * self.x())
                + -(operand.as_inner().r() * self.y() * self.y())
                + T::TWO * operand.as_inner().tx() * self.w() * self.y()
                + T::TWO * operand.as_inner().ty() * self.w() * self.x(),
            operand.as_inner().ps() * self.x() * self.x()
                + operand.as_inner().ps() * self.y() * self.y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Unitized<Point<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.as_inner().tx() * self.as_inner().x() * self.as_inner().y()
                + operand.as_inner().ty() * self.as_inner().x() * self.as_inner().x(),
            -(operand.as_inner().tx() * self.as_inner().x() * self.as_inner().x())
                + T::TWO * operand.as_inner().ty() * self.as_inner().x() * self.as_inner().y()
                + operand.as_inner().tx() * self.as_inner().y() * self.as_inner().y(),
            -(operand.as_inner().r() * self.as_inner().x() * self.as_inner().x())
                + -(operand.as_inner().r() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.as_inner().tx() * self.as_inner().w() * self.as_inner().y()
                + T::TWO * operand.as_inner().ty() * self.as_inner().w() * self.as_inner().x(),
            operand.as_inner().ps() * self.as_inner().x() * self.as_inner().x()
                + operand.as_inner().ps() * self.as_inner().y() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            self.x() * operand.y() * self.y() - self.y() * operand.x() * self.y()
                + self.x() * operand.x() * self.x()
                + self.y() * operand.y() * self.x(),
            self.x() * operand.x() * self.y() - self.x() * operand.y() * self.x()
                + self.y() * operand.x() * self.x()
                + self.y() * operand.y() * self.y(),
            self.w() * operand.x() * self.x() - self.y() * operand.w() * self.y()
                + self.w() * operand.y() * self.y()
                - self.x() * operand.w() * self.x()
                + self.x() * operand.x() * self.w()
                + self.y() * operand.y() * self.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(operand.x() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.y() * self.as_inner().x() * self.as_inner().y()
                + operand.x() * self.as_inner().x() * self.as_inner().x(),
            -(operand.y() * self.as_inner().x() * self.as_inner().x())
                + T::TWO * operand.x() * self.as_inner().x() * self.as_inner().y()
                + operand.y() * self.as_inner().y() * self.as_inner().y(),
            -(operand.w() * self.as_inner().x() * self.as_inner().x())
                + -(operand.w() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.x() * self.as_inner().w() * self.as_inner().x()
                + T::TWO * operand.y() * self.as_inner().w() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.y() * self.y())
                + T::TWO * operand.as_inner().y() * self.x() * self.y()
                + operand.as_inner().x() * self.x() * self.x(),
            -(operand.as_inner().y() * self.x() * self.x())
                + T::TWO * operand.as_inner().x() * self.x() * self.y()
                + operand.as_inner().y() * self.y() * self.y(),
            -(operand.as_inner().w() * self.x() * self.x())
                + -(operand.as_inner().w() * self.y() * self.y())
                + T::TWO * operand.as_inner().x() * self.w() * self.x()
                + T::TWO * operand.as_inner().y() * self.w() * self.y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.as_inner().y() * self.as_inner().x() * self.as_inner().y()
                + operand.as_inner().x() * self.as_inner().x() * self.as_inner().x(),
            -(operand.as_inner().y() * self.as_inner().x() * self.as_inner().x())
                + T::TWO * operand.as_inner().x() * self.as_inner().x() * self.as_inner().y()
                + operand.as_inner().y() * self.as_inner().y() * self.as_inner().y(),
            -(operand.as_inner().w() * self.as_inner().x() * self.as_inner().x())
                + -(operand.as_inner().w() * self.as_inner().y() * self.as_inner().y())
                + T::TWO * operand.as_inner().x() * self.as_inner().w() * self.as_inner().x()
                + T::TWO * operand.as_inner().y() * self.as_inner().w() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(self.x() * operand.s() * self.x() + self.y() * operand.s() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(
            operand.s() * self.as_inner().x() * self.as_inner().x()
                + operand.s() * self.as_inner().y() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            self.x() * operand.ps() * self.x() + self.y() * operand.ps() * self.y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.ps() * self.as_inner().x() * self.as_inner().x()
                + operand.ps() * self.as_inner().y() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.as_inner().ps() * self.x() * self.x()
                + operand.as_inner().ps() * self.y() * self.y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.as_inner().ps() * self.as_inner().x() * self.as_inner().x()
                + operand.as_inner().ps() * self.as_inner().y() * self.as_inner().y(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Scalar<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            self.s() * operand.s() * self.s(),
            self.s() * operand.d() * self.s(),
            self.s() * operand.nx() * self.s(),
            -(-(self.s() * operand.ny() * self.s())),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Unitized<Scalar<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Scalar<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.s() * self.s(),
            operand.as_inner().d() * self.s() * self.s(),
            operand.as_inner().nx() * self.s() * self.s(),
            operand.as_inner().ny() * self.s() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Unitized<Scalar<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            self.s() * operand.d() * self.s(),
            self.s() * operand.nx() * self.s(),
            -(-(self.s() * operand.ny() * self.s())),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            operand.as_inner().d() * self.s() * self.s(),
            operand.as_inner().nx() * self.s() * self.s(),
            operand.as_inner().ny() * self.s() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Scalar<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            self.s() * operand.ty() * self.s(),
            self.s() * operand.tx() * self.s(),
            self.s() * operand.r() * self.s(),
            self.s() * operand.ps() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Unitized<Scalar<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Scalar<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            operand.as_inner().ty() * self.s() * self.s(),
            operand.as_inner().tx() * self.s() * self.s(),
            operand.as_inner().r() * self.s() * self.s(),
            operand.as_inner().ps() * self.s() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Unitized<Scalar<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            self.s() * operand.x() * self.s(),
            self.s() * operand.y() * self.s(),
            self.s() * operand.w() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            operand.as_inner().x() * self.s() * self.s(),
            operand.as_inner().y() * self.s() * self.s(),
            operand.as_inner().w() * self.s() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(self.s() * operand.s() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(self.s() * operand.ps() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(operand.as_inner().ps() * self.s() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Trivector<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(T::zero(), T::zero(), T::zero(), -(T::zero()))
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Flector<T>> for Unitized<Trivector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Trivector<T> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Flector<T>>> for Unitized<Trivector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), -(T::zero()))
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Line<T>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Line<T>>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Trivector<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Motor<T>> for Unitized<Trivector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Trivector<T> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Motor<T>>> for Unitized<Trivector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Point<T>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Point<T>>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Scalar<T>> for Unitized<Trivector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Scalar<T>>> for Unitized<Trivector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Trivector<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Sandwich<Unitized<Trivector<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn sandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            self.nx() * operand.ny() * self.d() - self.nx() * operand.d() * self.ny()
                + self.ny() * operand.d() * self.nx()
                - self.ny() * operand.nx() * self.d()
                + self.ny() * operand.s() * self.ny()
                - self.d() * operand.ny() * self.nx()
                + self.nx() * operand.s() * self.nx()
                + self.s() * operand.nx() * self.nx()
                - self.nx() * operand.nx() * self.s()
                + self.s() * operand.ny() * self.ny()
                - self.ny() * operand.ny() * self.s()
                + self.d() * operand.nx() * self.ny(),
            self.s() * operand.ny() * self.nx()
                - self.ny() * operand.nx() * self.s()
                - self.nx() * operand.d() * self.nx()
                + self.ny() * operand.s() * self.nx()
                + self.nx() * operand.nx() * self.d()
                - self.ny() * operand.d() * self.ny()
                - self.nx() * operand.s() * self.ny()
                + self.ny() * operand.ny() * self.d()
                - self.s() * operand.nx() * self.ny()
                + self.d() * operand.nx() * self.nx()
                + self.d() * operand.ny() * self.ny()
                + self.nx() * operand.ny() * self.s(),
            self.nx() * operand.ny() * self.ny() + self.ny() * operand.ny() * self.nx()
                - self.ny() * operand.nx() * self.ny()
                + self.nx() * operand.nx() * self.nx(),
            -(-(self.nx() * operand.nx() * self.ny()) + self.nx() * operand.ny() * self.nx()
                - self.ny() * operand.nx() * self.nx()
                - self.ny() * operand.ny() * self.ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Unitized<Flector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            operand.s(),
            -(operand.d())
                + -T::TWO * operand.nx() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.nx() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.ny() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.ny() * self.as_inner().nx() * self.as_inner().s(),
            -T::TWO * operand.nx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.ny() * self.as_inner().nx() * self.as_inner().ny()
                + operand.nx(),
            -(operand.ny())
                + T::TWO * operand.nx() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.ny() * self.as_inner().ny() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.nx() * self.nx()
                + operand.as_inner().s() * self.ny() * self.ny(),
            -(operand.as_inner().d() * self.nx() * self.nx())
                + -(operand.as_inner().d() * self.ny() * self.ny())
                + -T::TWO * operand.as_inner().nx() * self.ny() * self.s()
                + T::TWO * operand.as_inner().nx() * self.d() * self.nx()
                + T::TWO * operand.as_inner().ny() * self.d() * self.ny()
                + T::TWO * operand.as_inner().ny() * self.nx() * self.s(),
            -(operand.as_inner().nx() * self.ny() * self.ny())
                + T::TWO * operand.as_inner().ny() * self.nx() * self.ny()
                + operand.as_inner().nx() * self.nx() * self.nx(),
            -(operand.as_inner().ny() * self.nx() * self.nx())
                + T::TWO * operand.as_inner().nx() * self.nx() * self.ny()
                + operand.as_inner().ny() * self.ny() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Unitized<Flector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s(),
            -(operand.as_inner().d())
                + -T::TWO * operand.as_inner().nx() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.as_inner().nx() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().ny() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ny() * self.as_inner().nx() * self.as_inner().s(),
            -T::TWO * operand.as_inner().nx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ny() * self.as_inner().nx() * self.as_inner().ny()
                + operand.as_inner().nx(),
            -(operand.as_inner().ny())
                + T::TWO * operand.as_inner().nx() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ny() * self.as_inner().ny() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Flector<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            self.nx() * operand.nx() * self.d()
                + self.s() * operand.ny() * self.nx()
                + self.d() * operand.nx() * self.nx()
                - self.ny() * operand.d() * self.ny()
                - self.ny() * operand.nx() * self.s()
                + self.d() * operand.ny() * self.ny()
                + self.nx() * operand.ny() * self.s()
                + self.ny() * operand.ny() * self.d()
                - self.s() * operand.nx() * self.ny()
                - self.nx() * operand.d() * self.nx(),
            -(self.ny() * operand.nx() * self.ny())
                + self.nx() * operand.nx() * self.nx()
                + self.ny() * operand.ny() * self.nx()
                + self.nx() * operand.ny() * self.ny(),
            -(-(self.nx() * operand.nx() * self.ny())
                - self.ny() * operand.ny() * self.ny()
                - self.ny() * operand.nx() * self.nx()
                + self.nx() * operand.ny() * self.nx()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Unitized<Flector<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -(operand.d())
                + -T::TWO * operand.nx() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.nx() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.ny() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.ny() * self.as_inner().nx() * self.as_inner().s(),
            -T::TWO * operand.nx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.ny() * self.as_inner().nx() * self.as_inner().ny()
                + operand.nx(),
            -(operand.ny())
                + T::TWO * operand.nx() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.ny() * self.as_inner().ny() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Flector<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -(operand.as_inner().d() * self.nx() * self.nx())
                + -(operand.as_inner().d() * self.ny() * self.ny())
                + -T::TWO * operand.as_inner().nx() * self.ny() * self.s()
                + T::TWO * operand.as_inner().nx() * self.d() * self.nx()
                + T::TWO * operand.as_inner().ny() * self.d() * self.ny()
                + T::TWO * operand.as_inner().ny() * self.nx() * self.s(),
            -(operand.as_inner().nx() * self.ny() * self.ny())
                + T::TWO * operand.as_inner().ny() * self.nx() * self.ny()
                + operand.as_inner().nx() * self.nx() * self.nx(),
            -(operand.as_inner().ny() * self.nx() * self.nx())
                + T::TWO * operand.as_inner().nx() * self.nx() * self.ny()
                + operand.as_inner().ny() * self.ny() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Unitized<Flector<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -(operand.as_inner().d())
                + -T::TWO * operand.as_inner().nx() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.as_inner().nx() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().ny() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ny() * self.as_inner().nx() * self.as_inner().s(),
            -T::TWO * operand.as_inner().nx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ny() * self.as_inner().nx() * self.as_inner().ny()
                + operand.as_inner().nx(),
            -(operand.as_inner().ny())
                + T::TWO * operand.as_inner().nx() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ny() * self.as_inner().ny() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            self.nx() * operand.ty() * self.nx()
                + self.nx() * operand.tx() * self.ny()
                + self.nx() * operand.r() * self.d()
                - self.ny() * operand.ty() * self.ny()
                + self.ny() * operand.r() * self.s()
                + self.s() * operand.ps() * self.nx()
                + self.d() * operand.r() * self.nx()
                - self.d() * operand.ps() * self.ny()
                + self.ny() * operand.tx() * self.nx()
                + self.ny() * operand.ps() * self.d()
                + self.s() * operand.r() * self.ny()
                - self.nx() * operand.ps() * self.s(),
            -(self.s() * operand.r() * self.nx())
                + self.ny() * operand.tx() * self.ny()
                + self.ny() * operand.ty() * self.nx()
                - self.nx() * operand.ps() * self.d()
                + self.s() * operand.ps() * self.ny()
                + self.d() * operand.r() * self.ny()
                - self.nx() * operand.r() * self.s()
                - self.nx() * operand.tx() * self.nx()
                + self.nx() * operand.ty() * self.ny()
                - self.ny() * operand.ps() * self.s()
                + self.d() * operand.ps() * self.nx()
                + self.ny() * operand.r() * self.d(),
            -(self.ny() * operand.r() * self.ny())
                - self.ny() * operand.ps() * self.nx()
                - self.nx() * operand.r() * self.nx()
                + self.nx() * operand.ps() * self.ny(),
            self.nx() * operand.ps() * self.nx() - self.ny() * operand.r() * self.nx()
                + self.nx() * operand.r() * self.ny()
                + self.ny() * operand.ps() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Unitized<Flector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            -T::TWO * operand.ty() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.r() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.r() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.tx() * self.as_inner().nx() * self.as_inner().ny()
                + operand.ty(),
            -(operand.tx())
                + -T::TWO * operand.r() * self.as_inner().nx() * self.as_inner().s()
                + T::TWO * operand.r() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.tx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.ty() * self.as_inner().nx() * self.as_inner().ny(),
            -(operand.r()),
            operand.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.ny() * self.ny())
                + T::TWO * operand.as_inner().r() * self.d() * self.nx()
                + T::TWO * operand.as_inner().r() * self.ny() * self.s()
                + T::TWO * operand.as_inner().tx() * self.nx() * self.ny()
                + operand.as_inner().ty() * self.nx() * self.nx(),
            -(operand.as_inner().tx() * self.nx() * self.nx())
                + -T::TWO * operand.as_inner().r() * self.nx() * self.s()
                + T::TWO * operand.as_inner().r() * self.d() * self.ny()
                + T::TWO * operand.as_inner().ty() * self.nx() * self.ny()
                + operand.as_inner().tx() * self.ny() * self.ny(),
            -(operand.as_inner().r() * self.nx() * self.nx())
                + -(operand.as_inner().r() * self.ny() * self.ny()),
            operand.as_inner().ps() * self.nx() * self.nx()
                + operand.as_inner().ps() * self.ny() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Unitized<Flector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -T::TWO * operand.as_inner().ty() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.as_inner().r() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().r() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.as_inner().tx() * self.as_inner().nx() * self.as_inner().ny()
                + operand.as_inner().ty(),
            -(operand.as_inner().tx())
                + -T::TWO * operand.as_inner().r() * self.as_inner().nx() * self.as_inner().s()
                + T::TWO * operand.as_inner().r() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.as_inner().tx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ty() * self.as_inner().nx() * self.as_inner().ny(),
            -(operand.as_inner().r()),
            operand.as_inner().ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Flector<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            self.nx() * operand.y() * self.ny() + self.nx() * operand.x() * self.nx()
                - self.ny() * operand.x() * self.ny()
                + self.s() * operand.w() * self.ny()
                + self.d() * operand.w() * self.nx()
                + self.ny() * operand.w() * self.s()
                + self.ny() * operand.y() * self.nx()
                + self.nx() * operand.w() * self.d(),
            -(self.nx() * operand.y() * self.nx())
                + self.ny() * operand.x() * self.nx()
                + self.ny() * operand.w() * self.d()
                - self.nx() * operand.w() * self.s()
                + self.d() * operand.w() * self.ny()
                - self.s() * operand.w() * self.nx()
                + self.nx() * operand.x() * self.ny()
                + self.ny() * operand.y() * self.ny(),
            -(self.nx() * operand.w() * self.nx()) - self.ny() * operand.w() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Unitized<Flector<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -T::TWO * operand.x() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.w() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.w() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.y() * self.as_inner().nx() * self.as_inner().ny()
                + operand.x(),
            -(operand.y())
                + -T::TWO * operand.w() * self.as_inner().nx() * self.as_inner().s()
                + T::TWO * operand.w() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.x() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.y() * self.as_inner().ny() * self.as_inner().ny(),
            -(operand.w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Flector<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.ny() * self.ny())
                + T::TWO * operand.as_inner().w() * self.d() * self.nx()
                + T::TWO * operand.as_inner().w() * self.ny() * self.s()
                + T::TWO * operand.as_inner().y() * self.nx() * self.ny()
                + operand.as_inner().x() * self.nx() * self.nx(),
            -(operand.as_inner().y() * self.nx() * self.nx())
                + -T::TWO * operand.as_inner().w() * self.nx() * self.s()
                + T::TWO * operand.as_inner().w() * self.d() * self.ny()
                + T::TWO * operand.as_inner().x() * self.nx() * self.ny()
                + operand.as_inner().y() * self.ny() * self.ny(),
            -(operand.as_inner().w() * self.nx() * self.nx())
                + -(operand.as_inner().w() * self.ny() * self.ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Unitized<Flector<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -T::TWO * operand.as_inner().x() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.as_inner().w() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().w() * self.as_inner().ny() * self.as_inner().s()
                + T::TWO * operand.as_inner().y() * self.as_inner().nx() * self.as_inner().ny()
                + operand.as_inner().x(),
            -(operand.as_inner().y())
                + -T::TWO * operand.as_inner().w() * self.as_inner().nx() * self.as_inner().s()
                + T::TWO * operand.as_inner().w() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.as_inner().x() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.as_inner().y() * self.as_inner().ny() * self.as_inner().ny(),
            -(operand.as_inner().w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Flector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(
            self.ny() * operand.s() * self.ny() + self.nx() * operand.s() * self.nx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Unitized<Flector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(operand.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Flector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Unitized<Flector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Flector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            self.nx() * operand.ps() * self.nx() + self.ny() * operand.ps() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Unitized<Flector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(operand.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Flector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.as_inner().ps() * self.nx() * self.nx()
                + operand.as_inner().ps() * self.ny() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Unitized<Flector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(operand.as_inner().ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            self.ny() * operand.s() * self.ny() - self.d() * operand.ny() * self.nx()
                + self.d() * operand.nx() * self.ny()
                + self.nx() * operand.s() * self.nx()
                - self.nx() * operand.d() * self.ny()
                - self.ny() * operand.nx() * self.d()
                + self.nx() * operand.ny() * self.d()
                + self.ny() * operand.d() * self.nx(),
            self.ny() * operand.s() * self.nx()
                - self.nx() * operand.d() * self.nx()
                - self.ny() * operand.d() * self.ny()
                + self.d() * operand.nx() * self.nx()
                + self.d() * operand.ny() * self.ny()
                + self.nx() * operand.nx() * self.d()
                + self.ny() * operand.ny() * self.d()
                - self.nx() * operand.s() * self.ny(),
            -(self.ny() * operand.nx() * self.ny())
                + self.nx() * operand.nx() * self.nx()
                + self.ny() * operand.ny() * self.nx()
                + self.nx() * operand.ny() * self.ny(),
            -(-(self.ny() * operand.nx() * self.nx())
                - self.ny() * operand.ny() * self.ny()
                - self.nx() * operand.nx() * self.ny()
                + self.nx() * operand.ny() * self.nx()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Unitized<Line<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            operand.s(),
            -(operand.d())
                + T::TWO * operand.nx() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.ny() * self.as_inner().d() * self.as_inner().ny(),
            -T::TWO * operand.nx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.ny() * self.as_inner().nx() * self.as_inner().ny()
                + operand.nx(),
            -(operand.ny())
                + T::TWO * operand.nx() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.ny() * self.as_inner().ny() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.nx() * self.nx()
                + operand.as_inner().s() * self.ny() * self.ny(),
            -(operand.as_inner().d() * self.nx() * self.nx())
                + -(operand.as_inner().d() * self.ny() * self.ny())
                + T::TWO * operand.as_inner().nx() * self.d() * self.nx()
                + T::TWO * operand.as_inner().ny() * self.d() * self.ny(),
            -(operand.as_inner().nx() * self.ny() * self.ny())
                + T::TWO * operand.as_inner().ny() * self.nx() * self.ny()
                + operand.as_inner().nx() * self.nx() * self.nx(),
            -(operand.as_inner().ny() * self.nx() * self.nx())
                + T::TWO * operand.as_inner().nx() * self.nx() * self.ny()
                + operand.as_inner().ny() * self.ny() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Unitized<Line<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s(),
            -(operand.as_inner().d())
                + T::TWO * operand.as_inner().nx() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().ny() * self.as_inner().d() * self.as_inner().ny(),
            -T::TWO * operand.as_inner().nx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ny() * self.as_inner().nx() * self.as_inner().ny()
                + operand.as_inner().nx(),
            -(operand.as_inner().ny())
                + T::TWO * operand.as_inner().nx() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ny() * self.as_inner().ny() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -(self.nx() * operand.d() * self.nx())
                + self.ny() * operand.ny() * self.d()
                + self.d() * operand.nx() * self.nx()
                + self.nx() * operand.nx() * self.d()
                - self.ny() * operand.d() * self.ny()
                + self.d() * operand.ny() * self.ny(),
            self.ny() * operand.ny() * self.nx()
                + self.nx() * operand.nx() * self.nx()
                + self.nx() * operand.ny() * self.ny()
                - self.ny() * operand.nx() * self.ny(),
            -(-(self.nx() * operand.nx() * self.ny()) + self.nx() * operand.ny() * self.nx()
                - self.ny() * operand.ny() * self.ny()
                - self.ny() * operand.nx() * self.nx()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -(operand.d())
                + T::TWO * operand.nx() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.ny() * self.as_inner().d() * self.as_inner().ny(),
            -T::TWO * operand.nx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.ny() * self.as_inner().nx() * self.as_inner().ny()
                + operand.nx(),
            -(operand.ny())
                + T::TWO * operand.nx() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.ny() * self.as_inner().ny() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -(operand.as_inner().d() * self.nx() * self.nx())
                + -(operand.as_inner().d() * self.ny() * self.ny())
                + T::TWO * operand.as_inner().nx() * self.d() * self.nx()
                + T::TWO * operand.as_inner().ny() * self.d() * self.ny(),
            -(operand.as_inner().nx() * self.ny() * self.ny())
                + T::TWO * operand.as_inner().ny() * self.nx() * self.ny()
                + operand.as_inner().nx() * self.nx() * self.nx(),
            -(operand.as_inner().ny() * self.nx() * self.nx())
                + T::TWO * operand.as_inner().nx() * self.nx() * self.ny()
                + operand.as_inner().ny() * self.ny() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -(operand.as_inner().d())
                + T::TWO * operand.as_inner().nx() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().ny() * self.as_inner().d() * self.as_inner().ny(),
            -T::TWO * operand.as_inner().nx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ny() * self.as_inner().nx() * self.as_inner().ny()
                + operand.as_inner().nx(),
            -(operand.as_inner().ny())
                + T::TWO * operand.as_inner().nx() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ny() * self.as_inner().ny() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            self.ny() * operand.tx() * self.nx()
                + self.nx() * operand.ty() * self.nx()
                + self.nx() * operand.tx() * self.ny()
                - self.d() * operand.ps() * self.ny()
                + self.d() * operand.r() * self.nx()
                + self.nx() * operand.r() * self.d()
                + self.ny() * operand.ps() * self.d()
                - self.ny() * operand.ty() * self.ny(),
            -(self.nx() * operand.tx() * self.nx())
                + self.d() * operand.ps() * self.nx()
                + self.ny() * operand.tx() * self.ny()
                - self.nx() * operand.ps() * self.d()
                + self.nx() * operand.ty() * self.ny()
                + self.ny() * operand.ty() * self.nx()
                + self.d() * operand.r() * self.ny()
                + self.ny() * operand.r() * self.d(),
            -(self.nx() * operand.r() * self.nx()) + self.nx() * operand.ps() * self.ny()
                - self.ny() * operand.r() * self.ny()
                - self.ny() * operand.ps() * self.nx(),
            self.nx() * operand.ps() * self.nx() + self.nx() * operand.r() * self.ny()
                - self.ny() * operand.r() * self.nx()
                + self.ny() * operand.ps() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Unitized<Line<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            -T::TWO * operand.ty() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.r() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.tx() * self.as_inner().nx() * self.as_inner().ny()
                + operand.ty(),
            -(operand.tx())
                + T::TWO * operand.r() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.tx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.ty() * self.as_inner().nx() * self.as_inner().ny(),
            -(operand.r()),
            operand.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.ny() * self.ny())
                + T::TWO * operand.as_inner().r() * self.d() * self.nx()
                + T::TWO * operand.as_inner().tx() * self.nx() * self.ny()
                + operand.as_inner().ty() * self.nx() * self.nx(),
            -(operand.as_inner().tx() * self.nx() * self.nx())
                + T::TWO * operand.as_inner().r() * self.d() * self.ny()
                + T::TWO * operand.as_inner().ty() * self.nx() * self.ny()
                + operand.as_inner().tx() * self.ny() * self.ny(),
            -(operand.as_inner().r() * self.nx() * self.nx())
                + -(operand.as_inner().r() * self.ny() * self.ny()),
            operand.as_inner().ps() * self.nx() * self.nx()
                + operand.as_inner().ps() * self.ny() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Unitized<Line<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -T::TWO * operand.as_inner().ty() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.as_inner().r() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().tx() * self.as_inner().nx() * self.as_inner().ny()
                + operand.as_inner().ty(),
            -(operand.as_inner().tx())
                + T::TWO * operand.as_inner().r() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.as_inner().tx() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.as_inner().ty() * self.as_inner().nx() * self.as_inner().ny(),
            -(operand.as_inner().r()),
            operand.as_inner().ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            self.nx() * operand.y() * self.ny() - self.ny() * operand.x() * self.ny()
                + self.nx() * operand.w() * self.d()
                + self.ny() * operand.y() * self.nx()
                + self.nx() * operand.x() * self.nx()
                + self.d() * operand.w() * self.nx(),
            -(self.nx() * operand.y() * self.nx())
                + self.ny() * operand.x() * self.nx()
                + self.d() * operand.w() * self.ny()
                + self.ny() * operand.w() * self.d()
                + self.nx() * operand.x() * self.ny()
                + self.ny() * operand.y() * self.ny(),
            -(self.ny() * operand.w() * self.ny()) - self.nx() * operand.w() * self.nx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -T::TWO * operand.x() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.w() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.y() * self.as_inner().nx() * self.as_inner().ny()
                + operand.x(),
            -(operand.y())
                + T::TWO * operand.w() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.x() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.y() * self.as_inner().ny() * self.as_inner().ny(),
            -(operand.w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.ny() * self.ny())
                + T::TWO * operand.as_inner().w() * self.d() * self.nx()
                + T::TWO * operand.as_inner().y() * self.nx() * self.ny()
                + operand.as_inner().x() * self.nx() * self.nx(),
            -(operand.as_inner().y() * self.nx() * self.nx())
                + T::TWO * operand.as_inner().w() * self.d() * self.ny()
                + T::TWO * operand.as_inner().x() * self.nx() * self.ny()
                + operand.as_inner().y() * self.ny() * self.ny(),
            -(operand.as_inner().w() * self.nx() * self.nx())
                + -(operand.as_inner().w() * self.ny() * self.ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -T::TWO * operand.as_inner().x() * self.as_inner().ny() * self.as_inner().ny()
                + T::TWO * operand.as_inner().w() * self.as_inner().d() * self.as_inner().nx()
                + T::TWO * operand.as_inner().y() * self.as_inner().nx() * self.as_inner().ny()
                + operand.as_inner().x(),
            -(operand.as_inner().y())
                + T::TWO * operand.as_inner().w() * self.as_inner().d() * self.as_inner().ny()
                + T::TWO * operand.as_inner().x() * self.as_inner().nx() * self.as_inner().ny()
                + T::TWO * operand.as_inner().y() * self.as_inner().ny() * self.as_inner().ny(),
            -(operand.as_inner().w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(
            self.nx() * operand.s() * self.nx() + self.ny() * operand.s() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(operand.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            self.nx() * operand.ps() * self.nx() + self.ny() * operand.ps() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(operand.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.as_inner().ps() * self.nx() * self.nx()
                + operand.as_inner().ps() * self.ny() * self.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(operand.as_inner().ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            self.ty() * operand.ny() * self.r()
                - self.ps() * operand.ny() * self.tx()
                - self.ps() * operand.d() * self.r()
                + self.r() * operand.d() * self.ps()
                + self.r() * operand.s() * self.r()
                + self.r() * operand.nx() * self.tx()
                - self.ps() * operand.nx() * self.ty()
                - self.r() * operand.ny() * self.ty()
                - self.tx() * operand.nx() * self.r()
                + self.ty() * operand.nx() * self.ps()
                + self.tx() * operand.ny() * self.ps()
                + self.ps() * operand.s() * self.ps(),
            self.r() * operand.ny() * self.tx() - self.ps() * operand.ny() * self.ty()
                + self.r() * operand.nx() * self.ty()
                + self.tx() * operand.nx() * self.ps()
                - self.ty() * operand.ny() * self.ps()
                + self.ps() * operand.s() * self.r()
                + self.ps() * operand.nx() * self.tx()
                + self.r() * operand.d() * self.r()
                - self.r() * operand.s() * self.ps()
                + self.ty() * operand.nx() * self.r()
                + self.tx() * operand.ny() * self.r()
                + self.ps() * operand.d() * self.ps(),
            self.ps() * operand.ny() * self.r() + self.r() * operand.ny() * self.ps()
                - self.r() * operand.nx() * self.r()
                + self.ps() * operand.nx() * self.ps(),
            -(self.r() * operand.nx() * self.ps()
                + self.r() * operand.ny() * self.r()
                + self.ps() * operand.nx() * self.r()
                - self.ps() * operand.ny() * self.ps()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Unitized<Motor<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            operand.s(),
            -T::TWO * operand.ny() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.nx() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.nx() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.ny() * self.as_inner().r() * self.as_inner().tx()
                + operand.d(),
            -T::TWO * operand.nx() * self.as_inner().r() * self.as_inner().r()
                + T::TWO * operand.ny() * self.as_inner().ps() * self.as_inner().r()
                + operand.nx(),
            -T::TWO * operand.nx() * self.as_inner().ps() * self.as_inner().r()
                + -T::TWO * operand.ny() * self.as_inner().r() * self.as_inner().r()
                + operand.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.ps() * self.ps()
                + operand.as_inner().s() * self.r() * self.r(),
            -T::TWO * operand.as_inner().ny() * self.ps() * self.ty()
                + T::TWO * operand.as_inner().nx() * self.ps() * self.tx()
                + T::TWO * operand.as_inner().nx() * self.r() * self.ty()
                + T::TWO * operand.as_inner().ny() * self.r() * self.tx()
                + operand.as_inner().d() * self.ps() * self.ps()
                + operand.as_inner().d() * self.r() * self.r(),
            -(operand.as_inner().nx() * self.r() * self.r())
                + T::TWO * operand.as_inner().ny() * self.ps() * self.r()
                + operand.as_inner().nx() * self.ps() * self.ps(),
            -(operand.as_inner().ny() * self.r() * self.r())
                + -T::TWO * operand.as_inner().nx() * self.ps() * self.r()
                + operand.as_inner().ny() * self.ps() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Unitized<Motor<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s(),
            -T::TWO * operand.as_inner().ny() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.as_inner().nx() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.as_inner().nx() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.as_inner().ny() * self.as_inner().r() * self.as_inner().tx()
                + operand.as_inner().d(),
            -T::TWO * operand.as_inner().nx() * self.as_inner().r() * self.as_inner().r()
                + T::TWO * operand.as_inner().ny() * self.as_inner().ps() * self.as_inner().r()
                + operand.as_inner().nx(),
            -T::TWO * operand.as_inner().nx() * self.as_inner().ps() * self.as_inner().r()
                + -T::TWO * operand.as_inner().ny() * self.as_inner().r() * self.as_inner().r()
                + operand.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Motor<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            self.ty() * operand.nx() * self.r()
                + self.r() * operand.d() * self.r()
                + self.tx() * operand.ny() * self.r()
                + self.r() * operand.nx() * self.ty()
                + self.ps() * operand.nx() * self.tx()
                + self.ps() * operand.d() * self.ps()
                - self.ps() * operand.ny() * self.ty()
                + self.tx() * operand.nx() * self.ps()
                + self.r() * operand.ny() * self.tx()
                - self.ty() * operand.ny() * self.ps(),
            self.ps() * operand.ny() * self.r()
                + self.ps() * operand.nx() * self.ps()
                + self.r() * operand.ny() * self.ps()
                - self.r() * operand.nx() * self.r(),
            -(self.r() * operand.nx() * self.ps() - self.ps() * operand.ny() * self.ps()
                + self.r() * operand.ny() * self.r()
                + self.ps() * operand.nx() * self.r()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Unitized<Motor<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -T::TWO * operand.ny() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.nx() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.nx() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.ny() * self.as_inner().r() * self.as_inner().tx()
                + operand.d(),
            -T::TWO * operand.nx() * self.as_inner().r() * self.as_inner().r()
                + T::TWO * operand.ny() * self.as_inner().ps() * self.as_inner().r()
                + operand.nx(),
            -T::TWO * operand.nx() * self.as_inner().ps() * self.as_inner().r()
                + -T::TWO * operand.ny() * self.as_inner().r() * self.as_inner().r()
                + operand.ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Motor<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -T::TWO * operand.as_inner().ny() * self.ps() * self.ty()
                + T::TWO * operand.as_inner().nx() * self.ps() * self.tx()
                + T::TWO * operand.as_inner().nx() * self.r() * self.ty()
                + T::TWO * operand.as_inner().ny() * self.r() * self.tx()
                + operand.as_inner().d() * self.ps() * self.ps()
                + operand.as_inner().d() * self.r() * self.r(),
            -(operand.as_inner().nx() * self.r() * self.r())
                + T::TWO * operand.as_inner().ny() * self.ps() * self.r()
                + operand.as_inner().nx() * self.ps() * self.ps(),
            -(operand.as_inner().ny() * self.r() * self.r())
                + -T::TWO * operand.as_inner().nx() * self.ps() * self.r()
                + operand.as_inner().ny() * self.ps() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Unitized<Motor<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -T::TWO * operand.as_inner().ny() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.as_inner().nx() * self.as_inner().ps() * self.as_inner().tx()
                + T::TWO * operand.as_inner().nx() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.as_inner().ny() * self.as_inner().r() * self.as_inner().tx()
                + operand.as_inner().d(),
            -T::TWO * operand.as_inner().nx() * self.as_inner().r() * self.as_inner().r()
                + T::TWO * operand.as_inner().ny() * self.as_inner().ps() * self.as_inner().r()
                + operand.as_inner().nx(),
            -T::TWO * operand.as_inner().nx() * self.as_inner().ps() * self.as_inner().r()
                + -T::TWO * operand.as_inner().ny() * self.as_inner().r() * self.as_inner().r()
                + operand.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            self.r() * operand.tx() * self.ps()
                - self.ps() * operand.r() * self.tx()
                - self.r() * operand.ty() * self.r()
                + self.r() * operand.r() * self.ty()
                + self.ps() * operand.ty() * self.ps()
                - self.tx() * operand.r() * self.ps()
                + self.ty() * operand.r() * self.r()
                - self.r() * operand.ps() * self.tx()
                + self.ps() * operand.tx() * self.r()
                + self.ty() * operand.ps() * self.ps()
                - self.ps() * operand.ps() * self.ty()
                + self.tx() * operand.ps() * self.r(),
            self.tx() * operand.r() * self.r() + self.tx() * operand.ps() * self.ps()
                - self.r() * operand.ty() * self.ps()
                + self.r() * operand.r() * self.tx()
                - self.r() * operand.tx() * self.r()
                - self.ps() * operand.ty() * self.r()
                + self.ty() * operand.r() * self.ps()
                - self.ty() * operand.ps() * self.r()
                + self.r() * operand.ps() * self.ty()
                + self.ps() * operand.tx() * self.ps()
                + self.ps() * operand.r() * self.ty()
                - self.ps() * operand.ps() * self.tx(),
            -(self.ps() * operand.ps() * self.r())
                + self.r() * operand.r() * self.r()
                + self.r() * operand.ps() * self.ps()
                + self.ps() * operand.r() * self.ps(),
            -(self.r() * operand.r() * self.ps())
                + self.ps() * operand.ps() * self.ps()
                + self.ps() * operand.r() * self.r()
                + self.r() * operand.ps() * self.r(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Unitized<Motor<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            -T::TWO * operand.r() * self.as_inner().ps() * self.as_inner().tx()
                + -T::TWO * operand.ty() * self.as_inner().r() * self.as_inner().r()
                + T::TWO * operand.r() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.tx() * self.as_inner().ps() * self.as_inner().r()
                + operand.ty(),
            -T::TWO * operand.tx() * self.as_inner().r() * self.as_inner().r()
                + -T::TWO * operand.ty() * self.as_inner().ps() * self.as_inner().r()
                + T::TWO * operand.r() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.r() * self.as_inner().r() * self.as_inner().tx()
                + operand.tx(),
            operand.r(),
            operand.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.r() * self.r())
                + -T::TWO * operand.as_inner().r() * self.ps() * self.tx()
                + T::TWO * operand.as_inner().r() * self.r() * self.ty()
                + T::TWO * operand.as_inner().tx() * self.ps() * self.r()
                + operand.as_inner().ty() * self.ps() * self.ps(),
            -(operand.as_inner().tx() * self.r() * self.r())
                + -T::TWO * operand.as_inner().ty() * self.ps() * self.r()
                + T::TWO * operand.as_inner().r() * self.ps() * self.ty()
                + T::TWO * operand.as_inner().r() * self.r() * self.tx()
                + operand.as_inner().tx() * self.ps() * self.ps(),
            operand.as_inner().r() * self.ps() * self.ps()
                + operand.as_inner().r() * self.r() * self.r(),
            operand.as_inner().ps() * self.ps() * self.ps()
                + operand.as_inner().ps() * self.r() * self.r(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Unitized<Motor<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -T::TWO * operand.as_inner().r() * self.as_inner().ps() * self.as_inner().tx()
                + -T::TWO * operand.as_inner().ty() * self.as_inner().r() * self.as_inner().r()
                + T::TWO * operand.as_inner().r() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.as_inner().tx() * self.as_inner().ps() * self.as_inner().r()
                + operand.as_inner().ty(),
            -T::TWO * operand.as_inner().tx() * self.as_inner().r() * self.as_inner().r()
                + -T::TWO * operand.as_inner().ty() * self.as_inner().ps() * self.as_inner().r()
                + T::TWO * operand.as_inner().r() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.as_inner().r() * self.as_inner().r() * self.as_inner().tx()
                + operand.as_inner().tx(),
            operand.as_inner().r(),
            operand.as_inner().ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Motor<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            self.ps() * operand.y() * self.r() + self.r() * operand.y() * self.ps()
                - self.ps() * operand.w() * self.tx()
                - self.tx() * operand.w() * self.ps()
                - self.r() * operand.x() * self.r()
                + self.ps() * operand.x() * self.ps()
                + self.r() * operand.w() * self.ty()
                + self.ty() * operand.w() * self.r(),
            self.ty() * operand.w() * self.ps()
                + self.ps() * operand.y() * self.ps()
                + self.r() * operand.w() * self.tx()
                + self.tx() * operand.w() * self.r()
                - self.ps() * operand.x() * self.r()
                + self.ps() * operand.w() * self.ty()
                - self.r() * operand.x() * self.ps()
                - self.r() * operand.y() * self.r(),
            self.ps() * operand.w() * self.ps() + self.r() * operand.w() * self.r(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Unitized<Motor<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -T::TWO * operand.w() * self.as_inner().ps() * self.as_inner().tx()
                + -T::TWO * operand.x() * self.as_inner().r() * self.as_inner().r()
                + T::TWO * operand.w() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.y() * self.as_inner().ps() * self.as_inner().r()
                + operand.x(),
            -T::TWO * operand.x() * self.as_inner().ps() * self.as_inner().r()
                + -T::TWO * operand.y() * self.as_inner().r() * self.as_inner().r()
                + T::TWO * operand.w() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.w() * self.as_inner().r() * self.as_inner().tx()
                + operand.y(),
            operand.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Motor<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.r() * self.r())
                + -T::TWO * operand.as_inner().w() * self.ps() * self.tx()
                + T::TWO * operand.as_inner().w() * self.r() * self.ty()
                + T::TWO * operand.as_inner().y() * self.ps() * self.r()
                + operand.as_inner().x() * self.ps() * self.ps(),
            -(operand.as_inner().y() * self.r() * self.r())
                + -T::TWO * operand.as_inner().x() * self.ps() * self.r()
                + T::TWO * operand.as_inner().w() * self.ps() * self.ty()
                + T::TWO * operand.as_inner().w() * self.r() * self.tx()
                + operand.as_inner().y() * self.ps() * self.ps(),
            operand.as_inner().w() * self.ps() * self.ps()
                + operand.as_inner().w() * self.r() * self.r(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Unitized<Motor<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -T::TWO * operand.as_inner().w() * self.as_inner().ps() * self.as_inner().tx()
                + -T::TWO * operand.as_inner().x() * self.as_inner().r() * self.as_inner().r()
                + T::TWO * operand.as_inner().w() * self.as_inner().r() * self.as_inner().ty()
                + T::TWO * operand.as_inner().y() * self.as_inner().ps() * self.as_inner().r()
                + operand.as_inner().x(),
            -T::TWO * operand.as_inner().x() * self.as_inner().ps() * self.as_inner().r()
                + -T::TWO * operand.as_inner().y() * self.as_inner().r() * self.as_inner().r()
                + T::TWO * operand.as_inner().w() * self.as_inner().ps() * self.as_inner().ty()
                + T::TWO * operand.as_inner().w() * self.as_inner().r() * self.as_inner().tx()
                + operand.as_inner().y(),
            operand.as_inner().w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Motor<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(
            self.r() * operand.s() * self.r() + self.ps() * operand.s() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Unitized<Motor<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(operand.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Motor<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Unitized<Motor<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Motor<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            self.ps() * operand.ps() * self.ps() + self.r() * operand.ps() * self.r(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Unitized<Motor<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(operand.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Motor<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            operand.as_inner().ps() * self.ps() * self.ps()
                + operand.as_inner().ps() * self.r() * self.r(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Unitized<Motor<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(operand.as_inner().ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            self.x() * operand.ny() * self.w()
                + self.w() * operand.s() * self.w()
                + self.w() * operand.nx() * self.y()
                - self.w() * operand.ny() * self.x()
                - self.y() * operand.nx() * self.w(),
            self.w() * operand.nx() * self.x()
                + self.x() * operand.nx() * self.w()
                + self.y() * operand.ny() * self.w()
                + self.w() * operand.d() * self.w()
                + self.w() * operand.ny() * self.y(),
            -(self.w() * operand.nx() * self.w()),
            -(self.w() * operand.ny() * self.w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Unitized<Point<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            operand.s(),
            T::TWO * operand.nx() * self.as_inner().w() * self.as_inner().x()
                + T::TWO * operand.ny() * self.as_inner().w() * self.as_inner().y()
                + operand.d(),
            -(operand.nx()),
            -(operand.ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.w() * self.w(),
            T::TWO * operand.as_inner().nx() * self.w() * self.x()
                + T::TWO * operand.as_inner().ny() * self.w() * self.y()
                + operand.as_inner().d() * self.w() * self.w(),
            -(operand.as_inner().nx() * self.w() * self.w()),
            -(operand.as_inner().ny() * self.w() * self.w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Unitized<Point<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s(),
            T::TWO * operand.as_inner().nx() * self.as_inner().w() * self.as_inner().x()
                + T::TWO * operand.as_inner().ny() * self.as_inner().w() * self.as_inner().y()
                + operand.as_inner().d(),
            -(operand.as_inner().nx()),
            -(operand.as_inner().ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            self.x() * operand.nx() * self.w()
                + self.w() * operand.d() * self.w()
                + self.y() * operand.ny() * self.w()
                + self.w() * operand.nx() * self.x()
                + self.w() * operand.ny() * self.y(),
            -(self.w() * operand.nx() * self.w()),
            -(self.w() * operand.ny() * self.w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            T::TWO * operand.nx() * self.as_inner().w() * self.as_inner().x()
                + T::TWO * operand.ny() * self.as_inner().w() * self.as_inner().y()
                + operand.d(),
            -(operand.nx()),
            -(operand.ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            T::TWO * operand.as_inner().nx() * self.w() * self.x()
                + T::TWO * operand.as_inner().ny() * self.w() * self.y()
                + operand.as_inner().d() * self.w() * self.w(),
            -(operand.as_inner().nx() * self.w() * self.w()),
            -(operand.as_inner().ny() * self.w() * self.w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            T::TWO * operand.as_inner().nx() * self.as_inner().w() * self.as_inner().x()
                + T::TWO * operand.as_inner().ny() * self.as_inner().w() * self.as_inner().y()
                + operand.as_inner().d(),
            -(operand.as_inner().nx()),
            -(operand.as_inner().ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            self.w() * operand.r() * self.x() + self.x() * operand.r() * self.w()
                - self.w() * operand.ps() * self.y()
                + self.y() * operand.ps() * self.w()
                - self.w() * operand.ty() * self.w(),
            -(self.w() * operand.tx() * self.w())
                + self.w() * operand.r() * self.y()
                + self.w() * operand.ps() * self.x()
                - self.x() * operand.ps() * self.w()
                + self.y() * operand.r() * self.w(),
            self.w() * operand.r() * self.w(),
            self.w() * operand.ps() * self.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Unitized<Point<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.ty()) + T::TWO * operand.r() * self.as_inner().w() * self.as_inner().x(),
            -(operand.tx()) + T::TWO * operand.r() * self.as_inner().w() * self.as_inner().y(),
            operand.r(),
            operand.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty() * self.w() * self.w())
                + T::TWO * operand.as_inner().r() * self.w() * self.x(),
            -(operand.as_inner().tx() * self.w() * self.w())
                + T::TWO * operand.as_inner().r() * self.w() * self.y(),
            operand.as_inner().r() * self.w() * self.w(),
            operand.as_inner().ps() * self.w() * self.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Unitized<Point<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            -(operand.as_inner().ty())
                + T::TWO * operand.as_inner().r() * self.as_inner().w() * self.as_inner().x(),
            -(operand.as_inner().tx())
                + T::TWO * operand.as_inner().r() * self.as_inner().w() * self.as_inner().y(),
            operand.as_inner().r(),
            operand.as_inner().ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            self.w() * operand.w() * self.x() - self.w() * operand.x() * self.w()
                + self.x() * operand.w() * self.w(),
            self.y() * operand.w() * self.w() - self.w() * operand.y() * self.w()
                + self.w() * operand.w() * self.y(),
            self.w() * operand.w() * self.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(operand.x()) + T::TWO * operand.w() * self.as_inner().w() * self.as_inner().x(),
            -(operand.y()) + T::TWO * operand.w() * self.as_inner().w() * self.as_inner().y(),
            operand.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x() * self.w() * self.w())
                + T::TWO * operand.as_inner().w() * self.w() * self.x(),
            -(operand.as_inner().y() * self.w() * self.w())
                + T::TWO * operand.as_inner().w() * self.w() * self.y(),
            operand.as_inner().w() * self.w() * self.w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(operand.as_inner().x())
                + T::TWO * operand.as_inner().w() * self.as_inner().w() * self.as_inner().x(),
            -(operand.as_inner().y())
                + T::TWO * operand.as_inner().w() * self.as_inner().w() * self.as_inner().y(),
            operand.as_inner().w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(self.w() * operand.s() * self.w())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(operand.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(self.w() * operand.ps() * self.w())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(operand.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(operand.as_inner().ps() * self.w() * self.w())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(operand.as_inner().ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Scalar<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(T::zero(), T::zero(), T::zero(), -(T::zero()))
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Unitized<Scalar<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Scalar<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Unitized<Scalar<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), -(T::zero()))
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Scalar<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Unitized<Scalar<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Scalar<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Unitized<Scalar<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(T::zero(), T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Trivector<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(
            self.ps() * operand.s() * self.ps(),
            self.ps() * operand.d() * self.ps(),
            self.ps() * operand.nx() * self.ps(),
            -(-(self.ps() * operand.ny() * self.ps())),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Flector<T>> for Unitized<Trivector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Flector<T>) -> Flector<T> {
        Flector::new_unchecked(operand.s(), operand.d(), operand.nx(), operand.ny())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Trivector<T> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s() * self.ps() * self.ps(),
            operand.as_inner().d() * self.ps() * self.ps(),
            operand.as_inner().nx() * self.ps() * self.ps(),
            operand.as_inner().ny() * self.ps() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Flector<T>>> for Unitized<Trivector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        Flector::new_unchecked(
            operand.as_inner().s(),
            operand.as_inner().d(),
            operand.as_inner().nx(),
            operand.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            self.ps() * operand.d() * self.ps(),
            self.ps() * operand.nx() * self.ps(),
            -(-(self.ps() * operand.ny() * self.ps())),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Line<T>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Line<T>) -> Line<T> {
        Line::new_unchecked(operand.d(), operand.nx(), operand.ny())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            operand.as_inner().d() * self.ps() * self.ps(),
            operand.as_inner().nx() * self.ps() * self.ps(),
            operand.as_inner().ny() * self.ps() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Line<T>>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            operand.as_inner().d(),
            operand.as_inner().nx(),
            operand.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Trivector<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(
            self.ps() * operand.ty() * self.ps(),
            self.ps() * operand.tx() * self.ps(),
            self.ps() * operand.r() * self.ps(),
            self.ps() * operand.ps() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Motor<T>> for Unitized<Trivector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Motor<T>) -> Motor<T> {
        Motor::new_unchecked(operand.ty(), operand.tx(), operand.r(), operand.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Trivector<T> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            operand.as_inner().ty() * self.ps() * self.ps(),
            operand.as_inner().tx() * self.ps() * self.ps(),
            operand.as_inner().r() * self.ps() * self.ps(),
            operand.as_inner().ps() * self.ps() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Motor<T>>> for Unitized<Trivector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        Motor::new_unchecked(
            operand.as_inner().ty(),
            operand.as_inner().tx(),
            operand.as_inner().r(),
            operand.as_inner().ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            self.ps() * operand.x() * self.ps(),
            self.ps() * operand.y() * self.ps(),
            self.ps() * operand.w() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Point<T>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Point<T>) -> Point<T> {
        Point::new_unchecked(operand.x(), operand.y(), operand.w())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            operand.as_inner().x() * self.ps() * self.ps(),
            operand.as_inner().y() * self.ps() * self.ps(),
            operand.as_inner().w() * self.ps() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Point<T>>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            operand.as_inner().x(),
            operand.as_inner().y(),
            operand.as_inner().w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(self.ps() * operand.s() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Scalar<T>> for Unitized<Trivector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(operand.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Scalar<T>>> for Unitized<Trivector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(self.ps() * operand.ps() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Trivector<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(operand.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(operand.as_inner().ps() * self.ps() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antisandwich<Unitized<Trivector<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antisandwich(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(operand.as_inner().ps())
    }
}
impl<T: Float> Transform<Flector<T>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Flector<T>> for Unitized<Flector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Unitized<Flector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Flector<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Unitized<Flector<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Flector<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Unitized<Flector<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Unitized<Flector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Unitized<Flector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Flector<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Unitized<Flector<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Flector<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Unitized<Flector<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Flector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Unitized<Flector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Flector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Unitized<Flector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Flector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Unitized<Flector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Flector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Unitized<Flector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Flector<T>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Flector<T>> for Unitized<Line<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Unitized<Line<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Unitized<Line<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Unitized<Line<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Flector<T>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Flector<T>> for Unitized<Motor<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Unitized<Motor<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Motor<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Unitized<Motor<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Motor<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Unitized<Motor<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Unitized<Motor<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Unitized<Motor<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Motor<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Unitized<Motor<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Motor<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Unitized<Motor<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Motor<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Unitized<Motor<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Motor<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Unitized<Motor<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Motor<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Unitized<Motor<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Motor<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Unitized<Motor<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Flector<T>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Flector<T>> for Unitized<Point<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Unitized<Point<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Unitized<Point<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Unitized<Point<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Flector<T>> for Scalar<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Flector<T>> for Unitized<Scalar<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Scalar<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Unitized<Scalar<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Scalar<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Unitized<Scalar<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Scalar<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Unitized<Scalar<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Flector<T>> for Trivector<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Flector<T>> for Unitized<Trivector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Flector<T>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Trivector<T> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Flector<T>>> for Unitized<Trivector<T>> {
    type Output = Flector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Flector<T>>) -> Flector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Line<T>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Line<T>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Line<T>>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Line<T>>) -> Line<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Trivector<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Motor<T>> for Unitized<Trivector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Motor<T>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Trivector<T> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Motor<T>>> for Unitized<Trivector<T>> {
    type Output = Motor<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Motor<T>>) -> Motor<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Point<T>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Point<T>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Point<T>>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Point<T>>) -> Point<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Scalar<T>> for Unitized<Trivector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Scalar<T>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Scalar<T>>> for Unitized<Trivector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Scalar<T>>) -> Scalar<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Trivector<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Trivector<T>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
impl<T: Float> Transform<Unitized<Trivector<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn transform(&self, operand: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antisandwich(operand)
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Flector<T>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (self.d() * operand.s() * self.d() - self.d() * operand.d() * self.s()
                + self.s() * operand.d() * self.d()
                + self.s() * operand.s() * self.s())
                * inv_norm_sq,
            (self.d() * operand.d() * self.d() - self.s() * operand.s() * self.d()
                + self.s() * operand.d() * self.s()
                + self.d() * operand.s() * self.s())
                * inv_norm_sq,
            (-(self.s() * operand.d() * self.ny()) + self.ny() * operand.s() * self.d()
                - self.s() * operand.s() * self.nx()
                + self.d() * operand.d() * self.nx()
                + self.s() * operand.nx() * self.s()
                - self.d() * operand.s() * self.ny()
                + self.nx() * operand.s() * self.s()
                - self.ny() * operand.d() * self.s()
                + self.s() * operand.ny() * self.d()
                - self.d() * operand.nx() * self.d()
                + self.d() * operand.ny() * self.s()
                + self.nx() * operand.d() * self.d())
                * inv_norm_sq,
            (-(-(self.s() * operand.ny() * self.s()) - self.ny() * operand.s() * self.s()
                + self.d() * operand.ny() * self.d()
                - self.ny() * operand.d() * self.d()
                - self.d() * operand.s() * self.nx()
                - self.s() * operand.d() * self.nx()
                - self.nx() * operand.d() * self.s()
                + self.s() * operand.s() * self.ny()
                + self.s() * operand.nx() * self.d()
                - self.d() * operand.d() * self.ny()
                + self.nx() * operand.s() * self.d()
                + self.d() * operand.nx() * self.s()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Line<T>> for Flector<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (self.d() * operand.d() * self.d() + self.s() * operand.d() * self.s()) * inv_norm_sq,
            (self.d() * operand.d() * self.nx()
                - self.s() * operand.d() * self.ny()
                - self.d() * operand.nx() * self.d()
                - self.ny() * operand.d() * self.s()
                + self.s() * operand.nx() * self.s()
                + self.s() * operand.ny() * self.d()
                + self.d() * operand.ny() * self.s()
                + self.nx() * operand.d() * self.d())
                * inv_norm_sq,
            (-(self.s() * operand.nx() * self.d()
                - self.s() * operand.ny() * self.s()
                - self.d() * operand.d() * self.ny()
                - self.s() * operand.d() * self.nx()
                + self.d() * operand.nx() * self.s()
                + self.d() * operand.ny() * self.d()
                - self.ny() * operand.d() * self.d()
                - self.nx() * operand.d() * self.s()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Motor<T>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (self.s() * operand.ty() * self.s() + self.s() * operand.tx() * self.d()
                - self.d() * operand.ty() * self.d()
                + self.d() * operand.tx() * self.s())
                * inv_norm_sq,
            (-(self.d() * operand.tx() * self.d())
                - self.d() * operand.ty() * self.s()
                - self.s() * operand.ty() * self.d()
                + self.s() * operand.tx() * self.s())
                * inv_norm_sq,
            (self.s() * operand.ps() * self.d()
                + self.s() * operand.ty() * self.ny()
                + self.nx() * operand.ty() * self.d()
                - self.nx() * operand.tx() * self.s()
                + self.d() * operand.r() * self.d()
                + self.d() * operand.tx() * self.ny()
                - self.s() * operand.tx() * self.nx()
                + self.d() * operand.ty() * self.nx()
                - self.d() * operand.ps() * self.s()
                + self.s() * operand.r() * self.s()
                + self.ny() * operand.ty() * self.s()
                + self.ny() * operand.tx() * self.d())
                * inv_norm_sq,
            (self.nx() * operand.ty() * self.s()
                + self.d() * operand.ty() * self.ny()
                + self.d() * operand.ps() * self.d()
                + self.nx() * operand.tx() * self.d()
                + self.ny() * operand.tx() * self.s()
                + self.s() * operand.ps() * self.s()
                - self.s() * operand.tx() * self.ny()
                - self.d() * operand.tx() * self.nx()
                - self.s() * operand.ty() * self.nx()
                - self.s() * operand.r() * self.d()
                + self.d() * operand.r() * self.s()
                - self.ny() * operand.ty() * self.d())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Point<T>> for Flector<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (self.s() * operand.x() * self.s()
                + self.s() * operand.y() * self.d()
                + self.d() * operand.y() * self.s()
                - self.d() * operand.x() * self.d())
                * inv_norm_sq,
            (self.s() * operand.y() * self.s()
                - self.d() * operand.y() * self.d()
                - self.s() * operand.x() * self.d()
                - self.d() * operand.x() * self.s())
                * inv_norm_sq,
            (self.nx() * operand.x() * self.d()
                + self.ny() * operand.y() * self.d()
                + self.d() * operand.x() * self.nx()
                + self.s() * operand.x() * self.ny()
                - self.s() * operand.y() * self.nx()
                + self.d() * operand.y() * self.ny()
                + self.s() * operand.w() * self.s()
                + self.d() * operand.w() * self.d()
                - self.nx() * operand.y() * self.s()
                + self.ny() * operand.x() * self.s())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Scalar<T>> for Flector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked(
            (self.d() * operand.s() * self.d() + self.s() * operand.s() * self.s()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Trivector<T>> for Flector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked(
            (self.d() * operand.ps() * self.d() + self.s() * operand.ps() * self.s()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Flector<T>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (self.d() * operand.s() * self.d()) * inv_norm_sq,
            (self.d() * operand.d() * self.d()) * inv_norm_sq,
            (self.ny() * operand.s() * self.d() + self.nx() * operand.d() * self.d()
                - self.d() * operand.nx() * self.d()
                + self.d() * operand.d() * self.nx()
                - self.d() * operand.s() * self.ny())
                * inv_norm_sq,
            (-(self.nx() * operand.s() * self.d() + self.d() * operand.ny() * self.d()
                - self.d() * operand.s() * self.nx()
                - self.d() * operand.d() * self.ny()
                - self.ny() * operand.d() * self.d()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Line<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (self.d() * operand.d() * self.d()) * inv_norm_sq,
            (self.nx() * operand.d() * self.d() - self.d() * operand.nx() * self.d()
                + self.d() * operand.d() * self.nx())
                * inv_norm_sq,
            (-(-(self.ny() * operand.d() * self.d()) - self.d() * operand.d() * self.ny()
                + self.d() * operand.ny() * self.d()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Motor<T>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (-(self.d() * operand.ty() * self.d())) * inv_norm_sq,
            (-(self.d() * operand.tx() * self.d())) * inv_norm_sq,
            (self.d() * operand.r() * self.d()
                + self.nx() * operand.ty() * self.d()
                + self.ny() * operand.tx() * self.d()
                + self.d() * operand.ty() * self.nx()
                + self.d() * operand.tx() * self.ny())
                * inv_norm_sq,
            (-(self.d() * operand.tx() * self.nx())
                + self.d() * operand.ty() * self.ny()
                + self.nx() * operand.tx() * self.d()
                + self.d() * operand.ps() * self.d()
                - self.ny() * operand.ty() * self.d())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Point<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (-(self.d() * operand.x() * self.d())) * inv_norm_sq,
            (-(self.d() * operand.y() * self.d())) * inv_norm_sq,
            (self.d() * operand.x() * self.nx()
                + self.nx() * operand.x() * self.d()
                + self.d() * operand.w() * self.d()
                + self.ny() * operand.y() * self.d()
                + self.d() * operand.y() * self.ny())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Scalar<T>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked(
            (self.d() * operand.s() * self.d()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Trivector<T>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked(
            (self.d() * operand.ps() * self.d()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Flector<T>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (self.ty() * operand.s() * self.ty()
                + self.tx() * operand.s() * self.tx()
                + self.ty() * operand.d() * self.tx()
                - self.tx() * operand.d() * self.ty())
                * inv_norm_sq,
            (-(self.tx() * operand.d() * self.tx()) - self.tx() * operand.s() * self.ty()
                + self.ty() * operand.s() * self.tx()
                - self.ty() * operand.d() * self.ty())
                * inv_norm_sq,
            (-(self.r() * operand.s() * self.tx())
                + self.r() * operand.d() * self.ty()
                + self.tx() * operand.ny() * self.ty()
                + self.ps() * operand.s() * self.ty()
                + self.tx() * operand.d() * self.ps()
                + self.ps() * operand.d() * self.tx()
                + self.ty() * operand.d() * self.r()
                + self.ty() * operand.nx() * self.ty()
                - self.ty() * operand.s() * self.ps()
                + self.ty() * operand.ny() * self.tx()
                - self.tx() * operand.nx() * self.tx()
                + self.tx() * operand.s() * self.r())
                * inv_norm_sq,
            (-(-(self.tx() * operand.d() * self.r())
                + self.ty() * operand.s() * self.r()
                + self.ty() * operand.d() * self.ps()
                - self.tx() * operand.nx() * self.ty()
                - self.r() * operand.s() * self.ty()
                + self.ty() * operand.ny() * self.ty()
                + self.ps() * operand.d() * self.ty()
                - self.tx() * operand.ny() * self.tx()
                - self.ty() * operand.nx() * self.tx()
                + self.tx() * operand.s() * self.ps()
                - self.r() * operand.d() * self.tx()
                - self.ps() * operand.s() * self.tx()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Line<T>> for Motor<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (-(self.ty() * operand.d() * self.ty()) - self.tx() * operand.d() * self.tx())
                * inv_norm_sq,
            (self.ty() * operand.nx() * self.ty() + self.tx() * operand.d() * self.ps()
                - self.tx() * operand.nx() * self.tx()
                + self.ps() * operand.d() * self.tx()
                + self.r() * operand.d() * self.ty()
                + self.tx() * operand.ny() * self.ty()
                + self.ty() * operand.d() * self.r()
                + self.ty() * operand.ny() * self.tx())
                * inv_norm_sq,
            (-(self.ps() * operand.d() * self.ty()
                - self.r() * operand.d() * self.tx()
                - self.ty() * operand.nx() * self.tx()
                - self.tx() * operand.d() * self.r()
                + self.ty() * operand.d() * self.ps()
                - self.tx() * operand.nx() * self.ty()
                + self.ty() * operand.ny() * self.ty()
                - self.tx() * operand.ny() * self.tx()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Motor<T>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (self.ty() * operand.ty() * self.ty() + self.ty() * operand.tx() * self.tx()
                - self.tx() * operand.ty() * self.tx()
                + self.tx() * operand.tx() * self.ty())
                * inv_norm_sq,
            (-(self.ty() * operand.tx() * self.ty())
                + self.tx() * operand.tx() * self.tx()
                + self.tx() * operand.ty() * self.ty()
                + self.ty() * operand.ty() * self.tx())
                * inv_norm_sq,
            (self.ps() * operand.tx() * self.ty()
                + self.ty() * operand.tx() * self.ps()
                + self.r() * operand.tx() * self.tx()
                - self.ps() * operand.ty() * self.tx()
                - self.ty() * operand.r() * self.ty()
                + self.r() * operand.ty() * self.ty()
                + self.ty() * operand.ty() * self.r()
                - self.ty() * operand.ps() * self.tx()
                - self.tx() * operand.ty() * self.ps()
                + self.tx() * operand.tx() * self.r()
                - self.tx() * operand.r() * self.tx()
                + self.tx() * operand.ps() * self.ty())
                * inv_norm_sq,
            (self.ps() * operand.tx() * self.tx()
                + self.ps() * operand.ty() * self.ty()
                + self.tx() * operand.ps() * self.tx()
                - self.tx() * operand.ty() * self.r()
                + self.ty() * operand.tx() * self.r()
                - self.r() * operand.tx() * self.ty()
                + self.ty() * operand.ps() * self.ty()
                + self.r() * operand.ty() * self.tx()
                + self.tx() * operand.r() * self.ty()
                - self.ty() * operand.ty() * self.ps()
                - self.tx() * operand.tx() * self.ps()
                - self.ty() * operand.r() * self.tx())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Point<T>> for Motor<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (self.ty() * operand.x() * self.ty() + self.ty() * operand.y() * self.tx()
                - self.tx() * operand.x() * self.tx()
                + self.tx() * operand.y() * self.ty())
                * inv_norm_sq,
            (-(self.ty() * operand.y() * self.ty())
                + self.tx() * operand.x() * self.ty()
                + self.ty() * operand.x() * self.tx()
                + self.tx() * operand.y() * self.tx())
                * inv_norm_sq,
            (self.tx() * operand.y() * self.r() - self.tx() * operand.x() * self.ps()
                + self.ty() * operand.y() * self.ps()
                + self.r() * operand.x() * self.ty()
                + self.ps() * operand.y() * self.ty()
                - self.tx() * operand.w() * self.tx()
                + self.r() * operand.y() * self.tx()
                - self.ty() * operand.w() * self.ty()
                - self.ps() * operand.x() * self.tx()
                + self.ty() * operand.x() * self.r())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Scalar<T>> for Motor<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked(
            (self.ty() * operand.s() * self.ty() + self.tx() * operand.s() * self.tx())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Trivector<T>> for Motor<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked(
            (self.ty() * operand.ps() * self.ty() + self.tx() * operand.ps() * self.tx())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Flector<T>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (self.y() * operand.s() * self.y() - self.y() * operand.d() * self.x()
                + self.x() * operand.s() * self.x()
                + self.x() * operand.d() * self.y())
                * inv_norm_sq,
            (-(self.y() * operand.d() * self.y()) + self.x() * operand.s() * self.y()
                - self.y() * operand.s() * self.x()
                - self.x() * operand.d() * self.x())
                * inv_norm_sq,
            (self.x() * operand.d() * self.w()
                + self.x() * operand.ny() * self.y()
                + self.y() * operand.ny() * self.x()
                + self.w() * operand.d() * self.x()
                + self.y() * operand.s() * self.w()
                + self.x() * operand.nx() * self.x()
                - self.y() * operand.nx() * self.y()
                - self.w() * operand.s() * self.y())
                * inv_norm_sq,
            (-(-(self.x() * operand.nx() * self.y()) - self.y() * operand.ny() * self.y()
                + self.x() * operand.s() * self.w()
                - self.y() * operand.nx() * self.x()
                - self.y() * operand.d() * self.w()
                + self.x() * operand.ny() * self.x()
                - self.w() * operand.s() * self.x()
                - self.w() * operand.d() * self.y()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Line<T>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (-(self.x() * operand.d() * self.x()) - self.y() * operand.d() * self.y())
                * inv_norm_sq,
            (self.x() * operand.ny() * self.y()
                + self.y() * operand.ny() * self.x()
                + self.x() * operand.nx() * self.x()
                + self.x() * operand.d() * self.w()
                + self.w() * operand.d() * self.x()
                - self.y() * operand.nx() * self.y())
                * inv_norm_sq,
            (-(self.x() * operand.ny() * self.x()
                - self.y() * operand.d() * self.w()
                - self.w() * operand.d() * self.y()
                - self.y() * operand.ny() * self.y()
                - self.y() * operand.nx() * self.x()
                - self.x() * operand.nx() * self.y()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Motor<T>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (self.x() * operand.ty() * self.x() - self.y() * operand.ty() * self.y()
                + self.y() * operand.tx() * self.x()
                + self.x() * operand.tx() * self.y())
                * inv_norm_sq,
            (self.x() * operand.ty() * self.y() + self.y() * operand.tx() * self.y()
                - self.x() * operand.tx() * self.x()
                + self.y() * operand.ty() * self.x())
                * inv_norm_sq,
            (-(self.x() * operand.ps() * self.y())
                + self.y() * operand.tx() * self.w()
                + self.w() * operand.ty() * self.x()
                + self.w() * operand.tx() * self.y()
                + self.x() * operand.ty() * self.w()
                - self.x() * operand.r() * self.x()
                - self.y() * operand.r() * self.y()
                + self.y() * operand.ps() * self.x())
                * inv_norm_sq,
            (self.y() * operand.r() * self.x() + self.y() * operand.ps() * self.y()
                - self.w() * operand.tx() * self.x()
                - self.y() * operand.ty() * self.w()
                + self.x() * operand.ps() * self.x()
                + self.w() * operand.ty() * self.y()
                + self.x() * operand.tx() * self.w()
                - self.x() * operand.r() * self.y())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Point<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (self.x() * operand.x() * self.x() - self.y() * operand.x() * self.y()
                + self.y() * operand.y() * self.x()
                + self.x() * operand.y() * self.y())
                * inv_norm_sq,
            (self.y() * operand.y() * self.y()
                + self.y() * operand.x() * self.x()
                + self.x() * operand.x() * self.y()
                - self.x() * operand.y() * self.x())
                * inv_norm_sq,
            (self.w() * operand.y() * self.y() + self.y() * operand.y() * self.w()
                - self.x() * operand.w() * self.x()
                + self.x() * operand.x() * self.w()
                - self.y() * operand.w() * self.y()
                + self.w() * operand.x() * self.x())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Scalar<T>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked(
            (self.x() * operand.s() * self.x() + self.y() * operand.s() * self.y()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Trivector<T>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked(
            (self.x() * operand.ps() * self.x() + self.y() * operand.ps() * self.y()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Flector<T>> for Scalar<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (self.s() * operand.s() * self.s()) * inv_norm_sq,
            (self.s() * operand.d() * self.s()) * inv_norm_sq,
            (self.s() * operand.nx() * self.s()) * inv_norm_sq,
            (-(-(self.s() * operand.ny() * self.s()))) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Line<T>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (self.s() * operand.d() * self.s()) * inv_norm_sq,
            (self.s() * operand.nx() * self.s()) * inv_norm_sq,
            (-(-(self.s() * operand.ny() * self.s()))) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Motor<T>> for Scalar<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (self.s() * operand.ty() * self.s()) * inv_norm_sq,
            (self.s() * operand.tx() * self.s()) * inv_norm_sq,
            (self.s() * operand.r() * self.s()) * inv_norm_sq,
            (self.s() * operand.ps() * self.s()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Point<T>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (self.s() * operand.x() * self.s()) * inv_norm_sq,
            (self.s() * operand.y() * self.s()) * inv_norm_sq,
            (self.s() * operand.w() * self.s()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Scalar<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked(
            (self.s() * operand.s() * self.s()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Trivector<T>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked(
            (self.s() * operand.ps() * self.s()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Flector<T>> for Trivector<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (-(T::zero())) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Line<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (-(T::zero())) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Motor<T>> for Trivector<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Point<T>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Scalar<T>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked((T::zero()) * inv_norm_sq))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseSandwich<Trivector<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_sandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked((T::zero()) * inv_norm_sq))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Flector<T>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (self.ny() * operand.s() * self.ny()
                + self.nx() * operand.ny() * self.d()
                + self.s() * operand.nx() * self.nx()
                - self.d() * operand.ny() * self.nx()
                + self.ny() * operand.d() * self.nx()
                - self.ny() * operand.nx() * self.d()
                - self.nx() * operand.d() * self.ny()
                - self.nx() * operand.nx() * self.s()
                + self.d() * operand.nx() * self.ny()
                + self.s() * operand.ny() * self.ny()
                - self.ny() * operand.ny() * self.s()
                + self.nx() * operand.s() * self.nx())
                * inv_norm_sq,
            (-(self.s() * operand.nx() * self.ny())
                + self.ny() * operand.ny() * self.d()
                + self.nx() * operand.nx() * self.d()
                + self.d() * operand.nx() * self.nx()
                - self.nx() * operand.s() * self.ny()
                + self.nx() * operand.ny() * self.s()
                + self.ny() * operand.s() * self.nx()
                + self.s() * operand.ny() * self.nx()
                - self.ny() * operand.d() * self.ny()
                - self.ny() * operand.nx() * self.s()
                - self.nx() * operand.d() * self.nx()
                + self.d() * operand.ny() * self.ny())
                * inv_norm_sq,
            (self.nx() * operand.nx() * self.nx() + self.nx() * operand.ny() * self.ny()
                - self.ny() * operand.nx() * self.ny()
                + self.ny() * operand.ny() * self.nx())
                * inv_norm_sq,
            (-(-(self.ny() * operand.nx() * self.nx()) - self.ny() * operand.ny() * self.ny()
                + self.nx() * operand.ny() * self.nx()
                - self.nx() * operand.nx() * self.ny()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Line<T>> for Flector<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (self.s() * operand.ny() * self.nx()
                - self.s() * operand.nx() * self.ny()
                - self.ny() * operand.nx() * self.s()
                - self.nx() * operand.d() * self.nx()
                + self.ny() * operand.ny() * self.d()
                + self.d() * operand.nx() * self.nx()
                + self.nx() * operand.ny() * self.s()
                + self.nx() * operand.nx() * self.d()
                - self.ny() * operand.d() * self.ny()
                + self.d() * operand.ny() * self.ny())
                * inv_norm_sq,
            (self.nx() * operand.ny() * self.ny() - self.ny() * operand.nx() * self.ny()
                + self.nx() * operand.nx() * self.nx()
                + self.ny() * operand.ny() * self.nx())
                * inv_norm_sq,
            (-(-(self.nx() * operand.nx() * self.ny()) + self.nx() * operand.ny() * self.nx()
                - self.ny() * operand.nx() * self.nx()
                - self.ny() * operand.ny() * self.ny()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Motor<T>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (self.ny() * operand.tx() * self.nx()
                + self.s() * operand.ps() * self.nx()
                + self.s() * operand.r() * self.ny()
                - self.d() * operand.ps() * self.ny()
                - self.ny() * operand.ty() * self.ny()
                + self.ny() * operand.ps() * self.d()
                + self.nx() * operand.tx() * self.ny()
                - self.nx() * operand.ps() * self.s()
                + self.nx() * operand.r() * self.d()
                + self.d() * operand.r() * self.nx()
                + self.nx() * operand.ty() * self.nx()
                + self.ny() * operand.r() * self.s())
                * inv_norm_sq,
            (self.s() * operand.ps() * self.ny()
                + self.ny() * operand.ty() * self.nx()
                + self.nx() * operand.ty() * self.ny()
                + self.ny() * operand.tx() * self.ny()
                - self.ny() * operand.ps() * self.s()
                + self.d() * operand.ps() * self.nx()
                - self.nx() * operand.ps() * self.d()
                + self.ny() * operand.r() * self.d()
                + self.d() * operand.r() * self.ny()
                - self.nx() * operand.tx() * self.nx()
                - self.s() * operand.r() * self.nx()
                - self.nx() * operand.r() * self.s())
                * inv_norm_sq,
            (-(self.nx() * operand.r() * self.nx()) + self.nx() * operand.ps() * self.ny()
                - self.ny() * operand.r() * self.ny()
                - self.ny() * operand.ps() * self.nx())
                * inv_norm_sq,
            (self.nx() * operand.r() * self.ny() + self.ny() * operand.ps() * self.ny()
                - self.ny() * operand.r() * self.nx()
                + self.nx() * operand.ps() * self.nx())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Point<T>> for Flector<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (self.ny() * operand.w() * self.s()
                + self.s() * operand.w() * self.ny()
                + self.nx() * operand.y() * self.ny()
                + self.ny() * operand.y() * self.nx()
                + self.nx() * operand.x() * self.nx()
                - self.ny() * operand.x() * self.ny()
                + self.d() * operand.w() * self.nx()
                + self.nx() * operand.w() * self.d())
                * inv_norm_sq,
            (-(self.s() * operand.w() * self.nx())
                + self.ny() * operand.y() * self.ny()
                + self.ny() * operand.w() * self.d()
                + self.nx() * operand.x() * self.ny()
                - self.nx() * operand.w() * self.s()
                - self.nx() * operand.y() * self.nx()
                + self.d() * operand.w() * self.ny()
                + self.ny() * operand.x() * self.nx())
                * inv_norm_sq,
            (-(self.ny() * operand.w() * self.ny()) - self.nx() * operand.w() * self.nx())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Scalar<T>> for Flector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked(
            (self.nx() * operand.s() * self.nx() + self.ny() * operand.s() * self.ny())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Trivector<T>> for Flector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked(
            (self.nx() * operand.ps() * self.nx() + self.ny() * operand.ps() * self.ny())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Flector<T>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (self.ny() * operand.s() * self.ny()
                + self.d() * operand.nx() * self.ny()
                + self.nx() * operand.ny() * self.d()
                - self.d() * operand.ny() * self.nx()
                + self.ny() * operand.d() * self.nx()
                - self.ny() * operand.nx() * self.d()
                - self.nx() * operand.d() * self.ny()
                + self.nx() * operand.s() * self.nx())
                * inv_norm_sq,
            (self.d() * operand.ny() * self.ny() + self.d() * operand.nx() * self.nx()
                - self.nx() * operand.d() * self.nx()
                + self.ny() * operand.ny() * self.d()
                + self.nx() * operand.nx() * self.d()
                - self.nx() * operand.s() * self.ny()
                + self.ny() * operand.s() * self.nx()
                - self.ny() * operand.d() * self.ny())
                * inv_norm_sq,
            (self.nx() * operand.nx() * self.nx()
                + self.ny() * operand.ny() * self.nx()
                + self.nx() * operand.ny() * self.ny()
                - self.ny() * operand.nx() * self.ny())
                * inv_norm_sq,
            (-(self.nx() * operand.ny() * self.nx()
                - self.ny() * operand.ny() * self.ny()
                - self.nx() * operand.nx() * self.ny()
                - self.ny() * operand.nx() * self.nx()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Line<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (self.d() * operand.nx() * self.nx()
                + self.ny() * operand.ny() * self.d()
                + self.nx() * operand.nx() * self.d()
                - self.ny() * operand.d() * self.ny()
                - self.nx() * operand.d() * self.nx()
                + self.d() * operand.ny() * self.ny())
                * inv_norm_sq,
            (self.nx() * operand.nx() * self.nx()
                + self.ny() * operand.ny() * self.nx()
                + self.nx() * operand.ny() * self.ny()
                - self.ny() * operand.nx() * self.ny())
                * inv_norm_sq,
            (-(-(self.ny() * operand.ny() * self.ny())
                - self.nx() * operand.nx() * self.ny()
                - self.ny() * operand.nx() * self.nx()
                + self.nx() * operand.ny() * self.nx()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Motor<T>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (self.nx() * operand.ty() * self.nx()
                + self.ny() * operand.tx() * self.nx()
                + self.nx() * operand.r() * self.d()
                + self.d() * operand.r() * self.nx()
                - self.ny() * operand.ty() * self.ny()
                + self.nx() * operand.tx() * self.ny()
                - self.d() * operand.ps() * self.ny()
                + self.ny() * operand.ps() * self.d())
                * inv_norm_sq,
            (-(self.nx() * operand.ps() * self.d()) - self.nx() * operand.tx() * self.nx()
                + self.nx() * operand.ty() * self.ny()
                + self.ny() * operand.r() * self.d()
                + self.ny() * operand.ty() * self.nx()
                + self.ny() * operand.tx() * self.ny()
                + self.d() * operand.r() * self.ny()
                + self.d() * operand.ps() * self.nx())
                * inv_norm_sq,
            (-(self.nx() * operand.r() * self.nx()) + self.nx() * operand.ps() * self.ny()
                - self.ny() * operand.r() * self.ny()
                - self.ny() * operand.ps() * self.nx())
                * inv_norm_sq,
            (self.nx() * operand.r() * self.ny() + self.nx() * operand.ps() * self.nx()
                - self.ny() * operand.r() * self.nx()
                + self.ny() * operand.ps() * self.ny())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Point<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (self.nx() * operand.y() * self.ny()
                + self.ny() * operand.y() * self.nx()
                + self.nx() * operand.w() * self.d()
                - self.ny() * operand.x() * self.ny()
                + self.d() * operand.w() * self.nx()
                + self.nx() * operand.x() * self.nx())
                * inv_norm_sq,
            (self.d() * operand.w() * self.ny()
                + self.ny() * operand.w() * self.d()
                + self.ny() * operand.x() * self.nx()
                + self.nx() * operand.x() * self.ny()
                - self.nx() * operand.y() * self.nx()
                + self.ny() * operand.y() * self.ny())
                * inv_norm_sq,
            (-(self.nx() * operand.w() * self.nx()) - self.ny() * operand.w() * self.ny())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Scalar<T>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked(
            (self.nx() * operand.s() * self.nx() + self.ny() * operand.s() * self.ny())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Trivector<T>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked(
            (self.nx() * operand.ps() * self.nx() + self.ny() * operand.ps() * self.ny())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Flector<T>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (-(self.ps() * operand.nx() * self.ty()) - self.ps() * operand.ny() * self.tx()
                + self.r() * operand.nx() * self.tx()
                + self.ty() * operand.nx() * self.ps()
                + self.tx() * operand.ny() * self.ps()
                - self.ps() * operand.d() * self.r()
                + self.r() * operand.d() * self.ps()
                - self.r() * operand.ny() * self.ty()
                + self.ty() * operand.ny() * self.r()
                + self.r() * operand.s() * self.r()
                - self.tx() * operand.nx() * self.r()
                + self.ps() * operand.s() * self.ps())
                * inv_norm_sq,
            (self.ps() * operand.nx() * self.tx() - self.ps() * operand.ny() * self.ty()
                + self.tx() * operand.nx() * self.ps()
                + self.r() * operand.nx() * self.ty()
                + self.r() * operand.d() * self.r()
                - self.r() * operand.s() * self.ps()
                + self.r() * operand.ny() * self.tx()
                + self.ty() * operand.nx() * self.r()
                + self.ps() * operand.s() * self.r()
                + self.ps() * operand.d() * self.ps()
                + self.tx() * operand.ny() * self.r()
                - self.ty() * operand.ny() * self.ps())
                * inv_norm_sq,
            (self.r() * operand.ny() * self.ps() + self.ps() * operand.ny() * self.r()
                - self.r() * operand.nx() * self.r()
                + self.ps() * operand.nx() * self.ps())
                * inv_norm_sq,
            (-(-(self.ps() * operand.ny() * self.ps())
                + self.r() * operand.nx() * self.ps()
                + self.r() * operand.ny() * self.r()
                + self.ps() * operand.nx() * self.r()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Line<T>> for Motor<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (self.r() * operand.nx() * self.ty() + self.ps() * operand.d() * self.ps()
                - self.ty() * operand.ny() * self.ps()
                + self.tx() * operand.nx() * self.ps()
                + self.r() * operand.d() * self.r()
                + self.r() * operand.ny() * self.tx()
                + self.tx() * operand.ny() * self.r()
                + self.ps() * operand.nx() * self.tx()
                - self.ps() * operand.ny() * self.ty()
                + self.ty() * operand.nx() * self.r())
                * inv_norm_sq,
            (self.ps() * operand.nx() * self.ps() + self.ps() * operand.ny() * self.r()
                - self.r() * operand.nx() * self.r()
                + self.r() * operand.ny() * self.ps())
                * inv_norm_sq,
            (-(self.r() * operand.ny() * self.r() + self.ps() * operand.nx() * self.r()
                - self.ps() * operand.ny() * self.ps()
                + self.r() * operand.nx() * self.ps()))
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Motor<T>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (-(self.ps() * operand.ps() * self.ty())
                + self.ps() * operand.ty() * self.ps()
                + self.ty() * operand.ps() * self.ps()
                + self.ps() * operand.tx() * self.r()
                + self.tx() * operand.ps() * self.r()
                + self.ty() * operand.r() * self.r()
                - self.tx() * operand.r() * self.ps()
                + self.r() * operand.tx() * self.ps()
                + self.r() * operand.r() * self.ty()
                - self.ps() * operand.r() * self.tx()
                - self.r() * operand.ty() * self.r()
                - self.r() * operand.ps() * self.tx())
                * inv_norm_sq,
            (self.tx() * operand.r() * self.r() - self.ty() * operand.ps() * self.r()
                + self.ty() * operand.r() * self.ps()
                - self.r() * operand.tx() * self.r()
                + self.r() * operand.ps() * self.ty()
                + self.tx() * operand.ps() * self.ps()
                - self.ps() * operand.ty() * self.r()
                - self.ps() * operand.ps() * self.tx()
                - self.r() * operand.ty() * self.ps()
                + self.r() * operand.r() * self.tx()
                + self.ps() * operand.r() * self.ty()
                + self.ps() * operand.tx() * self.ps())
                * inv_norm_sq,
            (self.r() * operand.r() * self.r()
                + self.r() * operand.ps() * self.ps()
                + self.ps() * operand.r() * self.ps()
                - self.ps() * operand.ps() * self.r())
                * inv_norm_sq,
            (self.ps() * operand.r() * self.r() + self.ps() * operand.ps() * self.ps()
                - self.r() * operand.r() * self.ps()
                + self.r() * operand.ps() * self.r())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Point<T>> for Motor<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (-(self.tx() * operand.w() * self.ps())
                + self.r() * operand.w() * self.ty()
                + self.ps() * operand.x() * self.ps()
                - self.r() * operand.x() * self.r()
                - self.ps() * operand.w() * self.tx()
                + self.r() * operand.y() * self.ps()
                + self.ty() * operand.w() * self.r()
                + self.ps() * operand.y() * self.r())
                * inv_norm_sq,
            (self.ty() * operand.w() * self.ps() - self.r() * operand.y() * self.r()
                + self.tx() * operand.w() * self.r()
                - self.ps() * operand.x() * self.r()
                + self.ps() * operand.y() * self.ps()
                + self.ps() * operand.w() * self.ty()
                - self.r() * operand.x() * self.ps()
                + self.r() * operand.w() * self.tx())
                * inv_norm_sq,
            (self.ps() * operand.w() * self.ps() + self.r() * operand.w() * self.r()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Scalar<T>> for Motor<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked(
            (self.r() * operand.s() * self.r() + self.ps() * operand.s() * self.ps()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Trivector<T>> for Motor<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked(
            (self.r() * operand.ps() * self.r() + self.ps() * operand.ps() * self.ps())
                * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Flector<T>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (-(self.y() * operand.nx() * self.w())
                + self.x() * operand.ny() * self.w()
                + self.w() * operand.nx() * self.y()
                - self.w() * operand.ny() * self.x()
                + self.w() * operand.s() * self.w())
                * inv_norm_sq,
            (self.y() * operand.ny() * self.w()
                + self.x() * operand.nx() * self.w()
                + self.w() * operand.ny() * self.y()
                + self.w() * operand.d() * self.w()
                + self.w() * operand.nx() * self.x())
                * inv_norm_sq,
            (-(self.w() * operand.nx() * self.w())) * inv_norm_sq,
            (-(self.w() * operand.ny() * self.w())) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Line<T>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (self.w() * operand.d() * self.w()
                + self.w() * operand.nx() * self.x()
                + self.y() * operand.ny() * self.w()
                + self.x() * operand.nx() * self.w()
                + self.w() * operand.ny() * self.y())
                * inv_norm_sq,
            (-(self.w() * operand.nx() * self.w())) * inv_norm_sq,
            (-(self.w() * operand.ny() * self.w())) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Motor<T>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (self.y() * operand.ps() * self.w() - self.w() * operand.ty() * self.w()
                + self.x() * operand.r() * self.w()
                + self.w() * operand.r() * self.x()
                - self.w() * operand.ps() * self.y())
                * inv_norm_sq,
            (-(self.x() * operand.ps() * self.w()) + self.w() * operand.r() * self.y()
                - self.w() * operand.tx() * self.w()
                + self.w() * operand.ps() * self.x()
                + self.y() * operand.r() * self.w())
                * inv_norm_sq,
            (self.w() * operand.r() * self.w()) * inv_norm_sq,
            (self.w() * operand.ps() * self.w()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Point<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (self.x() * operand.w() * self.w() - self.w() * operand.x() * self.w()
                + self.w() * operand.w() * self.x())
                * inv_norm_sq,
            (-(self.w() * operand.y() * self.w())
                + self.w() * operand.w() * self.y()
                + self.y() * operand.w() * self.w())
                * inv_norm_sq,
            (self.w() * operand.w() * self.w()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Scalar<T>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked(
            (self.w() * operand.s() * self.w()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Trivector<T>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked(
            (self.w() * operand.ps() * self.w()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Flector<T>> for Scalar<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (-(T::zero())) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Line<T>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (-(T::zero())) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Motor<T>> for Scalar<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Point<T>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
            (T::zero()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Scalar<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked((T::zero()) * inv_norm_sq))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Trivector<T>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked((T::zero()) * inv_norm_sq))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Flector<T>> for Trivector<T> {
    type Output = Flector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Flector<T>) -> Option<Flector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Flector::new_unchecked(
            (self.ps() * operand.s() * self.ps()) * inv_norm_sq,
            (self.ps() * operand.d() * self.ps()) * inv_norm_sq,
            (self.ps() * operand.nx() * self.ps()) * inv_norm_sq,
            (-(-(self.ps() * operand.ny() * self.ps()))) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Line<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Line<T>) -> Option<Line<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Line::new_unchecked(
            (self.ps() * operand.d() * self.ps()) * inv_norm_sq,
            (self.ps() * operand.nx() * self.ps()) * inv_norm_sq,
            (-(-(self.ps() * operand.ny() * self.ps()))) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Motor<T>> for Trivector<T> {
    type Output = Motor<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Motor<T>) -> Option<Motor<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Motor::new_unchecked(
            (self.ps() * operand.ty() * self.ps()) * inv_norm_sq,
            (self.ps() * operand.tx() * self.ps()) * inv_norm_sq,
            (self.ps() * operand.r() * self.ps()) * inv_norm_sq,
            (self.ps() * operand.ps() * self.ps()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Point<T>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Point<T>) -> Option<Point<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Point::new_unchecked(
            (self.ps() * operand.x() * self.ps()) * inv_norm_sq,
            (self.ps() * operand.y() * self.ps()) * inv_norm_sq,
            (self.ps() * operand.w() * self.ps()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Scalar<T>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Scalar<T>) -> Option<Scalar<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Scalar::new_unchecked(
            (self.ps() * operand.s() * self.ps()) * inv_norm_sq,
        ))
    }
}
#[allow(unused_variables)]
impl<T: Float> InverseAntisandwich<Trivector<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn try_inverse_antisandwich(&self, operand: &Trivector<T>) -> Option<Trivector<T>> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Trivector::new_unchecked(
            (self.ps() * operand.ps() * self.ps()) * inv_norm_sq,
        ))
    }
}
impl<T: Float> Versor<Flector<T>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn compose(&self, other: &Flector<T>) -> Flector<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Line<T>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn compose(&self, other: &Line<T>) -> Flector<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Motor<T>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Motor<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Point<T>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Point<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Scalar<T>> for Flector<T> {
    type Output = Flector<T>;
    #[inline]
    fn compose(&self, other: &Scalar<T>) -> Flector<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Trivector<T>> for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Trivector<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Flector<T>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn compose(&self, other: &Flector<T>) -> Flector<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Line<T>> for Line<T> {
    type Output = Flector<T>;
    #[inline]
    fn compose(&self, other: &Line<T>) -> Flector<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Motor<T>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Motor<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Point<T>> for Line<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Point<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Scalar<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn compose(&self, other: &Scalar<T>) -> Line<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Trivector<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn compose(&self, other: &Trivector<T>) -> Point<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Flector<T>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Flector<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Line<T>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Line<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Motor<T>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn compose(&self, other: &Motor<T>) -> Flector<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Point<T>> for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn compose(&self, other: &Point<T>) -> Flector<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Scalar<T>> for Motor<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Scalar<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Trivector<T>> for Motor<T> {
    type Output = Line<T>;
    #[inline]
    fn compose(&self, other: &Trivector<T>) -> Line<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Flector<T>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Flector<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Line<T>> for Point<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Line<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Motor<T>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn compose(&self, other: &Motor<T>) -> Flector<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Point<T>> for Point<T> {
    type Output = Flector<T>;
    #[inline]
    fn compose(&self, other: &Point<T>) -> Flector<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Scalar<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn compose(&self, other: &Scalar<T>) -> Point<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Trivector<T>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn compose(&self, other: &Trivector<T>) -> Line<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Flector<T>> for Scalar<T> {
    type Output = Flector<T>;
    #[inline]
    fn compose(&self, other: &Flector<T>) -> Flector<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Line<T>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn compose(&self, other: &Line<T>) -> Line<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Motor<T>> for Scalar<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Motor<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Point<T>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn compose(&self, other: &Point<T>) -> Point<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Scalar<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn compose(&self, other: &Scalar<T>) -> Scalar<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Trivector<T>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn compose(&self, other: &Trivector<T>) -> Trivector<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Flector<T>> for Trivector<T> {
    type Output = Motor<T>;
    #[inline]
    fn compose(&self, other: &Flector<T>) -> Motor<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Line<T>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn compose(&self, other: &Line<T>) -> Point<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Motor<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn compose(&self, other: &Motor<T>) -> Line<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Point<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn compose(&self, other: &Point<T>) -> Line<T> {
        *self * *other
    }
}
impl<T: Float> Versor<Scalar<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn compose(&self, other: &Scalar<T>) -> Trivector<T> {
        *self * *other
    }
}
impl<T: Float> ScalarProduct<Line<T>> for Line<T> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Line<T>) -> T {
        -(self.d() * rhs.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> ScalarProduct<Line<T>> for Unitized<Line<T>> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Line<T>) -> T {
        -(rhs.d() * self.as_inner().d())
    }
}
#[allow(unused_variables)]
impl<T: Float> ScalarProduct<Unitized<Line<T>>> for Line<T> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Unitized<Line<T>>) -> T {
        -(rhs.as_inner().d() * self.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> ScalarProduct<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Unitized<Line<T>>) -> T {
        -(rhs.as_inner().d() * self.as_inner().d())
    }
}
impl<T: Float> ScalarProduct<Point<T>> for Point<T> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Point<T>) -> T {
        self.x() * rhs.x() + self.y() * rhs.y()
    }
}
#[allow(unused_variables)]
impl<T: Float> ScalarProduct<Point<T>> for Unitized<Point<T>> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Point<T>) -> T {
        rhs.x() * self.as_inner().x() + rhs.y() * self.as_inner().y()
    }
}
#[allow(unused_variables)]
impl<T: Float> ScalarProduct<Unitized<Point<T>>> for Point<T> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Unitized<Point<T>>) -> T {
        rhs.as_inner().x() * self.x() + rhs.as_inner().y() * self.y()
    }
}
#[allow(unused_variables)]
impl<T: Float> ScalarProduct<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Unitized<Point<T>>) -> T {
        rhs.as_inner().x() * self.as_inner().x() + rhs.as_inner().y() * self.as_inner().y()
    }
}
impl<T: Float> ScalarProduct<Scalar<T>> for Scalar<T> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Scalar<T>) -> T {
        self.s() * rhs.s()
    }
}
#[allow(unused_variables)]
impl<T: Float> ScalarProduct<Scalar<T>> for Unitized<Scalar<T>> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Scalar<T>) -> T {
        T::zero()
    }
}
#[allow(unused_variables)]
impl<T: Float> ScalarProduct<Unitized<Scalar<T>>> for Scalar<T> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Unitized<Scalar<T>>) -> T {
        T::zero()
    }
}
#[allow(unused_variables)]
impl<T: Float> ScalarProduct<Unitized<Scalar<T>>> for Unitized<Scalar<T>> {
    type Scalar = T;
    #[inline]
    fn scalar_product(&self, rhs: &Unitized<Scalar<T>>) -> T {
        T::zero()
    }
}
impl<T: Float> BulkContract<Line<T>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Line<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.d() * self.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Line<T>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Line<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.d() * self.as_inner().d())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Line<T>>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Line<T>>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.as_inner().d() * self.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Line<T>>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.as_inner().d() * self.as_inner().d())
    }
}
impl<T: Float> BulkContract<Point<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.y() * self.d()),
            rhs.x() * self.d(),
            -(rhs.x() * self.ny()) + rhs.y() * self.nx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Point<T>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.y() * self.as_inner().d()),
            rhs.x() * self.as_inner().d(),
            -(rhs.x() * self.as_inner().ny()) + rhs.y() * self.as_inner().nx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Point<T>>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().y() * self.d()),
            rhs.as_inner().x() * self.d(),
            -(rhs.as_inner().x() * self.ny()) + rhs.as_inner().y() * self.nx(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Point<T>>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().y() * self.as_inner().d()),
            rhs.as_inner().x() * self.as_inner().d(),
            -(rhs.as_inner().x() * self.as_inner().ny())
                + rhs.as_inner().y() * self.as_inner().nx(),
        )
    }
}
impl<T: Float> BulkContract<Scalar<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Scalar<T>) -> Line<T> {
        Line::new_unchecked(rhs.s() * self.d(), rhs.s() * self.nx(), rhs.s() * self.ny())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Scalar<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Scalar<T>) -> Line<T> {
        Line::new_unchecked(
            rhs.s() * self.as_inner().d(),
            rhs.s() * self.as_inner().nx(),
            rhs.s() * self.as_inner().ny(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Scalar<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Scalar<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Scalar<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Scalar<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> BulkContract<Point<T>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Point<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.x() * self.x() + rhs.y() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Point<T>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Point<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.x() * self.as_inner().x() + rhs.y() * self.as_inner().y())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Point<T>>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Point<T>>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.as_inner().x() * self.x() + rhs.as_inner().y() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Point<T>>) -> Scalar<T> {
        Scalar::new_unchecked(
            rhs.as_inner().x() * self.as_inner().x() + rhs.as_inner().y() * self.as_inner().y(),
        )
    }
}
impl<T: Float> BulkContract<Scalar<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Scalar<T>) -> Point<T> {
        Point::new_unchecked(rhs.s() * self.x(), rhs.s() * self.y(), rhs.s() * self.w())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Scalar<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Scalar<T>) -> Point<T> {
        Point::new_unchecked(
            rhs.s() * self.as_inner().x(),
            rhs.s() * self.as_inner().y(),
            rhs.s() * self.as_inner().w(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Scalar<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Scalar<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Scalar<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Scalar<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> BulkContract<Scalar<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(rhs.s() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Scalar<T>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Scalar<T>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Scalar<T>>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Scalar<T>>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Scalar<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
impl<T: Float> BulkContract<Line<T>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), rhs.d() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Line<T>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), rhs.d() * self.as_inner().ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Line<T>>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), rhs.as_inner().d() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Line<T>>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(
            T::zero(),
            T::zero(),
            rhs.as_inner().d() * self.as_inner().ps(),
        )
    }
}
impl<T: Float> BulkContract<Point<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), rhs.x() * self.ps(), rhs.y() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Point<T>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.x() * self.as_inner().ps(),
            rhs.y() * self.as_inner().ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Point<T>>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.as_inner().x() * self.ps(),
            rhs.as_inner().y() * self.ps(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Point<T>>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.as_inner().x() * self.as_inner().ps(),
            rhs.as_inner().y() * self.as_inner().ps(),
        )
    }
}
impl<T: Float> BulkContract<Scalar<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Scalar<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.s() * self.ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Scalar<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Scalar<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.s() * self.as_inner().ps())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Scalar<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Scalar<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkContract<Unitized<Scalar<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_contract(&self, rhs: &Unitized<Scalar<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
impl<T: Float> WeightContract<Line<T>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Line<T>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.nx() * self.nx()) + -(rhs.ny() * self.ny()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Line<T>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Line<T>) -> Scalar<T> {
        Scalar::new_unchecked(
            -(rhs.nx() * self.as_inner().nx()) + -(rhs.ny() * self.as_inner().ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Line<T>>> for Line<T> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Line<T>>) -> Scalar<T> {
        Scalar::new_unchecked(
            -(rhs.as_inner().nx() * self.nx()) + -(rhs.as_inner().ny() * self.ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Line<T>>) -> Scalar<T> {
        Scalar::new_unchecked(
            -(rhs.as_inner().nx() * self.as_inner().nx())
                + -(rhs.as_inner().ny() * self.as_inner().ny()),
        )
    }
}
impl<T: Float> WeightContract<Point<T>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(-(rhs.w() * self.ny()), rhs.w() * self.nx(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Point<T>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.w() * self.as_inner().ny()),
            rhs.w() * self.as_inner().nx(),
            T::zero(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Point<T>>> for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().w() * self.ny()),
            rhs.as_inner().w() * self.nx(),
            T::zero(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Point<T>>> for Unitized<Line<T>> {
    type Output = Point<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Point<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().w() * self.as_inner().ny()),
            rhs.as_inner().w() * self.as_inner().nx(),
            T::zero(),
        )
    }
}
impl<T: Float> WeightContract<Point<T>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Point<T>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.w() * self.w()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Point<T>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Point<T>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.w() * self.as_inner().w()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Point<T>>> for Point<T> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Point<T>>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.as_inner().w() * self.w()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Point<T>>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.as_inner().w() * self.as_inner().w()))
    }
}
impl<T: Float> WeightContract<Line<T>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(-(rhs.nx() * self.ps()), -(rhs.ny() * self.ps()), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Line<T>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.nx() * self.as_inner().ps()),
            -(rhs.ny() * self.as_inner().ps()),
            T::zero(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Line<T>>> for Trivector<T> {
    type Output = Point<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().nx() * self.ps()),
            -(rhs.as_inner().ny() * self.ps()),
            T::zero(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Line<T>>> for Unitized<Trivector<T>> {
    type Output = Point<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().nx() * self.as_inner().ps()),
            -(rhs.as_inner().ny() * self.as_inner().ps()),
            T::zero(),
        )
    }
}
impl<T: Float> WeightContract<Point<T>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(-(rhs.w() * self.ps()), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Point<T>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(-(rhs.w() * self.as_inner().ps()), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Point<T>>> for Trivector<T> {
    type Output = Line<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(-(rhs.as_inner().w() * self.ps()), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Point<T>>> for Unitized<Trivector<T>> {
    type Output = Line<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.as_inner().w() * self.as_inner().ps()),
            T::zero(),
            T::zero(),
        )
    }
}
impl<T: Float> WeightContract<Trivector<T>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Trivector<T>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.ps() * self.ps()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Trivector<T>> for Unitized<Trivector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Trivector<T>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.ps() * self.as_inner().ps()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Trivector<T>>> for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Trivector<T>>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.as_inner().ps() * self.ps()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightContract<Unitized<Trivector<T>>> for Unitized<Trivector<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_contract(&self, rhs: &Unitized<Trivector<T>>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.as_inner().ps() * self.as_inner().ps()))
    }
}
impl<T: Float> BulkExpand<Line<T>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Line<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.d() * self.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Line<T>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Line<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.d() * self.as_inner().d())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Line<T>>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Line<T>>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.as_inner().d() * self.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Line<T>>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.as_inner().d() * self.as_inner().d())
    }
}
impl<T: Float> BulkExpand<Line<T>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Line<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), rhs.d() * self.y(), -(rhs.d() * self.x()))
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Line<T>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.d() * self.as_inner().y(),
            -(rhs.d() * self.as_inner().x()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Line<T>>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.as_inner().d() * self.y(),
            -(rhs.as_inner().d() * self.x()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Line<T>>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.as_inner().d() * self.as_inner().y(),
            -(rhs.as_inner().d() * self.as_inner().x()),
        )
    }
}
impl<T: Float> BulkExpand<Point<T>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Point<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.x() * self.x() + rhs.y() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Point<T>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Point<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.x() * self.as_inner().x() + rhs.y() * self.as_inner().y())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Point<T>>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Point<T>>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.as_inner().x() * self.x() + rhs.as_inner().y() * self.y())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Point<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            rhs.as_inner().x() * self.as_inner().x() + rhs.as_inner().y() * self.as_inner().y(),
        )
    }
}
impl<T: Float> BulkExpand<Line<T>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), rhs.d() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Line<T>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Line<T>>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), rhs.as_inner().d() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Line<T>>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> BulkExpand<Point<T>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), rhs.x() * self.s(), rhs.y() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Point<T>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Point<T>>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(
            T::zero(),
            rhs.as_inner().x() * self.s(),
            rhs.as_inner().y() * self.s(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Point<T>>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> BulkExpand<Scalar<T>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Scalar<T>) -> Trivector<T> {
        Trivector::new_unchecked(rhs.s() * self.s())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Scalar<T>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Scalar<T>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Scalar<T>>> for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Scalar<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> BulkExpand<Unitized<Scalar<T>>> for Unitized<Scalar<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn bulk_expand(&self, rhs: &Unitized<Scalar<T>>) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
impl<T: Float> WeightExpand<Line<T>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Line<T>) -> Trivector<T> {
        Trivector::new_unchecked(-(rhs.nx() * self.nx()) + -(rhs.ny() * self.ny()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Line<T>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Line<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            -(rhs.nx() * self.as_inner().nx()) + -(rhs.ny() * self.as_inner().ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Line<T>>> for Line<T> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Line<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            -(rhs.as_inner().nx() * self.nx()) + -(rhs.as_inner().ny() * self.ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Line<T>>) -> Trivector<T> {
        Trivector::new_unchecked(
            -(rhs.as_inner().nx() * self.as_inner().nx())
                + -(rhs.as_inner().ny() * self.as_inner().ny()),
        )
    }
}
impl<T: Float> WeightExpand<Trivector<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Trivector<T>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.ps() * self.d()),
            -(rhs.ps() * self.nx()),
            -(rhs.ps() * self.ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Trivector<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Trivector<T>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.ps() * self.as_inner().d()),
            -(rhs.ps() * self.as_inner().nx()),
            -(rhs.ps() * self.as_inner().ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Trivector<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Trivector<T>>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.as_inner().ps() * self.d()),
            -(rhs.as_inner().ps() * self.nx()),
            -(rhs.as_inner().ps() * self.ny()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Trivector<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Trivector<T>>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.as_inner().ps() * self.as_inner().d()),
            -(rhs.as_inner().ps() * self.as_inner().nx()),
            -(rhs.as_inner().ps() * self.as_inner().ny()),
        )
    }
}
impl<T: Float> WeightExpand<Line<T>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.ny() * self.x()) + rhs.nx() * self.y(),
            rhs.ny() * self.w(),
            -(rhs.nx() * self.w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Line<T>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.ny() * self.as_inner().x()) + rhs.nx() * self.as_inner().y(),
            rhs.ny() * self.as_inner().w(),
            -(rhs.nx() * self.as_inner().w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Line<T>>> for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.as_inner().ny() * self.x()) + rhs.as_inner().nx() * self.y(),
            rhs.as_inner().ny() * self.w(),
            -(rhs.as_inner().nx() * self.w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Line<T>>> for Unitized<Point<T>> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Line<T>>) -> Line<T> {
        Line::new_unchecked(
            -(rhs.as_inner().ny() * self.as_inner().x())
                + rhs.as_inner().nx() * self.as_inner().y(),
            rhs.as_inner().ny() * self.as_inner().w(),
            -(rhs.as_inner().nx() * self.as_inner().w()),
        )
    }
}
impl<T: Float> WeightExpand<Point<T>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Point<T>) -> Trivector<T> {
        Trivector::new_unchecked(-(rhs.w() * self.w()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Point<T>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Point<T>) -> Trivector<T> {
        Trivector::new_unchecked(-(rhs.w() * self.as_inner().w()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Point<T>>> for Point<T> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Point<T>>) -> Trivector<T> {
        Trivector::new_unchecked(-(rhs.as_inner().w() * self.w()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Point<T>>) -> Trivector<T> {
        Trivector::new_unchecked(-(rhs.as_inner().w() * self.as_inner().w()))
    }
}
impl<T: Float> WeightExpand<Trivector<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Trivector<T>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.ps() * self.x()),
            -(rhs.ps() * self.y()),
            -(rhs.ps() * self.w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Trivector<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Trivector<T>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.ps() * self.as_inner().x()),
            -(rhs.ps() * self.as_inner().y()),
            -(rhs.ps() * self.as_inner().w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Trivector<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Trivector<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().ps() * self.x()),
            -(rhs.as_inner().ps() * self.y()),
            -(rhs.as_inner().ps() * self.w()),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Trivector<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Trivector<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().ps() * self.as_inner().x()),
            -(rhs.as_inner().ps() * self.as_inner().y()),
            -(rhs.as_inner().ps() * self.as_inner().w()),
        )
    }
}
impl<T: Float> WeightExpand<Line<T>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(-(rhs.nx() * self.s()), -(rhs.ny() * self.s()), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Line<T>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Line<T>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Line<T>>> for Scalar<T> {
    type Output = Point<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(
            -(rhs.as_inner().nx() * self.s()),
            -(rhs.as_inner().ny() * self.s()),
            T::zero(),
        )
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Line<T>>> for Unitized<Scalar<T>> {
    type Output = Point<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Line<T>>) -> Point<T> {
        Point::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> WeightExpand<Point<T>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(-(rhs.w() * self.s()), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Point<T>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Point<T>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Point<T>>> for Scalar<T> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(-(rhs.as_inner().w() * self.s()), T::zero(), T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Point<T>>> for Unitized<Scalar<T>> {
    type Output = Line<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Point<T>>) -> Line<T> {
        Line::new_unchecked(T::zero(), T::zero(), T::zero())
    }
}
impl<T: Float> WeightExpand<Trivector<T>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Trivector<T>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.ps() * self.s()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Trivector<T>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Trivector<T>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Trivector<T>>> for Scalar<T> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Trivector<T>>) -> Scalar<T> {
        Scalar::new_unchecked(-(rhs.as_inner().ps() * self.s()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Trivector<T>>> for Unitized<Scalar<T>> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Trivector<T>>) -> Scalar<T> {
        Scalar::new_unchecked(T::zero())
    }
}
impl<T: Float> WeightExpand<Trivector<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(-(rhs.ps() * self.ps()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Trivector<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(-(rhs.ps() * self.as_inner().ps()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Trivector<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(-(rhs.as_inner().ps() * self.ps()))
    }
}
#[allow(unused_variables)]
impl<T: Float> WeightExpand<Unitized<Trivector<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_expand(&self, rhs: &Unitized<Trivector<T>>) -> Trivector<T> {
        Trivector::new_unchecked(-(rhs.as_inner().ps() * self.as_inner().ps()))
    }
}
impl<T: Float> Dot<Flector<T>> for Flector<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Flector<T>) -> T {
        self.s() * rhs.s() - self.d() * rhs.d()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Flector<T>> for Unitized<Flector<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Flector<T>) -> T {
        -(rhs.d() * self.as_inner().d()) + rhs.s() * self.as_inner().s()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Flector<T>>> for Flector<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Flector<T>>) -> T {
        -(rhs.as_inner().d() * self.d()) + rhs.as_inner().s() * self.s()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Flector<T>>> for Unitized<Flector<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Flector<T>>) -> T {
        -(rhs.as_inner().d() * self.as_inner().d()) + rhs.as_inner().s() * self.as_inner().s()
    }
}
impl<T: Float> Dot<Line<T>> for Flector<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Line<T>) -> T {
        -(self.d() * rhs.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Line<T>> for Unitized<Flector<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Line<T>) -> T {
        -(rhs.d() * self.as_inner().d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Line<T>>> for Flector<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Line<T>>) -> T {
        -(rhs.as_inner().d() * self.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Line<T>>> for Unitized<Flector<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Line<T>>) -> T {
        -(rhs.as_inner().d() * self.as_inner().d())
    }
}
impl<T: Float> Dot<Scalar<T>> for Flector<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Scalar<T>) -> T {
        self.s() * rhs.s()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Scalar<T>> for Unitized<Flector<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Scalar<T>) -> T {
        rhs.s() * self.as_inner().s()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Scalar<T>>> for Flector<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Scalar<T>>) -> T {
        T::zero()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Scalar<T>>> for Unitized<Flector<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Scalar<T>>) -> T {
        T::zero()
    }
}
impl<T: Float> Dot<Flector<T>> for Line<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Flector<T>) -> T {
        -(self.d() * rhs.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Flector<T>> for Unitized<Line<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Flector<T>) -> T {
        -(rhs.d() * self.as_inner().d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Flector<T>>> for Line<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Flector<T>>) -> T {
        -(rhs.as_inner().d() * self.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Flector<T>>> for Unitized<Line<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Flector<T>>) -> T {
        -(rhs.as_inner().d() * self.as_inner().d())
    }
}
impl<T: Float> Dot<Line<T>> for Line<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Line<T>) -> T {
        -(self.d() * rhs.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Line<T>> for Unitized<Line<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Line<T>) -> T {
        -(rhs.d() * self.as_inner().d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Line<T>>> for Line<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Line<T>>) -> T {
        -(rhs.as_inner().d() * self.d())
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Line<T>>) -> T {
        -(rhs.as_inner().d() * self.as_inner().d())
    }
}
impl<T: Float> Dot<Motor<T>> for Motor<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Motor<T>) -> T {
        self.ty() * rhs.ty() + self.tx() * rhs.tx()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Motor<T>> for Unitized<Motor<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Motor<T>) -> T {
        rhs.tx() * self.as_inner().tx() + rhs.ty() * self.as_inner().ty()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Motor<T>>> for Motor<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Motor<T>>) -> T {
        rhs.as_inner().tx() * self.tx() + rhs.as_inner().ty() * self.ty()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Motor<T>>> for Unitized<Motor<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Motor<T>>) -> T {
        rhs.as_inner().tx() * self.as_inner().tx() + rhs.as_inner().ty() * self.as_inner().ty()
    }
}
impl<T: Float> Dot<Point<T>> for Motor<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Point<T>) -> T {
        self.ty() * rhs.x() + self.tx() * rhs.y()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Point<T>> for Unitized<Motor<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Point<T>) -> T {
        rhs.x() * self.as_inner().ty() + rhs.y() * self.as_inner().tx()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Point<T>>> for Motor<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Point<T>>) -> T {
        rhs.as_inner().x() * self.ty() + rhs.as_inner().y() * self.tx()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Point<T>>> for Unitized<Motor<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Point<T>>) -> T {
        rhs.as_inner().x() * self.as_inner().ty() + rhs.as_inner().y() * self.as_inner().tx()
    }
}
impl<T: Float> Dot<Motor<T>> for Point<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Motor<T>) -> T {
        self.x() * rhs.ty() + self.y() * rhs.tx()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Motor<T>> for Unitized<Point<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Motor<T>) -> T {
        rhs.tx() * self.as_inner().y() + rhs.ty() * self.as_inner().x()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Motor<T>>> for Point<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Motor<T>>) -> T {
        rhs.as_inner().tx() * self.y() + rhs.as_inner().ty() * self.x()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Motor<T>>> for Unitized<Point<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Motor<T>>) -> T {
        rhs.as_inner().tx() * self.as_inner().y() + rhs.as_inner().ty() * self.as_inner().x()
    }
}
impl<T: Float> Dot<Point<T>> for Point<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Point<T>) -> T {
        self.x() * rhs.x() + self.y() * rhs.y()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Point<T>> for Unitized<Point<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Point<T>) -> T {
        rhs.x() * self.as_inner().x() + rhs.y() * self.as_inner().y()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Point<T>>> for Point<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Point<T>>) -> T {
        rhs.as_inner().x() * self.x() + rhs.as_inner().y() * self.y()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Point<T>>) -> T {
        rhs.as_inner().x() * self.as_inner().x() + rhs.as_inner().y() * self.as_inner().y()
    }
}
impl<T: Float> Dot<Flector<T>> for Scalar<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Flector<T>) -> T {
        self.s() * rhs.s()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Flector<T>> for Unitized<Scalar<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Flector<T>) -> T {
        T::zero()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Flector<T>>> for Scalar<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Flector<T>>) -> T {
        rhs.as_inner().s() * self.s()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Flector<T>>> for Unitized<Scalar<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Flector<T>>) -> T {
        T::zero()
    }
}
impl<T: Float> Dot<Scalar<T>> for Scalar<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Scalar<T>) -> T {
        self.s() * rhs.s()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Scalar<T>> for Unitized<Scalar<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Scalar<T>) -> T {
        T::zero()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Scalar<T>>> for Scalar<T> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Scalar<T>>) -> T {
        T::zero()
    }
}
#[allow(unused_variables)]
impl<T: Float> Dot<Unitized<Scalar<T>>> for Unitized<Scalar<T>> {
    type Scalar = T;
    #[inline]
    fn dot(&self, rhs: &Unitized<Scalar<T>>) -> T {
        T::zero()
    }
}
impl<T: Float> Antidot<Flector<T>> for Flector<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Flector<T>) -> T {
        self.nx() * rhs.nx() + self.ny() * rhs.ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Flector<T>> for Unitized<Flector<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Flector<T>) -> T {
        rhs.nx() * self.as_inner().nx() + rhs.ny() * self.as_inner().ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Flector<T>>> for Flector<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Flector<T>>) -> T {
        rhs.as_inner().nx() * self.nx() + rhs.as_inner().ny() * self.ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Flector<T>>> for Unitized<Flector<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Flector<T>>) -> T {
        rhs.as_inner().nx() * self.as_inner().nx() + rhs.as_inner().ny() * self.as_inner().ny()
    }
}
impl<T: Float> Antidot<Line<T>> for Flector<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Line<T>) -> T {
        self.nx() * rhs.nx() + self.ny() * rhs.ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Line<T>> for Unitized<Flector<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Line<T>) -> T {
        rhs.nx() * self.as_inner().nx() + rhs.ny() * self.as_inner().ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Line<T>>> for Flector<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Line<T>>) -> T {
        rhs.as_inner().nx() * self.nx() + rhs.as_inner().ny() * self.ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Line<T>>> for Unitized<Flector<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Line<T>>) -> T {
        rhs.as_inner().nx() * self.as_inner().nx() + rhs.as_inner().ny() * self.as_inner().ny()
    }
}
impl<T: Float> Antidot<Flector<T>> for Line<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Flector<T>) -> T {
        self.nx() * rhs.nx() + self.ny() * rhs.ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Flector<T>> for Unitized<Line<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Flector<T>) -> T {
        rhs.nx() * self.as_inner().nx() + rhs.ny() * self.as_inner().ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Flector<T>>> for Line<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Flector<T>>) -> T {
        rhs.as_inner().nx() * self.nx() + rhs.as_inner().ny() * self.ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Flector<T>>> for Unitized<Line<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Flector<T>>) -> T {
        rhs.as_inner().nx() * self.as_inner().nx() + rhs.as_inner().ny() * self.as_inner().ny()
    }
}
impl<T: Float> Antidot<Line<T>> for Line<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Line<T>) -> T {
        self.nx() * rhs.nx() + self.ny() * rhs.ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Line<T>> for Unitized<Line<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Line<T>) -> T {
        rhs.nx() * self.as_inner().nx() + rhs.ny() * self.as_inner().ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Line<T>>> for Line<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Line<T>>) -> T {
        rhs.as_inner().nx() * self.nx() + rhs.as_inner().ny() * self.ny()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Line<T>>) -> T {
        rhs.as_inner().nx() * self.as_inner().nx() + rhs.as_inner().ny() * self.as_inner().ny()
    }
}
impl<T: Float> Antidot<Motor<T>> for Motor<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Motor<T>) -> T {
        -(self.r() * rhs.r()) + self.ps() * rhs.ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Motor<T>> for Unitized<Motor<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Motor<T>) -> T {
        -(rhs.r() * self.as_inner().r()) + rhs.ps() * self.as_inner().ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Motor<T>>> for Motor<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Motor<T>>) -> T {
        -(rhs.as_inner().r() * self.r()) + rhs.as_inner().ps() * self.ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Motor<T>>> for Unitized<Motor<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Motor<T>>) -> T {
        -(rhs.as_inner().r() * self.as_inner().r()) + rhs.as_inner().ps() * self.as_inner().ps()
    }
}
impl<T: Float> Antidot<Point<T>> for Motor<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Point<T>) -> T {
        -(self.r() * rhs.w())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Point<T>> for Unitized<Motor<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Point<T>) -> T {
        -(rhs.w() * self.as_inner().r())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Point<T>>> for Motor<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Point<T>>) -> T {
        -(rhs.as_inner().w() * self.r())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Point<T>>> for Unitized<Motor<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Point<T>>) -> T {
        -(rhs.as_inner().w() * self.as_inner().r())
    }
}
impl<T: Float> Antidot<Trivector<T>> for Motor<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Trivector<T>) -> T {
        self.ps() * rhs.ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Trivector<T>> for Unitized<Motor<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Trivector<T>) -> T {
        rhs.ps() * self.as_inner().ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Trivector<T>>> for Motor<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Trivector<T>>) -> T {
        rhs.as_inner().ps() * self.ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Trivector<T>>> for Unitized<Motor<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Trivector<T>>) -> T {
        rhs.as_inner().ps() * self.as_inner().ps()
    }
}
impl<T: Float> Antidot<Motor<T>> for Point<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Motor<T>) -> T {
        -(self.w() * rhs.r())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Motor<T>> for Unitized<Point<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Motor<T>) -> T {
        -(rhs.r() * self.as_inner().w())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Motor<T>>> for Point<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Motor<T>>) -> T {
        -(rhs.as_inner().r() * self.w())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Motor<T>>> for Unitized<Point<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Motor<T>>) -> T {
        -(rhs.as_inner().r() * self.as_inner().w())
    }
}
impl<T: Float> Antidot<Point<T>> for Point<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Point<T>) -> T {
        -(self.w() * rhs.w())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Point<T>> for Unitized<Point<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Point<T>) -> T {
        -(rhs.w() * self.as_inner().w())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Point<T>>> for Point<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Point<T>>) -> T {
        -(rhs.as_inner().w() * self.w())
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Point<T>>) -> T {
        -(rhs.as_inner().w() * self.as_inner().w())
    }
}
impl<T: Float> Antidot<Motor<T>> for Trivector<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Motor<T>) -> T {
        self.ps() * rhs.ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Motor<T>> for Unitized<Trivector<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Motor<T>) -> T {
        rhs.ps() * self.as_inner().ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Motor<T>>> for Trivector<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Motor<T>>) -> T {
        rhs.as_inner().ps() * self.ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Motor<T>>> for Unitized<Trivector<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Motor<T>>) -> T {
        rhs.as_inner().ps() * self.as_inner().ps()
    }
}
impl<T: Float> Antidot<Trivector<T>> for Trivector<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Trivector<T>) -> T {
        self.ps() * rhs.ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Trivector<T>> for Unitized<Trivector<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Trivector<T>) -> T {
        rhs.ps() * self.as_inner().ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Trivector<T>>> for Trivector<T> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Trivector<T>>) -> T {
        rhs.as_inner().ps() * self.ps()
    }
}
#[allow(unused_variables)]
impl<T: Float> Antidot<Unitized<Trivector<T>>> for Unitized<Trivector<T>> {
    type Scalar = T;
    #[inline]
    fn antidot(&self, rhs: &Unitized<Trivector<T>>) -> T {
        rhs.as_inner().ps() * self.as_inner().ps()
    }
}
impl<T: Float> Project<Line<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn project(&self, target: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -(self.nx() * target.nx() * target.d()) - self.ny() * target.ny() * target.d(),
            -(self.nx() * target.nx() * target.nx()) - self.ny() * target.ny() * target.nx(),
            -(self.nx() * target.nx() * target.ny()) - self.ny() * target.ny() * target.ny(),
        )
    }
}
impl<T: Float> Project<Line<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn project(&self, target: &Line<T>) -> Line<T> {
        self.as_inner().project(target)
    }
}
impl<T: Float> Project<Unitized<Line<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn project(&self, target: &Unitized<Line<T>>) -> Line<T> {
        self.project(target.as_inner())
    }
}
impl<T: Float> Project<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn project(&self, target: &Unitized<Line<T>>) -> Line<T> {
        self.as_inner().project(target.as_inner())
    }
}
impl<T: Float> Project<Trivector<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn project(&self, target: &Trivector<T>) -> Line<T> {
        Line::new_unchecked(
            -(self.d() * target.ps() * target.ps()),
            -(self.nx() * target.ps() * target.ps()),
            -(self.ny() * target.ps() * target.ps()),
        )
    }
}
impl<T: Float> Project<Trivector<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn project(&self, target: &Trivector<T>) -> Line<T> {
        self.as_inner().project(target)
    }
}
impl<T: Float> Project<Unitized<Trivector<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn project(&self, target: &Unitized<Trivector<T>>) -> Line<T> {
        self.project(target.as_inner())
    }
}
impl<T: Float> Project<Unitized<Trivector<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn project(&self, target: &Unitized<Trivector<T>>) -> Line<T> {
        self.as_inner().project(target.as_inner())
    }
}
impl<T: Float> Project<Line<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Line<T>) -> Point<T> {
        Point::new_unchecked(
            self.w() * target.nx() * target.d()
                - self.x() * target.ny() * target.ny()
                - self.y() * target.nx() * target.ny(),
            -(self.x() * target.ny() * target.nx())
                - self.y() * target.nx() * target.nx()
                - self.w() * target.ny() * target.d(),
            -(self.w() * target.ny() * target.ny()) - self.w() * target.nx() * target.nx(),
        )
    }
}
impl<T: Float> Project<Line<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Line<T>) -> Point<T> {
        self.as_inner().project(target)
    }
}
impl<T: Float> Project<Unitized<Line<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Unitized<Line<T>>) -> Point<T> {
        self.project(target.as_inner())
    }
}
impl<T: Float> Project<Unitized<Line<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Unitized<Line<T>>) -> Point<T> {
        self.as_inner().project(target.as_inner())
    }
}
impl<T: Float> Project<Point<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(self.w() * target.w() * target.x()),
            -(self.w() * target.w() * target.y()),
            -(self.w() * target.w() * target.w()),
        )
    }
}
impl<T: Float> Project<Point<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Point<T>) -> Point<T> {
        self.as_inner().project(target)
    }
}
impl<T: Float> Project<Unitized<Point<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Unitized<Point<T>>) -> Point<T> {
        self.project(target.as_inner())
    }
}
impl<T: Float> Project<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Unitized<Point<T>>) -> Point<T> {
        self.as_inner().project(target.as_inner())
    }
}
impl<T: Float> Project<Trivector<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Trivector<T>) -> Point<T> {
        Point::new_unchecked(
            -(self.x() * target.ps() * target.ps()),
            -(self.y() * target.ps() * target.ps()),
            -(self.w() * target.ps() * target.ps()),
        )
    }
}
impl<T: Float> Project<Trivector<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Trivector<T>) -> Point<T> {
        self.as_inner().project(target)
    }
}
impl<T: Float> Project<Unitized<Trivector<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Unitized<Trivector<T>>) -> Point<T> {
        self.project(target.as_inner())
    }
}
impl<T: Float> Project<Unitized<Trivector<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn project(&self, target: &Unitized<Trivector<T>>) -> Point<T> {
        self.as_inner().project(target.as_inner())
    }
}
impl<T: Float> Project<Trivector<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn project(&self, target: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(-(self.ps() * target.ps() * target.ps()))
    }
}
impl<T: Float> Project<Trivector<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn project(&self, target: &Trivector<T>) -> Trivector<T> {
        self.as_inner().project(target)
    }
}
impl<T: Float> Project<Unitized<Trivector<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn project(&self, target: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.project(target.as_inner())
    }
}
impl<T: Float> Project<Unitized<Trivector<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn project(&self, target: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.as_inner().project(target.as_inner())
    }
}
impl<T: Float> Antiproject<Line<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn antiproject(&self, target: &Line<T>) -> Line<T> {
        Line::new_unchecked(
            -(self.nx() * target.nx() * target.d()) - self.ny() * target.ny() * target.d(),
            -(self.ny() * target.ny() * target.nx()) - self.nx() * target.nx() * target.nx(),
            -(self.nx() * target.nx() * target.ny()) - self.ny() * target.ny() * target.ny(),
        )
    }
}
impl<T: Float> Antiproject<Line<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn antiproject(&self, target: &Line<T>) -> Line<T> {
        self.as_inner().antiproject(target)
    }
}
impl<T: Float> Antiproject<Unitized<Line<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Line<T>>) -> Line<T> {
        self.antiproject(target.as_inner())
    }
}
impl<T: Float> Antiproject<Unitized<Line<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Line<T>>) -> Line<T> {
        self.as_inner().antiproject(target.as_inner())
    }
}
impl<T: Float> Antiproject<Point<T>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn antiproject(&self, target: &Point<T>) -> Line<T> {
        Line::new_unchecked(
            -(self.ny() * target.w() * target.y()) + self.nx() * target.w() * target.x(),
            -(self.nx() * target.w() * target.w()),
            -(self.ny() * target.w() * target.w()),
        )
    }
}
impl<T: Float> Antiproject<Point<T>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn antiproject(&self, target: &Point<T>) -> Line<T> {
        self.as_inner().antiproject(target)
    }
}
impl<T: Float> Antiproject<Unitized<Point<T>>> for Line<T> {
    type Output = Line<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Point<T>>) -> Line<T> {
        self.antiproject(target.as_inner())
    }
}
impl<T: Float> Antiproject<Unitized<Point<T>>> for Unitized<Line<T>> {
    type Output = Line<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Point<T>>) -> Line<T> {
        self.as_inner().antiproject(target.as_inner())
    }
}
impl<T: Float> Antiproject<Point<T>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn antiproject(&self, target: &Point<T>) -> Point<T> {
        Point::new_unchecked(
            -(self.w() * target.w() * target.x()),
            -(self.w() * target.w() * target.y()),
            -(self.w() * target.w() * target.w()),
        )
    }
}
impl<T: Float> Antiproject<Point<T>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn antiproject(&self, target: &Point<T>) -> Point<T> {
        self.as_inner().antiproject(target)
    }
}
impl<T: Float> Antiproject<Unitized<Point<T>>> for Point<T> {
    type Output = Point<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Point<T>>) -> Point<T> {
        self.antiproject(target.as_inner())
    }
}
impl<T: Float> Antiproject<Unitized<Point<T>>> for Unitized<Point<T>> {
    type Output = Point<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Point<T>>) -> Point<T> {
        self.as_inner().antiproject(target.as_inner())
    }
}
impl<T: Float> Antiproject<Line<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Line<T>) -> Trivector<T> {
        Trivector::new_unchecked(
            -(self.ps() * target.nx() * target.nx()) - self.ps() * target.ny() * target.ny(),
        )
    }
}
impl<T: Float> Antiproject<Line<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Line<T>) -> Trivector<T> {
        self.as_inner().antiproject(target)
    }
}
impl<T: Float> Antiproject<Unitized<Line<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Line<T>>) -> Trivector<T> {
        self.antiproject(target.as_inner())
    }
}
impl<T: Float> Antiproject<Unitized<Line<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Line<T>>) -> Trivector<T> {
        self.as_inner().antiproject(target.as_inner())
    }
}
impl<T: Float> Antiproject<Point<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Point<T>) -> Trivector<T> {
        Trivector::new_unchecked(-(self.ps() * target.w() * target.w()))
    }
}
impl<T: Float> Antiproject<Point<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Point<T>) -> Trivector<T> {
        self.as_inner().antiproject(target)
    }
}
impl<T: Float> Antiproject<Unitized<Point<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Point<T>>) -> Trivector<T> {
        self.antiproject(target.as_inner())
    }
}
impl<T: Float> Antiproject<Unitized<Point<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Point<T>>) -> Trivector<T> {
        self.as_inner().antiproject(target.as_inner())
    }
}
impl<T: Float> Antiproject<Trivector<T>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Trivector<T>) -> Trivector<T> {
        Trivector::new_unchecked(-(self.ps() * target.ps() * target.ps()))
    }
}
impl<T: Float> Antiproject<Trivector<T>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Trivector<T>) -> Trivector<T> {
        self.as_inner().antiproject(target)
    }
}
impl<T: Float> Antiproject<Unitized<Trivector<T>>> for Trivector<T> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.antiproject(target.as_inner())
    }
}
impl<T: Float> Antiproject<Unitized<Trivector<T>>> for Unitized<Trivector<T>> {
    type Output = Trivector<T>;
    #[inline]
    fn antiproject(&self, target: &Unitized<Trivector<T>>) -> Trivector<T> {
        self.as_inner().antiproject(target.as_inner())
    }
}
impl<T: Float> Reverse for Flector<T> {
    #[inline]
    fn reverse(&self) -> Self {
        Self::new_unchecked(self.s(), -self.d(), -self.nx(), -self.ny())
    }
}
impl<T: Float> Reverse for Line<T> {
    #[inline]
    fn reverse(&self) -> Self {
        Self::new_unchecked(-self.d(), -self.nx(), -self.ny())
    }
}
impl<T: Float> Reverse for Motor<T> {
    #[inline]
    fn reverse(&self) -> Self {
        Self::new_unchecked(self.ty(), self.tx(), self.r(), -self.ps())
    }
}
impl<T: Float> Reverse for Point<T> {
    #[inline]
    fn reverse(&self) -> Self {
        Self::new_unchecked(self.x(), self.y(), self.w())
    }
}
impl<T: Float> Reverse for Scalar<T> {
    #[inline]
    fn reverse(&self) -> Self {
        Self::new_unchecked(self.s())
    }
}
impl<T: Float> Reverse for Trivector<T> {
    #[inline]
    fn reverse(&self) -> Self {
        Self::new_unchecked(-self.ps())
    }
}
impl<T: Float> Antireverse for Flector<T> {
    #[inline]
    fn antireverse(&self) -> Self {
        Self::new_unchecked(-self.s(), self.d(), self.nx(), self.ny())
    }
}
impl<T: Float> Antireverse for Line<T> {
    #[inline]
    fn antireverse(&self) -> Self {
        Self::new_unchecked(self.d(), self.nx(), self.ny())
    }
}
impl<T: Float> Antireverse for Motor<T> {
    #[inline]
    fn antireverse(&self) -> Self {
        Self::new_unchecked(-self.ty(), -self.tx(), -self.r(), self.ps())
    }
}
impl<T: Float> Antireverse for Point<T> {
    #[inline]
    fn antireverse(&self) -> Self {
        Self::new_unchecked(-self.x(), -self.y(), -self.w())
    }
}
impl<T: Float> Antireverse for Scalar<T> {
    #[inline]
    fn antireverse(&self) -> Self {
        Self::new_unchecked(-self.s())
    }
}
impl<T: Float> Antireverse for Trivector<T> {
    #[inline]
    fn antireverse(&self) -> Self {
        Self::new_unchecked(self.ps())
    }
}
impl<T: Float> Involute for Flector<T> {
    #[inline]
    fn involute(&self) -> Self {
        Self::new_unchecked(self.s(), -self.d(), -self.nx(), -self.ny())
    }
}
impl<T: Float> Involute for Line<T> {
    #[inline]
    fn involute(&self) -> Self {
        Self::new_unchecked(-self.d(), -self.nx(), -self.ny())
    }
}
impl<T: Float> Involute for Motor<T> {
    #[inline]
    fn involute(&self) -> Self {
        Self::new_unchecked(self.ty(), self.tx(), self.r(), -self.ps())
    }
}
impl<T: Float> Involute for Point<T> {
    #[inline]
    fn involute(&self) -> Self {
        Self::new_unchecked(self.x(), self.y(), self.w())
    }
}
impl<T: Float> Involute for Scalar<T> {
    #[inline]
    fn involute(&self) -> Self {
        Self::new_unchecked(self.s())
    }
}
impl<T: Float> Involute for Trivector<T> {
    #[inline]
    fn involute(&self) -> Self {
        Self::new_unchecked(-self.ps())
    }
}
impl<T: Float> RightComplement for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn right_complement(&self) -> Motor<T> {
        Motor::new_unchecked(self.nx(), -self.ny(), self.d(), self.s())
    }
}
impl<T: Float> RightComplement for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn right_complement(&self) -> Point<T> {
        Point::new_unchecked(self.nx(), -self.ny(), self.d())
    }
}
impl<T: Float> RightComplement for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn right_complement(&self) -> Flector<T> {
        Flector::new_unchecked(self.ps(), self.r(), self.ty(), -self.tx())
    }
}
impl<T: Float> RightComplement for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn right_complement(&self) -> Line<T> {
        Line::new_unchecked(self.w(), self.x(), -self.y())
    }
}
impl<T: Float> RightComplement for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn right_complement(&self) -> Trivector<T> {
        Trivector::new_unchecked(self.s())
    }
}
impl<T: Float> RightComplement for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn right_complement(&self) -> Scalar<T> {
        Scalar::new_unchecked(self.ps())
    }
}
impl<T: Float> WeightDual for Flector<T> {
    type Output = Motor<T>;
    #[inline]
    fn weight_dual(&self) -> Motor<T> {
        Motor::new_unchecked(-self.nx(), self.ny(), T::zero(), T::zero())
    }
}
impl<T: Float> WeightDual for Line<T> {
    type Output = Point<T>;
    #[inline]
    fn weight_dual(&self) -> Point<T> {
        Point::new_unchecked(-self.nx(), self.ny(), T::zero())
    }
}
impl<T: Float> WeightDual for Motor<T> {
    type Output = Flector<T>;
    #[inline]
    fn weight_dual(&self) -> Flector<T> {
        Flector::new_unchecked(-self.ps(), -self.r(), T::zero(), T::zero())
    }
}
impl<T: Float> WeightDual for Point<T> {
    type Output = Line<T>;
    #[inline]
    fn weight_dual(&self) -> Line<T> {
        Line::new_unchecked(-self.w(), T::zero(), T::zero())
    }
}
impl<T: Float> WeightDual for Scalar<T> {
    type Output = Trivector<T>;
    #[inline]
    fn weight_dual(&self) -> Trivector<T> {
        Trivector::new_unchecked(T::zero())
    }
}
impl<T: Float> WeightDual for Trivector<T> {
    type Output = Scalar<T>;
    #[inline]
    fn weight_dual(&self) -> Scalar<T> {
        Scalar::new_unchecked(-self.ps())
    }
}
impl<T: Float> VersorInverse for Flector<T> {
    fn try_inverse(&self) -> Option<Self> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Self::new_unchecked(
            self.s() * inv_norm_sq,
            -self.d() * inv_norm_sq,
            -self.nx() * inv_norm_sq,
            -self.ny() * inv_norm_sq,
        ))
    }
}
impl<T: Float> VersorInverse for Line<T> {
    fn try_inverse(&self) -> Option<Self> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Self::new_unchecked(
            -self.d() * inv_norm_sq,
            -self.nx() * inv_norm_sq,
            -self.ny() * inv_norm_sq,
        ))
    }
}
impl<T: Float> VersorInverse for Motor<T> {
    fn try_inverse(&self) -> Option<Self> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Self::new_unchecked(
            self.ty() * inv_norm_sq,
            self.tx() * inv_norm_sq,
            self.r() * inv_norm_sq,
            -self.ps() * inv_norm_sq,
        ))
    }
}
impl<T: Float> VersorInverse for Point<T> {
    fn try_inverse(&self) -> Option<Self> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Self::new_unchecked(
            self.x() * inv_norm_sq,
            self.y() * inv_norm_sq,
            self.w() * inv_norm_sq,
        ))
    }
}
impl<T: Float> VersorInverse for Scalar<T> {
    fn try_inverse(&self) -> Option<Self> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Self::new_unchecked(self.s() * inv_norm_sq))
    }
}
impl<T: Float> VersorInverse for Trivector<T> {
    fn try_inverse(&self) -> Option<Self> {
        let norm_sq = <Self as crate::norm::DegenerateNormed>::bulk_norm_squared(self);
        if norm_sq.abs() < T::epsilon() {
            return None;
        }
        let inv_norm_sq = T::one() / norm_sq;
        Some(Self::new_unchecked(-self.ps() * inv_norm_sq))
    }
}
impl<T: Float> crate::norm::Normed for Flector<T> {
    type Scalar = T;
    #[inline]
    fn norm_squared(&self) -> T {
        self.s() * self.s() + self.d() * self.d()
    }
    fn try_normalize(&self) -> Option<Self> {
        let n = self.norm();
        if n < T::epsilon() {
            None
        } else {
            Some(self.scale(T::one() / n))
        }
    }
    #[inline]
    fn scale(&self, factor: T) -> Self {
        Self::new_unchecked(
            self.s() * factor,
            self.d() * factor,
            self.nx() * factor,
            self.ny() * factor,
        )
    }
}
impl<T: Float> crate::norm::Normed for Line<T> {
    type Scalar = T;
    #[inline]
    fn norm_squared(&self) -> T {
        self.d() * self.d()
    }
    fn try_normalize(&self) -> Option<Self> {
        let n = self.norm();
        if n < T::epsilon() {
            None
        } else {
            Some(self.scale(T::one() / n))
        }
    }
    #[inline]
    fn scale(&self, factor: T) -> Self {
        Self::new_unchecked(self.d() * factor, self.nx() * factor, self.ny() * factor)
    }
}
impl<T: Float> crate::norm::Normed for Motor<T> {
    type Scalar = T;
    #[inline]
    fn norm_squared(&self) -> T {
        self.ty() * self.ty() + self.tx() * self.tx()
    }
    fn try_normalize(&self) -> Option<Self> {
        let n = self.norm();
        if n < T::epsilon() {
            None
        } else {
            Some(self.scale(T::one() / n))
        }
    }
    #[inline]
    fn scale(&self, factor: T) -> Self {
        Self::new_unchecked(
            self.ty() * factor,
            self.tx() * factor,
            self.r() * factor,
            self.ps() * factor,
        )
    }
}
impl<T: Float> crate::norm::Normed for Point<T> {
    type Scalar = T;
    #[inline]
    fn norm_squared(&self) -> T {
        self.x() * self.x() + self.y() * self.y()
    }
    fn try_normalize(&self) -> Option<Self> {
        let n = self.norm();
        if n < T::epsilon() {
            None
        } else {
            Some(self.scale(T::one() / n))
        }
    }
    #[inline]
    fn scale(&self, factor: T) -> Self {
        Self::new_unchecked(self.x() * factor, self.y() * factor, self.w() * factor)
    }
}
impl<T: Float> crate::norm::Normed for Scalar<T> {
    type Scalar = T;
    #[inline]
    fn norm_squared(&self) -> T {
        self.s() * self.s()
    }
    fn try_normalize(&self) -> Option<Self> {
        let n = self.norm();
        if n < T::epsilon() {
            None
        } else {
            Some(self.scale(T::one() / n))
        }
    }
    #[inline]
    fn scale(&self, factor: T) -> Self {
        Self::new_unchecked(self.s() * factor)
    }
}
impl<T: Float> crate::norm::Normed for Trivector<T> {
    type Scalar = T;
    #[inline]
    fn norm_squared(&self) -> T {
        T::zero()
    }
    fn try_normalize(&self) -> Option<Self> {
        let n = self.norm();
        if n < T::epsilon() {
            None
        } else {
            Some(self.scale(T::one() / n))
        }
    }
    #[inline]
    fn scale(&self, factor: T) -> Self {
        Self::new_unchecked(self.ps() * factor)
    }
}
impl<T: Float> crate::norm::DegenerateNormed for Flector<T> {
    #[inline]
    fn bulk_norm_squared(&self) -> T {
        self.s() * self.s() + self.d() * self.d()
    }
    #[inline]
    fn weight_norm_squared(&self) -> T {
        self.nx() * self.nx() + self.ny() * self.ny()
    }
    fn try_unitize(&self) -> Option<Self> {
        let w = self.weight_norm();
        if w < T::epsilon() {
            None
        } else {
            let inv_w = T::one() / w;
            Some(Self::new_unchecked(
                self.s() * inv_w,
                self.d() * inv_w,
                self.nx() * inv_w,
                self.ny() * inv_w,
            ))
        }
    }
}
impl<T: Float> crate::norm::DegenerateNormed for Line<T> {
    #[inline]
    fn bulk_norm_squared(&self) -> T {
        self.d() * self.d()
    }
    #[inline]
    fn weight_norm_squared(&self) -> T {
        self.nx() * self.nx() + self.ny() * self.ny()
    }
    fn try_unitize(&self) -> Option<Self> {
        let w = self.weight_norm();
        if w < T::epsilon() {
            None
        } else {
            let inv_w = T::one() / w;
            Some(Self::new_unchecked(
                self.d() * inv_w,
                self.nx() * inv_w,
                self.ny() * inv_w,
            ))
        }
    }
}
impl<T: Float> crate::norm::DegenerateNormed for Motor<T> {
    #[inline]
    fn bulk_norm_squared(&self) -> T {
        self.ty() * self.ty() + self.tx() * self.tx()
    }
    #[inline]
    fn weight_norm_squared(&self) -> T {
        self.r() * self.r() + self.ps() * self.ps()
    }
    fn try_unitize(&self) -> Option<Self> {
        let w = self.weight_norm();
        if w < T::epsilon() {
            None
        } else {
            let inv_w = T::one() / w;
            Some(Self::new_unchecked(
                self.ty() * inv_w,
                self.tx() * inv_w,
                self.r() * inv_w,
                self.ps() * inv_w,
            ))
        }
    }
}
impl<T: Float> crate::norm::DegenerateNormed for Point<T> {
    #[inline]
    fn bulk_norm_squared(&self) -> T {
        self.x() * self.x() + self.y() * self.y()
    }
    #[inline]
    fn weight_norm_squared(&self) -> T {
        self.w() * self.w()
    }
    fn try_unitize(&self) -> Option<Self> {
        let w = self.weight_norm();
        if w < T::epsilon() {
            None
        } else {
            let inv_w = T::one() / w;
            Some(Self::new_unchecked(
                self.x() * inv_w,
                self.y() * inv_w,
                self.w() * inv_w,
            ))
        }
    }
}
impl<T: Float> crate::norm::DegenerateNormed for Scalar<T> {
    #[inline]
    fn bulk_norm_squared(&self) -> T {
        self.s() * self.s()
    }
    #[inline]
    fn weight_norm_squared(&self) -> T {
        T::zero()
    }
    fn try_unitize(&self) -> Option<Self> {
        let w = self.weight_norm();
        if w < T::epsilon() {
            None
        } else {
            let inv_w = T::one() / w;
            Some(Self::new_unchecked(self.s() * inv_w))
        }
    }
}
impl<T: Float> crate::norm::DegenerateNormed for Trivector<T> {
    #[inline]
    fn bulk_norm_squared(&self) -> T {
        T::zero()
    }
    #[inline]
    fn weight_norm_squared(&self) -> T {
        self.ps() * self.ps()
    }
    fn try_unitize(&self) -> Option<Self> {
        let w = self.weight_norm();
        if w < T::epsilon() {
            None
        } else {
            let inv_w = T::one() / w;
            Some(Self::new_unchecked(self.ps() * inv_w))
        }
    }
}
impl<T: Float + AbsDiffEq<Epsilon = T>> AbsDiffEq for Flector<T> {
    type Epsilon = T;
    fn default_epsilon() -> Self::Epsilon {
        T::default_epsilon()
    }
    fn abs_diff_eq(&self, other: &Self, epsilon: Self::Epsilon) -> bool {
        self.s().abs_diff_eq(&other.s(), epsilon)
            && self.d().abs_diff_eq(&other.d(), epsilon)
            && self.nx().abs_diff_eq(&other.nx(), epsilon)
            && self.ny().abs_diff_eq(&other.ny(), epsilon)
    }
}
impl<T: Float + RelativeEq<Epsilon = T>> RelativeEq for Flector<T> {
    fn default_max_relative() -> Self::Epsilon {
        T::default_max_relative()
    }
    fn relative_eq(
        &self,
        other: &Self,
        epsilon: Self::Epsilon,
        max_relative: Self::Epsilon,
    ) -> bool {
        self.s().relative_eq(&other.s(), epsilon, max_relative)
            && self.d().relative_eq(&other.d(), epsilon, max_relative)
            && self.nx().relative_eq(&other.nx(), epsilon, max_relative)
            && self.ny().relative_eq(&other.ny(), epsilon, max_relative)
    }
}
impl<T: Float + UlpsEq<Epsilon = T>> UlpsEq for Flector<T> {
    fn default_max_ulps() -> u32 {
        T::default_max_ulps()
    }
    fn ulps_eq(&self, other: &Self, epsilon: Self::Epsilon, max_ulps: u32) -> bool {
        self.s().ulps_eq(&other.s(), epsilon, max_ulps)
            && self.d().ulps_eq(&other.d(), epsilon, max_ulps)
            && self.nx().ulps_eq(&other.nx(), epsilon, max_ulps)
            && self.ny().ulps_eq(&other.ny(), epsilon, max_ulps)
    }
}
impl<T: Float + AbsDiffEq<Epsilon = T>> AbsDiffEq for Line<T> {
    type Epsilon = T;
    fn default_epsilon() -> Self::Epsilon {
        T::default_epsilon()
    }
    fn abs_diff_eq(&self, other: &Self, epsilon: Self::Epsilon) -> bool {
        self.d().abs_diff_eq(&other.d(), epsilon)
            && self.nx().abs_diff_eq(&other.nx(), epsilon)
            && self.ny().abs_diff_eq(&other.ny(), epsilon)
    }
}
impl<T: Float + RelativeEq<Epsilon = T>> RelativeEq for Line<T> {
    fn default_max_relative() -> Self::Epsilon {
        T::default_max_relative()
    }
    fn relative_eq(
        &self,
        other: &Self,
        epsilon: Self::Epsilon,
        max_relative: Self::Epsilon,
    ) -> bool {
        self.d().relative_eq(&other.d(), epsilon, max_relative)
            && self.nx().relative_eq(&other.nx(), epsilon, max_relative)
            && self.ny().relative_eq(&other.ny(), epsilon, max_relative)
    }
}
impl<T: Float + UlpsEq<Epsilon = T>> UlpsEq for Line<T> {
    fn default_max_ulps() -> u32 {
        T::default_max_ulps()
    }
    fn ulps_eq(&self, other: &Self, epsilon: Self::Epsilon, max_ulps: u32) -> bool {
        self.d().ulps_eq(&other.d(), epsilon, max_ulps)
            && self.nx().ulps_eq(&other.nx(), epsilon, max_ulps)
            && self.ny().ulps_eq(&other.ny(), epsilon, max_ulps)
    }
}
impl<T: Float + AbsDiffEq<Epsilon = T>> AbsDiffEq for Motor<T> {
    type Epsilon = T;
    fn default_epsilon() -> Self::Epsilon {
        T::default_epsilon()
    }
    fn abs_diff_eq(&self, other: &Self, epsilon: Self::Epsilon) -> bool {
        self.ty().abs_diff_eq(&other.ty(), epsilon)
            && self.tx().abs_diff_eq(&other.tx(), epsilon)
            && self.r().abs_diff_eq(&other.r(), epsilon)
            && self.ps().abs_diff_eq(&other.ps(), epsilon)
    }
}
impl<T: Float + RelativeEq<Epsilon = T>> RelativeEq for Motor<T> {
    fn default_max_relative() -> Self::Epsilon {
        T::default_max_relative()
    }
    fn relative_eq(
        &self,
        other: &Self,
        epsilon: Self::Epsilon,
        max_relative: Self::Epsilon,
    ) -> bool {
        self.ty().relative_eq(&other.ty(), epsilon, max_relative)
            && self.tx().relative_eq(&other.tx(), epsilon, max_relative)
            && self.r().relative_eq(&other.r(), epsilon, max_relative)
            && self.ps().relative_eq(&other.ps(), epsilon, max_relative)
    }
}
impl<T: Float + UlpsEq<Epsilon = T>> UlpsEq for Motor<T> {
    fn default_max_ulps() -> u32 {
        T::default_max_ulps()
    }
    fn ulps_eq(&self, other: &Self, epsilon: Self::Epsilon, max_ulps: u32) -> bool {
        self.ty().ulps_eq(&other.ty(), epsilon, max_ulps)
            && self.tx().ulps_eq(&other.tx(), epsilon, max_ulps)
            && self.r().ulps_eq(&other.r(), epsilon, max_ulps)
            && self.ps().ulps_eq(&other.ps(), epsilon, max_ulps)
    }
}
impl<T: Float + AbsDiffEq<Epsilon = T>> AbsDiffEq for Point<T> {
    type Epsilon = T;
    fn default_epsilon() -> Self::Epsilon {
        T::default_epsilon()
    }
    fn abs_diff_eq(&self, other: &Self, epsilon: Self::Epsilon) -> bool {
        self.x().abs_diff_eq(&other.x(), epsilon)
            && self.y().abs_diff_eq(&other.y(), epsilon)
            && self.w().abs_diff_eq(&other.w(), epsilon)
    }
}
impl<T: Float + RelativeEq<Epsilon = T>> RelativeEq for Point<T> {
    fn default_max_relative() -> Self::Epsilon {
        T::default_max_relative()
    }
    fn relative_eq(
        &self,
        other: &Self,
        epsilon: Self::Epsilon,
        max_relative: Self::Epsilon,
    ) -> bool {
        self.x().relative_eq(&other.x(), epsilon, max_relative)
            && self.y().relative_eq(&other.y(), epsilon, max_relative)
            && self.w().relative_eq(&other.w(), epsilon, max_relative)
    }
}
impl<T: Float + UlpsEq<Epsilon = T>> UlpsEq for Point<T> {
    fn default_max_ulps() -> u32 {
        T::default_max_ulps()
    }
    fn ulps_eq(&self, other: &Self, epsilon: Self::Epsilon, max_ulps: u32) -> bool {
        self.x().ulps_eq(&other.x(), epsilon, max_ulps)
            && self.y().ulps_eq(&other.y(), epsilon, max_ulps)
            && self.w().ulps_eq(&other.w(), epsilon, max_ulps)
    }
}
impl<T: Float + AbsDiffEq<Epsilon = T>> AbsDiffEq for Scalar<T> {
    type Epsilon = T;
    fn default_epsilon() -> Self::Epsilon {
        T::default_epsilon()
    }
    fn abs_diff_eq(&self, other: &Self, epsilon: Self::Epsilon) -> bool {
        self.s().abs_diff_eq(&other.s(), epsilon)
    }
}
impl<T: Float + RelativeEq<Epsilon = T>> RelativeEq for Scalar<T> {
    fn default_max_relative() -> Self::Epsilon {
        T::default_max_relative()
    }
    fn relative_eq(
        &self,
        other: &Self,
        epsilon: Self::Epsilon,
        max_relative: Self::Epsilon,
    ) -> bool {
        self.s().relative_eq(&other.s(), epsilon, max_relative)
    }
}
impl<T: Float + UlpsEq<Epsilon = T>> UlpsEq for Scalar<T> {
    fn default_max_ulps() -> u32 {
        T::default_max_ulps()
    }
    fn ulps_eq(&self, other: &Self, epsilon: Self::Epsilon, max_ulps: u32) -> bool {
        self.s().ulps_eq(&other.s(), epsilon, max_ulps)
    }
}
impl<T: Float + AbsDiffEq<Epsilon = T>> AbsDiffEq for Trivector<T> {
    type Epsilon = T;
    fn default_epsilon() -> Self::Epsilon {
        T::default_epsilon()
    }
    fn abs_diff_eq(&self, other: &Self, epsilon: Self::Epsilon) -> bool {
        self.ps().abs_diff_eq(&other.ps(), epsilon)
    }
}
impl<T: Float + RelativeEq<Epsilon = T>> RelativeEq for Trivector<T> {
    fn default_max_relative() -> Self::Epsilon {
        T::default_max_relative()
    }
    fn relative_eq(
        &self,
        other: &Self,
        epsilon: Self::Epsilon,
        max_relative: Self::Epsilon,
    ) -> bool {
        self.ps().relative_eq(&other.ps(), epsilon, max_relative)
    }
}
impl<T: Float + UlpsEq<Epsilon = T>> UlpsEq for Trivector<T> {
    fn default_max_ulps() -> u32 {
        T::default_max_ulps()
    }
    fn ulps_eq(&self, other: &Self, epsilon: Self::Epsilon, max_ulps: u32) -> bool {
        self.ps().ulps_eq(&other.ps(), epsilon, max_ulps)
    }
}
#[cfg(any(test, feature = "proptest-support"))]
#[allow(clippy::missing_docs_in_private_items)]
mod arbitrary_impls {
    use super::*;
    use proptest::prelude::*;
    use proptest::strategy::BoxedStrategy;
    use std::fmt::Debug;
    impl<T: Float + Debug + 'static> Arbitrary for Flector<T> {
        type Parameters = ();
        type Strategy = BoxedStrategy<Self>;
        fn arbitrary_with(_: Self::Parameters) -> Self::Strategy {
            (
                -100.0f64..100.0,
                -100.0f64..100.0,
                -100.0f64..100.0,
                -100.0f64..100.0,
            )
                .prop_map(|(x0, x1, x2, x3)| {
                    Flector::new_unchecked(
                        T::from_f64(x0),
                        T::from_f64(x1),
                        T::from_f64(x2),
                        T::from_f64(x3),
                    )
                })
                .boxed()
        }
    }
    impl<T: Float + Debug + 'static> Arbitrary for Line<T> {
        type Parameters = ();
        type Strategy = BoxedStrategy<Self>;
        fn arbitrary_with(_: Self::Parameters) -> Self::Strategy {
            (-100.0f64..100.0, -100.0f64..100.0, -100.0f64..100.0)
                .prop_map(|(x0, x1, x2)| {
                    Line::new_unchecked(T::from_f64(x0), T::from_f64(x1), T::from_f64(x2))
                })
                .boxed()
        }
    }
    impl<T: Float + Debug + 'static> Arbitrary for Motor<T> {
        type Parameters = ();
        type Strategy = BoxedStrategy<Self>;
        fn arbitrary_with(_: Self::Parameters) -> Self::Strategy {
            (
                -100.0f64..100.0,
                -100.0f64..100.0,
                -100.0f64..100.0,
                -100.0f64..100.0,
            )
                .prop_map(|(x0, x1, x2, x3)| {
                    Motor::new_unchecked(
                        T::from_f64(x0),
                        T::from_f64(x1),
                        T::from_f64(x2),
                        T::from_f64(x3),
                    )
                })
                .boxed()
        }
    }
    impl<T: Float + Debug + 'static> Arbitrary for Point<T> {
        type Parameters = ();
        type Strategy = BoxedStrategy<Self>;
        fn arbitrary_with(_: Self::Parameters) -> Self::Strategy {
            (-100.0f64..100.0, -100.0f64..100.0, -100.0f64..100.0)
                .prop_map(|(x0, x1, x2)| {
                    Point::new_unchecked(T::from_f64(x0), T::from_f64(x1), T::from_f64(x2))
                })
                .boxed()
        }
    }
    impl<T: Float + Debug + 'static> Arbitrary for Scalar<T> {
        type Parameters = ();
        type Strategy = BoxedStrategy<Self>;
        fn arbitrary_with(_: Self::Parameters) -> Self::Strategy {
            (-100.0f64..100.0)
                .prop_map(|x0| Scalar::new_unchecked(T::from_f64(x0)))
                .boxed()
        }
    }
    impl<T: Float + Debug + 'static> Arbitrary for Trivector<T> {
        type Parameters = ();
        type Strategy = BoxedStrategy<Self>;
        fn arbitrary_with(_: Self::Parameters) -> Self::Strategy {
            (-100.0f64..100.0)
                .prop_map(|x0| Trivector::new_unchecked(T::from_f64(x0)))
                .boxed()
        }
    }
}

// ============================================================
// Verification Tests (compare against Multivector)
// ============================================================

#[cfg(test)]
#[allow(clippy::missing_docs_in_private_items)]
mod verification_tests {
    use super::*;
    use crate::algebra::Multivector;
    #[allow(unused_imports)]
    use crate::norm::{DegenerateNormed, Normed};
    use crate::signature::Projective2;
    #[allow(unused_imports)]
    use crate::wrappers::{Bulk, Unit, Unitized};
    use approx::relative_eq;
    use proptest::prelude::*;

    /// Relative epsilon for floating-point comparisons in verification tests.
    /// Using relative comparison handles varying magnitudes better than absolute.
    const REL_EPSILON: f64 = 1e-10;

    proptest! {
        #[test]
        fn flector_add_matches_multivector(a in any::<Flector<f64>>(), b in any::<Flector<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a + b;
            let generic_result = mv_a + mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Add mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn flector_sub_matches_multivector(a in any::<Flector<f64>>(), b in any::<Flector<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a - b;
            let generic_result = mv_a - mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Sub mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn flector_neg_matches_multivector(a in any::<Flector<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();

            let specialized_result = -a;
            let generic_result = -mv_a;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Neg mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn line_add_matches_multivector(a in any::<Line<f64>>(), b in any::<Line<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a + b;
            let generic_result = mv_a + mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Add mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn line_sub_matches_multivector(a in any::<Line<f64>>(), b in any::<Line<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a - b;
            let generic_result = mv_a - mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Sub mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn line_neg_matches_multivector(a in any::<Line<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();

            let specialized_result = -a;
            let generic_result = -mv_a;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Neg mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn motor_add_matches_multivector(a in any::<Motor<f64>>(), b in any::<Motor<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a + b;
            let generic_result = mv_a + mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Add mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn motor_sub_matches_multivector(a in any::<Motor<f64>>(), b in any::<Motor<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a - b;
            let generic_result = mv_a - mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Sub mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn motor_neg_matches_multivector(a in any::<Motor<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();

            let specialized_result = -a;
            let generic_result = -mv_a;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Neg mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn point_add_matches_multivector(a in any::<Point<f64>>(), b in any::<Point<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a + b;
            let generic_result = mv_a + mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Add mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn point_sub_matches_multivector(a in any::<Point<f64>>(), b in any::<Point<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a - b;
            let generic_result = mv_a - mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Sub mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn point_neg_matches_multivector(a in any::<Point<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();

            let specialized_result = -a;
            let generic_result = -mv_a;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Neg mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn scalar_add_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Scalar<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a + b;
            let generic_result = mv_a + mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Add mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn scalar_sub_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Scalar<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a - b;
            let generic_result = mv_a - mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Sub mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn scalar_neg_matches_multivector(a in any::<Scalar<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();

            let specialized_result = -a;
            let generic_result = -mv_a;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Neg mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn trivector_add_matches_multivector(a in any::<Trivector<f64>>(), b in any::<Trivector<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a + b;
            let generic_result = mv_a + mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Add mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn trivector_sub_matches_multivector(a in any::<Trivector<f64>>(), b in any::<Trivector<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result = a - b;
            let generic_result = mv_a - mv_b;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Sub mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }

        #[test]
        fn trivector_neg_matches_multivector(a in any::<Trivector<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();

            let specialized_result = -a;
            let generic_result = -mv_a;

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Neg mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn wedge_line_point_trivector_matches_multivector(a in any::<Line<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::Wedge;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Trivector<f64> = a.wedge(&b);
            let generic_result = mv_a.exterior(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wedge product mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn wedge_line_scalar_line_matches_multivector(a in any::<Line<f64>>(), b in any::<Scalar<f64>>()) {
            use crate::ops::Wedge;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Line<f64> = a.wedge(&b);
            let generic_result = mv_a.exterior(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wedge product mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn wedge_point_line_trivector_matches_multivector(a in any::<Point<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::Wedge;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Trivector<f64> = a.wedge(&b);
            let generic_result = mv_a.exterior(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wedge product mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn wedge_point_point_line_matches_multivector(a in any::<Point<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::Wedge;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Line<f64> = a.wedge(&b);
            let generic_result = mv_a.exterior(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wedge product mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn wedge_point_scalar_point_matches_multivector(a in any::<Point<f64>>(), b in any::<Scalar<f64>>()) {
            use crate::ops::Wedge;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Point<f64> = a.wedge(&b);
            let generic_result = mv_a.exterior(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wedge product mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn wedge_scalar_line_line_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::Wedge;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Line<f64> = a.wedge(&b);
            let generic_result = mv_a.exterior(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wedge product mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn wedge_scalar_point_point_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::Wedge;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Point<f64> = a.wedge(&b);
            let generic_result = mv_a.exterior(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wedge product mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn wedge_scalar_scalar_scalar_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Scalar<f64>>()) {
            use crate::ops::Wedge;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Scalar<f64> = a.wedge(&b);
            let generic_result = mv_a.exterior(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wedge product mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn wedge_scalar_trivector_trivector_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Trivector<f64>>()) {
            use crate::ops::Wedge;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Trivector<f64> = a.wedge(&b);
            let generic_result = mv_a.exterior(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wedge product mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn wedge_trivector_scalar_trivector_matches_multivector(a in any::<Trivector<f64>>(), b in any::<Scalar<f64>>()) {
            use crate::ops::Wedge;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Trivector<f64> = a.wedge(&b);
            let generic_result = mv_a.exterior(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wedge product mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_contraction_line_line_scalar_matches_multivector(a in any::<Line<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::BulkContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Scalar<f64> = a.bulk_contract(&b);
            let generic_result = mv_a.bulk_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_contraction_line_point_point_matches_multivector(a in any::<Line<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::BulkContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Point<f64> = a.bulk_contract(&b);
            let generic_result = mv_a.bulk_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_contraction_line_scalar_line_matches_multivector(a in any::<Line<f64>>(), b in any::<Scalar<f64>>()) {
            use crate::ops::BulkContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Line<f64> = a.bulk_contract(&b);
            let generic_result = mv_a.bulk_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_contraction_point_point_scalar_matches_multivector(a in any::<Point<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::BulkContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Scalar<f64> = a.bulk_contract(&b);
            let generic_result = mv_a.bulk_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_contraction_point_scalar_point_matches_multivector(a in any::<Point<f64>>(), b in any::<Scalar<f64>>()) {
            use crate::ops::BulkContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Point<f64> = a.bulk_contract(&b);
            let generic_result = mv_a.bulk_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_contraction_scalar_scalar_scalar_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Scalar<f64>>()) {
            use crate::ops::BulkContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Scalar<f64> = a.bulk_contract(&b);
            let generic_result = mv_a.bulk_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_contraction_trivector_line_point_matches_multivector(a in any::<Trivector<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::BulkContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Point<f64> = a.bulk_contract(&b);
            let generic_result = mv_a.bulk_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_contraction_trivector_point_line_matches_multivector(a in any::<Trivector<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::BulkContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Line<f64> = a.bulk_contract(&b);
            let generic_result = mv_a.bulk_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_contraction_trivector_scalar_trivector_matches_multivector(a in any::<Trivector<f64>>(), b in any::<Scalar<f64>>()) {
            use crate::ops::BulkContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Trivector<f64> = a.bulk_contract(&b);
            let generic_result = mv_a.bulk_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_contraction_line_line_scalar_matches_multivector(a in any::<Line<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::WeightContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Scalar<f64> = a.weight_contract(&b);
            let generic_result = mv_a.weight_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_contraction_line_point_point_matches_multivector(a in any::<Line<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::WeightContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Point<f64> = a.weight_contract(&b);
            let generic_result = mv_a.weight_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_contraction_point_point_scalar_matches_multivector(a in any::<Point<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::WeightContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Scalar<f64> = a.weight_contract(&b);
            let generic_result = mv_a.weight_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_contraction_trivector_line_point_matches_multivector(a in any::<Trivector<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::WeightContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Point<f64> = a.weight_contract(&b);
            let generic_result = mv_a.weight_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_contraction_trivector_point_line_matches_multivector(a in any::<Trivector<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::WeightContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Line<f64> = a.weight_contract(&b);
            let generic_result = mv_a.weight_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_contraction_trivector_trivector_scalar_matches_multivector(a in any::<Trivector<f64>>(), b in any::<Trivector<f64>>()) {
            use crate::ops::WeightContract;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Scalar<f64> = a.weight_contract(&b);
            let generic_result = mv_a.weight_contraction(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight contraction mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_expansion_line_line_trivector_matches_multivector(a in any::<Line<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::BulkExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Trivector<f64> = a.bulk_expand(&b);
            let generic_result = mv_a.bulk_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_expansion_point_line_line_matches_multivector(a in any::<Point<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::BulkExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Line<f64> = a.bulk_expand(&b);
            let generic_result = mv_a.bulk_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_expansion_point_point_trivector_matches_multivector(a in any::<Point<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::BulkExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Trivector<f64> = a.bulk_expand(&b);
            let generic_result = mv_a.bulk_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_expansion_scalar_line_point_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::BulkExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Point<f64> = a.bulk_expand(&b);
            let generic_result = mv_a.bulk_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_expansion_scalar_point_line_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::BulkExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Line<f64> = a.bulk_expand(&b);
            let generic_result = mv_a.bulk_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn bulk_expansion_scalar_scalar_trivector_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Scalar<f64>>()) {
            use crate::ops::BulkExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Trivector<f64> = a.bulk_expand(&b);
            let generic_result = mv_a.bulk_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Bulk expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_expansion_line_line_trivector_matches_multivector(a in any::<Line<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::WeightExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Trivector<f64> = a.weight_expand(&b);
            let generic_result = mv_a.weight_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_expansion_line_trivector_line_matches_multivector(a in any::<Line<f64>>(), b in any::<Trivector<f64>>()) {
            use crate::ops::WeightExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Line<f64> = a.weight_expand(&b);
            let generic_result = mv_a.weight_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_expansion_point_line_line_matches_multivector(a in any::<Point<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::WeightExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Line<f64> = a.weight_expand(&b);
            let generic_result = mv_a.weight_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_expansion_point_point_trivector_matches_multivector(a in any::<Point<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::WeightExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Trivector<f64> = a.weight_expand(&b);
            let generic_result = mv_a.weight_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_expansion_point_trivector_point_matches_multivector(a in any::<Point<f64>>(), b in any::<Trivector<f64>>()) {
            use crate::ops::WeightExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Point<f64> = a.weight_expand(&b);
            let generic_result = mv_a.weight_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_expansion_scalar_line_point_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Line<f64>>()) {
            use crate::ops::WeightExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Point<f64> = a.weight_expand(&b);
            let generic_result = mv_a.weight_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_expansion_scalar_point_line_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Point<f64>>()) {
            use crate::ops::WeightExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Line<f64> = a.weight_expand(&b);
            let generic_result = mv_a.weight_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_expansion_scalar_trivector_scalar_matches_multivector(a in any::<Scalar<f64>>(), b in any::<Trivector<f64>>()) {
            use crate::ops::WeightExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Scalar<f64> = a.weight_expand(&b);
            let generic_result = mv_a.weight_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        #[test]
        fn weight_expansion_trivector_trivector_trivector_matches_multivector(a in any::<Trivector<f64>>(), b in any::<Trivector<f64>>()) {
            use crate::ops::WeightExpand;
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            let specialized_result: Trivector<f64> = a.weight_expand(&b);
            let generic_result = mv_a.weight_expansion(&mv_b);

            let specialized_mv: Multivector<f64, Projective2> = specialized_result.into();
            prop_assert!(
                relative_eq!(specialized_mv, generic_result, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight expansion mismatch: specialized={:?}, generic={:?}",
                specialized_mv, generic_result
            );
        }
    }

    proptest! {
        /// De Morgan: complement(a * b) = complement(a) ⋇ complement(b)
        #[test]
        fn de_morgan_geometric_line(a in any::<Line<f64>>(), b in any::<Line<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            // LHS: complement(a * b)
            let lhs = (mv_a * mv_b).complement();

            // RHS: complement(a) ⋇ complement(b)
            let rhs = mv_a.complement().antiproduct(&mv_b.complement());

            prop_assert!(
                relative_eq!(lhs, rhs, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "De Morgan (geometric) failed: complement(a*b)={:?}, complement(a)⋇complement(b)={:?}",
                lhs, rhs
            );
        }

        /// De Morgan: complement(a ⋇ b) = complement(a) * complement(b)
        #[test]
        fn de_morgan_antiproduct_line(a in any::<Line<f64>>(), b in any::<Line<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            // LHS: complement(a ⋇ b)
            let lhs = mv_a.antiproduct(&mv_b).complement();

            // RHS: complement(a) * complement(b)
            let rhs = mv_a.complement() * mv_b.complement();

            prop_assert!(
                relative_eq!(lhs, rhs, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "De Morgan (antiproduct) failed: complement(a⋇b)={:?}, complement(a)*complement(b)={:?}",
                lhs, rhs
            );
        }
    }

    proptest! {
        /// De Morgan: complement(a * b) = complement(a) ⋇ complement(b)
        #[test]
        fn de_morgan_geometric_point(a in any::<Point<f64>>(), b in any::<Point<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            // LHS: complement(a * b)
            let lhs = (mv_a * mv_b).complement();

            // RHS: complement(a) ⋇ complement(b)
            let rhs = mv_a.complement().antiproduct(&mv_b.complement());

            prop_assert!(
                relative_eq!(lhs, rhs, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "De Morgan (geometric) failed: complement(a*b)={:?}, complement(a)⋇complement(b)={:?}",
                lhs, rhs
            );
        }

        /// De Morgan: complement(a ⋇ b) = complement(a) * complement(b)
        #[test]
        fn de_morgan_antiproduct_point(a in any::<Point<f64>>(), b in any::<Point<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            // LHS: complement(a ⋇ b)
            let lhs = mv_a.antiproduct(&mv_b).complement();

            // RHS: complement(a) * complement(b)
            let rhs = mv_a.complement() * mv_b.complement();

            prop_assert!(
                relative_eq!(lhs, rhs, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "De Morgan (antiproduct) failed: complement(a⋇b)={:?}, complement(a)*complement(b)={:?}",
                lhs, rhs
            );
        }
    }

    proptest! {
        /// De Morgan: complement(a * b) = complement(a) ⋇ complement(b)
        #[test]
        fn de_morgan_geometric_scalar(a in any::<Scalar<f64>>(), b in any::<Scalar<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            // LHS: complement(a * b)
            let lhs = (mv_a * mv_b).complement();

            // RHS: complement(a) ⋇ complement(b)
            let rhs = mv_a.complement().antiproduct(&mv_b.complement());

            prop_assert!(
                relative_eq!(lhs, rhs, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "De Morgan (geometric) failed: complement(a*b)={:?}, complement(a)⋇complement(b)={:?}",
                lhs, rhs
            );
        }

        /// De Morgan: complement(a ⋇ b) = complement(a) * complement(b)
        #[test]
        fn de_morgan_antiproduct_scalar(a in any::<Scalar<f64>>(), b in any::<Scalar<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            // LHS: complement(a ⋇ b)
            let lhs = mv_a.antiproduct(&mv_b).complement();

            // RHS: complement(a) * complement(b)
            let rhs = mv_a.complement() * mv_b.complement();

            prop_assert!(
                relative_eq!(lhs, rhs, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "De Morgan (antiproduct) failed: complement(a⋇b)={:?}, complement(a)*complement(b)={:?}",
                lhs, rhs
            );
        }
    }

    proptest! {
        /// De Morgan: complement(a * b) = complement(a) ⋇ complement(b)
        #[test]
        fn de_morgan_geometric_trivector(a in any::<Trivector<f64>>(), b in any::<Trivector<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            // LHS: complement(a * b)
            let lhs = (mv_a * mv_b).complement();

            // RHS: complement(a) ⋇ complement(b)
            let rhs = mv_a.complement().antiproduct(&mv_b.complement());

            prop_assert!(
                relative_eq!(lhs, rhs, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "De Morgan (geometric) failed: complement(a*b)={:?}, complement(a)⋇complement(b)={:?}",
                lhs, rhs
            );
        }

        /// De Morgan: complement(a ⋇ b) = complement(a) * complement(b)
        #[test]
        fn de_morgan_antiproduct_trivector(a in any::<Trivector<f64>>(), b in any::<Trivector<f64>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = b.into();

            // LHS: complement(a ⋇ b)
            let lhs = mv_a.antiproduct(&mv_b).complement();

            // RHS: complement(a) * complement(b)
            let rhs = mv_a.complement() * mv_b.complement();

            prop_assert!(
                relative_eq!(lhs, rhs, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "De Morgan (antiproduct) failed: complement(a⋇b)={:?}, complement(a)*complement(b)={:?}",
                lhs, rhs
            );
        }
    }

    proptest! {
        /// Project idempotency with normalized target: project(project(a, unit_b), unit_b) == project(a, unit_b)
        #[test]
        fn project_idempotent_line_point(a in any::<Line<f64>>(), unit_b in any::<Unitized<Point<f64>>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = unit_b.into_inner().into();

            let first = mv_a.project(&mv_b);
            let second = first.project(&mv_b);

            prop_assert!(
                relative_eq!(first, second, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Project idempotency failed: first={:?}, second={:?}",
                first, second
            );
        }
    }

    proptest! {
        /// Project idempotency with normalized target: project(project(a, unit_b), unit_b) == project(a, unit_b)
        #[test]
        fn project_idempotent_trivector_line(a in any::<Trivector<f64>>(), unit_b in any::<Unitized<Line<f64>>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = unit_b.into_inner().into();

            let first = mv_a.project(&mv_b);
            let second = first.project(&mv_b);

            prop_assert!(
                relative_eq!(first, second, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Project idempotency failed: first={:?}, second={:?}",
                first, second
            );
        }
    }

    proptest! {
        /// Project idempotency with normalized target: project(project(a, unit_b), unit_b) == project(a, unit_b)
        #[test]
        fn project_idempotent_trivector_point(a in any::<Trivector<f64>>(), unit_b in any::<Unitized<Point<f64>>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = unit_b.into_inner().into();

            let first = mv_a.project(&mv_b);
            let second = first.project(&mv_b);

            prop_assert!(
                relative_eq!(first, second, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Project idempotency failed: first={:?}, second={:?}",
                first, second
            );
        }
    }

    proptest! {
        /// Antiproject idempotency with normalized target: antiproject(antiproject(a, unit_b), unit_b) == antiproject(a, unit_b)
        #[test]
        fn antiproject_idempotent_line_trivector(a in any::<Line<f64>>(), unit_b in any::<Unitized<Trivector<f64>>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = unit_b.into_inner().into();

            let first = mv_a.antiproject(&mv_b);
            let second = first.antiproject(&mv_b);

            prop_assert!(
                relative_eq!(first, second, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Antiproject idempotency failed: first={:?}, second={:?}",
                first, second
            );
        }
    }

    proptest! {
        /// Antiproject idempotency with normalized target: antiproject(antiproject(a, unit_b), unit_b) == antiproject(a, unit_b)
        #[test]
        fn antiproject_idempotent_point_line(a in any::<Point<f64>>(), unit_b in any::<Unitized<Line<f64>>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = unit_b.into_inner().into();

            let first = mv_a.antiproject(&mv_b);
            let second = first.antiproject(&mv_b);

            prop_assert!(
                relative_eq!(first, second, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Antiproject idempotency failed: first={:?}, second={:?}",
                first, second
            );
        }
    }

    proptest! {
        /// Antiproject idempotency with normalized target: antiproject(antiproject(a, unit_b), unit_b) == antiproject(a, unit_b)
        #[test]
        fn antiproject_idempotent_point_trivector(a in any::<Point<f64>>(), unit_b in any::<Unitized<Trivector<f64>>>()) {
            let mv_a: Multivector<f64, Projective2> = a.into();
            let mv_b: Multivector<f64, Projective2> = unit_b.into_inner().into();

            let first = mv_a.antiproject(&mv_b);
            let second = first.antiproject(&mv_b);

            prop_assert!(
                relative_eq!(first, second, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Antiproject idempotency failed: first={:?}, second={:?}",
                first, second
            );
        }
    }

    proptest! {
        /// Bulk<Flector>.bulk_norm() should equal 1.0 (by definition of Bulk wrapper).
        #[test]
        fn bulk_flector_bulk_norm_matches_inner(b in any::<Bulk<Flector<f64>>>()) {
            // Use explicit trait syntax to specify the type
            let inner_bulk = <Flector<f64> as DegenerateNormed>::bulk_norm(b.as_inner());
            let wrapper_bulk = <Bulk<Flector<f64>> as DegenerateNormed>::bulk_norm(&b);

            prop_assert!(
                relative_eq!(inner_bulk, 1.0, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Inner bulk_norm should be 1.0, got {}", inner_bulk
            );
            prop_assert!(
                relative_eq!(wrapper_bulk, 1.0, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wrapper bulk_norm should be 1.0, got {}", wrapper_bulk
            );
        }

        /// Bulk<Flector>.weight_norm() should match inner's weight_norm (delegation).
        #[test]
        fn bulk_flector_weight_norm_delegates(b in any::<Bulk<Flector<f64>>>()) {
            // Use explicit trait syntax to specify the type
            let inner_weight = <Flector<f64> as DegenerateNormed>::weight_norm(b.as_inner());
            let wrapper_weight = <Bulk<Flector<f64>> as DegenerateNormed>::weight_norm(&b);

            prop_assert!(
                relative_eq!(inner_weight, wrapper_weight, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight norms should match: {} vs {}", inner_weight, wrapper_weight
            );
        }
    }

    proptest! {
        /// Bulk<Line>.bulk_norm() should equal 1.0 (by definition of Bulk wrapper).
        #[test]
        fn bulk_line_bulk_norm_matches_inner(b in any::<Bulk<Line<f64>>>()) {
            // Use explicit trait syntax to specify the type
            let inner_bulk = <Line<f64> as DegenerateNormed>::bulk_norm(b.as_inner());
            let wrapper_bulk = <Bulk<Line<f64>> as DegenerateNormed>::bulk_norm(&b);

            prop_assert!(
                relative_eq!(inner_bulk, 1.0, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Inner bulk_norm should be 1.0, got {}", inner_bulk
            );
            prop_assert!(
                relative_eq!(wrapper_bulk, 1.0, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wrapper bulk_norm should be 1.0, got {}", wrapper_bulk
            );
        }

        /// Bulk<Line>.weight_norm() should match inner's weight_norm (delegation).
        #[test]
        fn bulk_line_weight_norm_delegates(b in any::<Bulk<Line<f64>>>()) {
            // Use explicit trait syntax to specify the type
            let inner_weight = <Line<f64> as DegenerateNormed>::weight_norm(b.as_inner());
            let wrapper_weight = <Bulk<Line<f64>> as DegenerateNormed>::weight_norm(&b);

            prop_assert!(
                relative_eq!(inner_weight, wrapper_weight, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight norms should match: {} vs {}", inner_weight, wrapper_weight
            );
        }
    }

    proptest! {
        /// Bulk<Motor>.bulk_norm() should equal 1.0 (by definition of Bulk wrapper).
        #[test]
        fn bulk_motor_bulk_norm_matches_inner(b in any::<Bulk<Motor<f64>>>()) {
            // Use explicit trait syntax to specify the type
            let inner_bulk = <Motor<f64> as DegenerateNormed>::bulk_norm(b.as_inner());
            let wrapper_bulk = <Bulk<Motor<f64>> as DegenerateNormed>::bulk_norm(&b);

            prop_assert!(
                relative_eq!(inner_bulk, 1.0, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Inner bulk_norm should be 1.0, got {}", inner_bulk
            );
            prop_assert!(
                relative_eq!(wrapper_bulk, 1.0, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wrapper bulk_norm should be 1.0, got {}", wrapper_bulk
            );
        }

        /// Bulk<Motor>.weight_norm() should match inner's weight_norm (delegation).
        #[test]
        fn bulk_motor_weight_norm_delegates(b in any::<Bulk<Motor<f64>>>()) {
            // Use explicit trait syntax to specify the type
            let inner_weight = <Motor<f64> as DegenerateNormed>::weight_norm(b.as_inner());
            let wrapper_weight = <Bulk<Motor<f64>> as DegenerateNormed>::weight_norm(&b);

            prop_assert!(
                relative_eq!(inner_weight, wrapper_weight, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight norms should match: {} vs {}", inner_weight, wrapper_weight
            );
        }
    }

    proptest! {
        /// Bulk<Point>.bulk_norm() should equal 1.0 (by definition of Bulk wrapper).
        #[test]
        fn bulk_point_bulk_norm_matches_inner(b in any::<Bulk<Point<f64>>>()) {
            // Use explicit trait syntax to specify the type
            let inner_bulk = <Point<f64> as DegenerateNormed>::bulk_norm(b.as_inner());
            let wrapper_bulk = <Bulk<Point<f64>> as DegenerateNormed>::bulk_norm(&b);

            prop_assert!(
                relative_eq!(inner_bulk, 1.0, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Inner bulk_norm should be 1.0, got {}", inner_bulk
            );
            prop_assert!(
                relative_eq!(wrapper_bulk, 1.0, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wrapper bulk_norm should be 1.0, got {}", wrapper_bulk
            );
        }

        /// Bulk<Point>.weight_norm() should match inner's weight_norm (delegation).
        #[test]
        fn bulk_point_weight_norm_delegates(b in any::<Bulk<Point<f64>>>()) {
            // Use explicit trait syntax to specify the type
            let inner_weight = <Point<f64> as DegenerateNormed>::weight_norm(b.as_inner());
            let wrapper_weight = <Bulk<Point<f64>> as DegenerateNormed>::weight_norm(&b);

            prop_assert!(
                relative_eq!(inner_weight, wrapper_weight, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight norms should match: {} vs {}", inner_weight, wrapper_weight
            );
        }
    }

    proptest! {
        /// Bulk<Scalar>.bulk_norm() should equal 1.0 (by definition of Bulk wrapper).
        #[test]
        fn bulk_scalar_bulk_norm_matches_inner(b in any::<Bulk<Scalar<f64>>>()) {
            // Use explicit trait syntax to specify the type
            let inner_bulk = <Scalar<f64> as DegenerateNormed>::bulk_norm(b.as_inner());
            let wrapper_bulk = <Bulk<Scalar<f64>> as DegenerateNormed>::bulk_norm(&b);

            prop_assert!(
                relative_eq!(inner_bulk, 1.0, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Inner bulk_norm should be 1.0, got {}", inner_bulk
            );
            prop_assert!(
                relative_eq!(wrapper_bulk, 1.0, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Wrapper bulk_norm should be 1.0, got {}", wrapper_bulk
            );
        }

        /// Bulk<Scalar>.weight_norm() should match inner's weight_norm (delegation).
        #[test]
        fn bulk_scalar_weight_norm_delegates(b in any::<Bulk<Scalar<f64>>>()) {
            // Use explicit trait syntax to specify the type
            let inner_weight = <Scalar<f64> as DegenerateNormed>::weight_norm(b.as_inner());
            let wrapper_weight = <Bulk<Scalar<f64>> as DegenerateNormed>::weight_norm(&b);

            prop_assert!(
                relative_eq!(inner_weight, wrapper_weight, epsilon = REL_EPSILON, max_relative = REL_EPSILON),
                "Weight norms should match: {} vs {}", inner_weight, wrapper_weight
            );
        }
    }
}
